# Blockbuilders Morning Report — 2026-05-30

## 1. Executive Summary

Two unpatched infrastructure CVEs (Redis RCE + PostgreSQL heap overflow) are the top priority today — both require a human to check and update the Docker image pins before the next deploy. On the code side, FEAT-113 (joserfc migration) is implemented and tested but blocked on a one-line human verification before it can merge. PR #481 (apiFetch module-graph split) is open with unknown CI status and no Codex review yet. Five AI-ready refactor issues are queued and untouched overnight.

---

## 2. Overnight Activity

| Item | Status | Link | Notes |
|------|--------|------|-------|
| Nightly research (2026-05-29) | Merged | #483 | Redis + PostgreSQL CVEs flagged |
| Morning report (2026-05-29) | Merged | #482 | — |
| PR #474 — backtests API handler refactor | Merged | #474 | FEAT-445 complete |
| PR #481 — apiFetch cleanup + module-graph split | Open | #481 | CI unknown, no review |
| PR #467 — docs(ai): report 2026-05-27 | Open (stale) | #467 | Bot PR, 3 days old |

---

## 3. PRs Ready for Your Decision

| PR | CI | Codex review | Risk | Recommendation |
|----|-----|--------------|------|----------------|
| #481 apiFetch module-graph split | ⚠️ UNKNOWN | None | Low | **Do not merge** — confirm CI passes first, then review |
| #467 docs(ai): report 2026-05-27 | ⚠️ UNKNOWN | None | None | Merge or close (stale bot PR) |

---

## 4. Blocked Tasks

| Issue | Blocker | Decision needed |
|-------|---------|-----------------|
| FEAT-113 (joserfc) | AC-006 open: maintainer must run `pip show python-jose` on deployed env; should return "not found" | Approve merge once verified |
| #334 PostgreSQL 18 eval | `needs-human` + `needs-plan` labels; no plan exists | Prioritise or defer |

---

## 5. Questions for You

1. **Redis:** What tag is pinned in `docker-compose.yml` / `docker-compose.prod.yml`? Is it >= the May 2026 patched release? (5 CVEs, CVSS 7.7, including RCE)
2. **PostgreSQL:** Which major version and image tag is in use? Is it >= 14.23 / 15.18 / 16.14 / 17.10 / 18.4?
3. **FEAT-113:** Can you run `pip show python-jose` on the deployed backend and confirm it returns "not found"? Unblocks the merge.
4. **Tailwind v4:** Is there appetite to plan a migration sprint? XyFlow and shadcn/ui have moved ahead; the longer we wait the bigger the big-bang becomes.

---

## 6. Risks

| Risk | Severity | Action |
|------|----------|--------|
| Redis RCE (CVE-2026-23479 + 4 others) | 🔴 HIGH | Check + update Docker image tag today |
| PostgreSQL heap overflow (3 CVEs, May 14 patches) | 🔴 HIGH | Check + update Docker image tag today |
| FEAT-113 in main without human sign-off | 🟡 MEDIUM | One `pip show` command unblocks it |
| PR #481 CI status unknown | 🟡 MEDIUM | Re-run CI or check workflow run |
| Tailwind v4 ecosystem drift | 🟢 LOW | Plan sprint, not emergency fix |

---

## 7. Agent Usage This Cycle

| Agent | Tasks | Notes |
|-------|-------|-------|
| Claude Code Action | Nightly research (2026-05-29), morning reports | Last ai-usage-log entry: 2026-05-20 (log not updated overnight) |
| Codex | No activity this cycle | 5 ai-ready issues untouched |
| Jules | No activity this cycle | — |

---

## 8. Recommended Plan for Today

1. **Fix Redis Docker image** — check `docker-compose.yml` + `docker-compose.prod.yml`; update to patched tag if behind. (15 min)
2. **Fix PostgreSQL Docker image** — same files; update to the May 14 patch release for your major version. (10 min)
3. **Sign off FEAT-113** — run `pip show python-jose` on deployed env; if clean, merge the branch. (5 min)
4. **Unblock PR #481** — re-run CI; if green, request Codex review and merge. (10 min)
5. **Assign one AI-ready issue** — #449 (extract indicator logic, risk-low, frontend) or #446 (consolidate data fetching, risk-medium, has spec) are the best next candidates for Codex/Jules.
