# Blockbuilders Morning Report — 2026-07-27

## 1. Executive summary

The nightly research cycle for 2026-07-26 ran but is stuck unmerged as PR [#835](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/835) (`UNSTABLE` CI) — the same auto-merge pipeline failure flagged as the top operational risk in yesterday's report has now caught its own next cycle. The confirmed P1 finding from 2026-07-25 (Next.js 16.2.6 vulnerable to 9 CVEs, 4 HIGH, patched in 16.2.11) remains unfixed in `frontend/package.json`, and the Starlette floor pin is still unbounded. Zero feature/backend/frontend code has merged in 33 days (last non-docs commit 2026-06-24), and `tasks/ai-usage-log.csv` has no new rows since 2026-05-29. Three growth experiments stay blocked on PostHog config decisions (46–49 days idle) and issue #334 (PostgreSQL 18 eval) is now 71 days idle. Today's priority is unchanged from yesterday and now more urgent: get the security patches applied and fix (or bypass) the docs-bot merge pipeline.

## 2. Overnight activity

| Item | Status | Link | Notes |
|---|---|---|---|
| Nightly research 2026-07-26 | Opened, NOT merged | [#835](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/835) | CI `UNSTABLE`; content unavailable to this report until merged |
| `docs/ai/nightly-research/` latest file on `main` | Still 2026-07-25 | — | Yesterday's P1/P2 findings (Next.js, Starlette CVEs) are the most current verified data |
| `next` pin in `frontend/package.json` | Still 16.2.6 | — | Vulnerable to 9 CVEs (4 HIGH); fix not yet applied |
| `starlette` pin in `backend/requirements.txt` | Still unbounded `>=0.49.1` | — | Fix not yet applied |
| Feature/backend/frontend commits | None | — | 33 days since last code merge (2026-06-24) |
| `tasks/ai-usage-log.csv` | No new rows | — | Last entry 2026-05-29; matches code-merge gap |

## 3. PRs ready for your decision

| PR | CI | Codex review | Risk | Recommendation |
|---|---|---|---|---|
| None are code-change PRs | — | — | — | Snapshot shows 20 open PRs, all docs-bot report/research bots (10 `UNSTABLE` failing checks, 10 `DIRTY` conflicts); prior cycle counted 38 total, so this list may be partial. **Do not merge any with CI failing**, including #835 |

## 4. Blocked tasks

| Issue | Blocker | Decision needed |
|---|---|---|
| #618 NL-wedge onboarding A/B | #620 HITL: PostHog flag/metrics config | 46 days idle — schedule or deprioritize |
| #570 WJL retention A/B | #573 HITL: PostHog experiment config | 48 days idle — schedule or deprioritize |
| #555 Activation drop-off cohorts | #561 HITL: diagnostic funnel + PostHog cutover | 49 days idle — schedule or deprioritize |
| #334 PostgreSQL 18 evaluation | No plan drafted | 71 days idle, longest-idle item — approve `/plan-feature FEAT-334`? |
| #509/#510/#511 backend refactors | `needs-spec`, no spec yet | 54 days idle — assign to Codex Web for spec? |
| Nightly research 2026-07-26 | PR #835 CI `UNSTABLE` | Merge pipeline fix or manual merge decision |

## 5. Questions for you

1. Approve bumping `next` to `16.2.11`+ in `frontend/package.json` now (9 CVEs, 4 HIGH, patch-level fix, unfixed for 2+ days)?
2. Approve pinning `starlette>=1.0.1` (or `>=1.3.1`) in `backend/requirements.txt` (5 known CVEs, 2 HIGH, no confirmed live exploit path)?
3. PR #835 (last night's research) is itself stuck `UNSTABLE` — should someone manually merge it, or is a pipeline fix needed before anything else lands?
4. Should someone investigate branch protection/required checks this week, given two consecutive nightly-research cycles have now been blocked from `main`?
5. Should #618/#570/#555 be rescheduled (PostHog config work) or formally deprioritized after 46–49 days at zero comments?
6. Approve `/plan-feature FEAT-334` for the PostgreSQL 18 evaluation (71 days idle, longest-idle tracked item)?

## 6. Risks

| Risk | Severity | Action |
|---|---|---|
| Next.js 16.2.6 vulnerable to 9 CVEs (4 HIGH: SSRF, DoS, middleware bypass) — unfixed for 2+ days since identification | HIGH | Bump to 16.2.11+ — patch-only, low effort |
| Docs-bot PR pipeline now blocking its own second consecutive cycle (#835 `UNSTABLE`, same as #829 before it) | HIGH (operational) | Needs pipeline fix or manual-merge decision today |
| Starlette floor unpinned, 5 known CVEs (2 HIGH) below current floor | MEDIUM | Pin `>=1.0.1` minimum; no confirmed live exploit path today |
| Zero feature code merged in 33 days | MEDIUM | Confirm this is an intentional pause, not a stuck pipeline |

## 7. Agent usage this cycle

| Agent | Tasks | Notes |
|---|---|---|
| Claude Code (nightly-research) | 1 research cycle (2026-07-26) | Produced PR #835; content not yet reviewable — CI failing before merge |
| Claude Code (morning-report) | 1 report (this one) | — |
| Codex / Jules | 0 implementation tasks logged | No new rows in `tasks/ai-usage-log.csv` since 2026-05-29 |

## 8. Recommended plan for today

1. Manually merge or fix CI on PR #835 so last night's research findings become reviewable — the pipeline is now blocking two cycles in a row.
2. Approve and apply the `next` → 16.2.11+ security bump (patch-level, low risk, unfixed since 2026-07-25).
3. Approve and apply the `starlette>=1.0.1` floor pin in `backend/requirements.txt`.
4. Decide on the docs-bot PR backlog: fix branch protection/required checks, or change merge policy, before a third cycle gets stuck.
5. Make a go/no-go call on the three PostHog HITL gates (#620/#573/#561) and on `/plan-feature FEAT-334`.
