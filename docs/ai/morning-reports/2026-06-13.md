# Blockbuilders Morning Report — 2026-06-13

## 1. Executive Summary

A very productive overnight cycle: **~15 PRs merged** completing ACTIONS #8 (NL-wedge repair pass, anti-abuse 429 ceiling, cost telemetry, "slow down" messaging, ADR-0016) and ACTIONS #10 (How-Backtests-Work trust page made public + linked + removed from in-app nav, ADR-0017), plus the ACTIONS #11 golden-backtest regression suite PRD/design (#648) which spawned three new `ready-for-agent` issues (#649, #651, #654). Nightly research (#650) confirms the dependency-CVE backlog is essentially cleared, leaving PG18 (#334) as the only open security-adjacent item. **Two carryover problems from yesterday remain unresolved**: `Deploy Backend to VPS` is still failing on every push to `main` (SSH auth, same as 06-11), and the bot-authored docs PRs (10 of them, #512–#629) are still sitting open and unactioned. New finding: CI on bot PRs shows `action_required` (not actually running), which both explains the stuck PRs and motivates today's `#654` CI-bootstrap ticket.

## 2. Overnight Activity

| Item | Status | Link | Notes |
|---|---|---|---|
| ACTIONS #8 — NL-wedge repair pass + telemetry (#627, #628) | ✅ Merged | [PR627](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/627), [PR628](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/628) | Re-draft on invalid graph; `clean\|repaired\|declined` resolution telemetry |
| ACTIONS #8 — anti-abuse 429 + cost observability + UX (#635, #636, #637) | ✅ Merged | [PR635](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/635)–[PR637](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/637) | Per-user ceiling, token cost logging, distinct "slow down" 429 message |
| ADR-0016 + repair-pass glossary (#630, #623) | ✅ Merged | [PR630](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/630), [PR623](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/623) | Cost-bounding doc for NL wedge |
| ACTIONS #10 — Trust page (public, linked, ADR-0017, nav cleanup) | ✅ Merged | [PR644](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/644)–[PR647](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/647), [PR638](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/638) | How-Backtests-Work now a standalone public page, linked from results, removed from app nav |
| ACTIONS #11 — golden-backtest regression suite PRD/design | ✅ Merged | [PR648](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/648) | Spawned #649, #651, #654 (all `ready-for-agent`) |
| Onboarding A/B routing fork, ADR-0014 (#622) | ✅ Merged | [PR622](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/622) | Closes #619 |
| Nightly Research (06-12) | ✅ Ran | [PR650](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/650) | Dependency/CVE backlog cleared except PG18 (#334) |
| Deploy Backend to VPS | ❌ Failed (7th run in a row) | [run 27418184319](https://github.com/pierrickmartino/blockbuilders.tech.bmad/actions/runs/27418184319) | Still failing at "Deploy to VPS" step, unresolved since 06-10 — carryover from 06-11 report |
| 10 stale auto docs(ai) PRs (#512–#629) | ⚠️ Still open | — | Carryover from 06-11 report, not actioned |

## 3. PRs Ready for Your Decision

| PR | CI | Codex Review | Risk | Recommendation |
|---|---|---|---|---|
| #650 (docs(ai): research 2026-06-12) | ⚠️ `action_required` (Vercel only ran) | n/a | Low | Docs-only; approve the gated CI run or merge once unblocked |
| #629/#575/#569/#564/#537/#527/#526/#523/#520/#512 (auto docs reports) | ✅ Vercel pass, but main CI gated | n/a | Low | Stale, mostly DIRTY merge state — bulk close (2nd day flagged) |

## 4. Blocked Tasks

| Issue | Blocker | Decision Needed |
|---|---|---|
| Deploy Backend to VPS | SSH auth failing 7 consecutive runs since 06-10 | Fix `SSH_PRIVATE_KEY`/host secret — backend changes (NL-wedge, trust page) not live |
| #620 ACTIONS #7 Slice 2 — PostHog `onboarding_ab` experiment | `ready-for-human`, manual PostHog config | You configure flag/metrics/guardrails in PostHog console |
| #334 / FEAT-114 — PostgreSQL 18 evaluation | `needs-human` + `needs-plan`, stale 4+ weeks | Approve plan, deprioritize, or grant staging access for benchmark |
| CI `action_required` on bot PRs | Workflow approval gate not understood/configured | Confirm whether this is intentional; relates to #654 |

## 5. Questions for You

1. **Deploy Backend to VPS** has now failed on every push to `main` since 06-10 15:49 (same `Deploy to VPS` step) — OK for me to investigate the SSH key/host secret, or is someone already on it outside this repo?
2. **CI shows `action_required`** on bot-authored PRs (e.g. #650) instead of running — is this an intentional approval gate for bot actors, or a misconfiguration that should be fixed alongside #654 (CI bootstrap for backend pytest)?
3. **10 stale auto "docs(ai)" PRs** (#512–#629) are still open/DIRTY for the second day running — OK to bulk close now?
4. **#620** (PostHog `onboarding_ab` 50/50 experiment + SRM/cost guardrails) is ready for you — set it up in PostHog console?
5. **#334 (PostgreSQL 18 eval)** has sat in `needs-human`/`needs-plan` for 4+ weeks; PG16.14 is confirmed CVE-clean — deprioritize further, or should I produce the implementation plan now?
6. **#649/#651/#654** (golden-backtest regression suite slices + CI pytest bootstrap) are all `ready-for-agent` — queue them for today's/tonight's agent cycle?

## 6. Risks

| Risk | Severity | Action |
|---|---|---|
| Backend deploy pipeline broken (SSH auth), 3 days unresolved | HIGH | Fix deploy SSH key secret — backend stale on VPS, blocking all recent feature work from going live |
| CI `action_required` on bot PRs — tests not actually running pre-merge | MEDIUM | Investigate workflow run-approval settings; prioritize #654 (CI pytest bootstrap) |
| Stale auto-PR pile-up (10 open, mostly DIRTY), 2nd day flagged | LOW | Bulk close |
| `tasks/ai-usage-log.csv` not updated since 2026-05-29 (~25 merged PRs untracked) | LOW | Backfill or fix logging step |
| #334 PG18 eval stuck `needs-human` 4+ weeks (no CVE urgency per research) | LOW | Decide: plan now or deprioritize |

## 7. Agent Usage This Cycle

| Agent | Tasks | Notes |
|---|---|---|
| Claude Code Agent | ~15 PRs merged: ACTIONS #8 (6 PRs: #623, #627, #628, #630, #635–#637), ACTIONS #10 (5 PRs: #638, #644–#647), ACTIONS #11 PRD (#648), ACTIONS #7 routing (#622), nightly research (#650) | All merged 06-12; spawned 3 new `ready-for-agent` issues (#649, #651, #654) |
| Codex / Jules | None observed this cycle | `ai-usage-log.csv` last entry 2026-05-29 — logging gap persists |

## 8. Recommended Plan for Today

1. Fix `Deploy Backend to VPS` SSH auth — 3-day-old blocker, all recent backend work (NL-wedge repair/anti-abuse, trust page) is undeployed.
2. Resolve the CI `action_required` gate on bot PRs (tie to #654 CI-bootstrap ticket) so merges get real pytest/lint coverage.
3. Bulk-close the 10 stale auto docs(ai) PRs (#512–#629) and merge/approve #650.
4. Configure #620 PostHog `onboarding_ab` experiment (human task).
5. Queue #649/#651 (golden-backtest goldens) for agent build now that the PRD/design has landed.
