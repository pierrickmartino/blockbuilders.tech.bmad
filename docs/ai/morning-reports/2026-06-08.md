# Blockbuilders Morning Report — 2026-06-08

## 1. Executive summary

Overnight, Claude Code shipped two of the four `entry_path` cohort slices (#556 persisted + stamped, #557 resolver in progress with no PR yet) and closed both queued frontend refactors (#449, #450); CI is green across the board, but **every merge to `main` since at least 2026-05-21 has failed to deploy to the VPS** with an SSH "no key found" error — a 15-run failure streak that has gone unflagged until now and means production has been running stale code for over two weeks. That deploy-pipeline break is today's must-fix; everything else (six stale bot doc PRs, one merge conflict, the parked PostgreSQL 18 evaluation) is routine cleanup.

## 2. Overnight activity

| Item | Status | Link | Notes |
|---|---|---|---|
| #556 persist `Strategy.entry_path` | Merged | [PR #563](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/563) | CI green; closed issue #556 |
| Cohort dimension docs split | Merged | [PR #562](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/562) | ADR-0009 + runbook + ANALYTICS_SETUP sync |
| #449 indicator extraction | Closed (implemented) | [issue #449](https://github.com/pierrickmartino/blockbuilders.tech.bmad/issues/449) | From research P0 queue |
| #450 BlockType derivation | Closed (implemented) | [issue #450](https://github.com/pierrickmartino/blockbuilders.tech.bmad/issues/450) | From research P0 queue |
| #557 cohort resolver | **In progress, no PR yet** | branch `claude/chat-session-5Yls6` | Commit `5e86574` pushed 09:16 UTC; not opened as PR |
| Deploy Backend to VPS | **Failing — 15 consecutive runs** | [run 27124094773](https://github.com/pierrickmartino/blockbuilders.tech.bmad/actions/runs/27124094773) | `ssh: no key found` since ≥2026-05-21 |

## 3. PRs ready for your decision

| PR | CI | Codex review | Risk | Recommendation |
|---|---|---|---|---|
| [#537 report 2026-06-07](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/537) | ✅ Vercel pass | n/a (bot docs) | Low | Merge |
| [#527 report 2026-06-06](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/527) | ✅ Vercel pass | n/a | Low | Merge |
| [#526 research 2026-06-05](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/526) | ✅ Vercel pass | n/a | Low | Merge |
| [#523 research 2026-06-04](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/523) | ✅ Vercel pass | n/a | Low | Merge |
| [#520 research 2026-06-03](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/520) | ✅ Vercel pass | n/a | Low | Merge |
| [#512 report 2026-06-03](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/512) | ✅ Vercel pass, but **CONFLICTING / DIRTY** | n/a | Low | Close (superseded) or rebase — main has moved 5 days past it |

*No PR is marked ready that has failing CI — all six listed are bot-authored docs PRs; #512 is the only one blocked, by a merge conflict, not CI.*

## 4. Blocked tasks

| Issue | Blocker | Decision needed |
|---|---|---|
| #334 PostgreSQL 18 evaluation | PG 18 not GA yet; PG 16.14 patched and in use | Confirm: keep parked until GA announced? |
| #561 HITL diagnostic-funnel build in PostHog | Operational-only (no code can do it) | Schedule your time to build the funnel/tile/breakdown per runbook §7 |
| #557 cohort resolver | Branch pushed but no PR opened | Decide whether to open PR now or let tonight's cycle finish the slice |

## 5. Questions for you

1. **Deploy pipeline (urgent):** The VPS SSH deploy key is rejected (`ssh: no key found`) on every merge since 2026-05-21 (15 straight failures, including this morning's #563 merge). Can you rotate/re-add the `appleboy/ssh-action` SSH key secret so production starts receiving merges again?
2. **PR #512** has a merge conflict with `main` and is 5 days stale (superseded by #520/#523/#526/#527). Close it, or should it be rebased and merged?
3. **Bot doc PRs #520/#523/#526/#527/#537** — all green, all mergeable. OK to merge them in a batch, or do you want to review each report first?
4. **#561 (PostHog diagnostic funnel)** — when can you carve out time to build the funnel, time-to-activation tile, and cohort cutover annotation per runbook §7?
5. **#557 cohort resolver** — branch `claude/chat-session-5Yls6` has the commit but no PR. Want it opened for review now, or left for tonight's cycle to finish and PR?

## 6. Risks

| Risk | Severity | Action |
|---|---|---|
| VPS deploy failing on every merge (SSH key) since ≥2026-05-21 | **HIGH** | Rotate SSH key secret; verify next merge deploys |
| Production running ≥15 merges behind `main` (incl. security/dep work merged this week) | **HIGH** | Same fix unblocks backlog of undeployed changes |
| PR #512 merge conflict, 5 days stale | LOW | Close or rebase |
| 6 bot doc PRs queued unmerged | LOW | Batch-merge to stop the queue growing |
| `tasks/ai-usage-log.csv` has no entries since 2026-05-29 despite continuous activity (#556/#557/#562/#563 this week) | LOW | Agent should append entries each run; gap makes usage tracking blind for the last ~10 days |

## 7. Agent usage this cycle

| Agent | Tasks | Notes |
|---|---|---|
| Claude Code Action | #556 (persist entry_path, merged #563), docs split (merged #562), #557 resolver (pushed, no PR) | All on `claude/chat-session-*` branches; CI green on each |
| — | #449, #450 implementation | Closed overnight; no log entry or PR link found to attribute the implementing agent — verify in `gh pr list --search "449"` if attribution is needed |
| (log gap) | — | `ai-usage-log.csv` last entry 2026-05-29 — cannot attribute model/tokens/test results for any work done 2026-05-30 → today |

## 8. Recommended plan for today

1. **Fix the VPS deploy SSH key** — highest priority; production has been stuck on a 2-week-old build.
2. **Batch-merge the five green bot doc PRs** (#520, #523, #526, #527, #537); close #512 as superseded/conflicting.
3. **Decide on #557** — open the PR for `claude/chat-session-5Yls6` now or let tonight's cycle finish it, so the cohort-resolver slice doesn't stall the #555 epic.
4. **Schedule the #561 PostHog dashboard build** (HITL-only; funnel, time-to-activation tile, cutover annotation).
5. **Restore `ai-usage-log.csv` logging** for the agent runs from 2026-05-30 onward so usage stays auditable.
