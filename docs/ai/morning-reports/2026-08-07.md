# Blockbuilders Morning Report — 2026-08-07

## 1. Executive summary

No code shipped overnight (or in the last 44 days — last backend/frontend commit was `e248b02` on 2026-06-24); the only overnight activity was the automated nightly-research bot opening PR #853, which re-confirms three unfixed P1/P2 security items (Next.js CVEs, Redis unpatched RCE-class bug, Starlette floor-pin gap) and reports a live npm supply-chain worm that does **not** touch this repo's resolved lockfile. The docs-bot PR backlog has grown to 44 open, unmerged automation PRs (oldest 65 days), and three PostHog-gated feature pairs remain stuck 54–57 days on a human HITL config step — both are now the primary drag on throughput, not code risk.

## 2. Overnight activity

| Item | Status | Link | Notes |
|---|---|---|---|
| Nightly research 2026-08-07 | PR opened, unmerged | [#853](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/853) | mergeStateStatus UNSTABLE; content not yet in `main` |
| Morning report 2026-08-06 | PR opened, unmerged | [#852](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/852) | mergeStateStatus UNSTABLE |
| Backend/frontend code | None | — | Last non-docs commit `e248b02`, 2026-06-24 (44 days ago) |
| Issue activity (#618/#620/#570/#573/#555/#561/#334/#509/#510/#511) | Unchanged | — | Zero new comments on any tracked issue since last cycle |

## 3. PRs ready for your decision

| PR | CI | Codex review | Risk | Recommendation |
|---|---|---|---|---|
| #853 research 2026-08-07 | Only Vercel check ran; mergeStateStatus UNSTABLE | none | low | **Not ready** — resolve why required checks aren't completing before merging |
| #852 report 2026-08-06 | Only Vercel check ran; mergeStateStatus UNSTABLE | none | low | **Not ready** — same as above |
| 42 older docs-bot PRs (#512…#845) | Mixed CLEAN/DIRTY/UNSTABLE | none | low | **Not ready** — none auto-merged; needs a dedicated cleanup pass (see §8) |

No feature/backend/frontend PRs are open. Nothing in this cycle meets the bar to merge.

## 4. Blocked tasks

| Issue | Blocker | Decision needed |
|---|---|---|
| #618 NL-wedge onboarding A/B | #620 HITL: configure `onboarding_ab` PostHog flag/metrics | Schedule config work or relabel deprioritized (54 days idle) |
| #570 WJL retention A/B | #573 HITL: configure `wjl_retention_ab` PostHog experiment | Same (56 days idle) |
| #555 Activation drop-off cohorts | #561 HITL: build diagnostic funnel + `time_to_activation` in PostHog | Same (57 days idle) |
| #334 PostgreSQL 18 evaluation | `needs-human` + `needs-plan`; draft spec `FEAT-114` exists but unreferenced | Approve `/plan-feature FEAT-114` or continue deferring (79 days idle) |

## 5. Questions for you

1. Approve bumping `next` 16.2.6 → 16.3.0 to fix 9 CVEs (4 HIGH), flagged for the 6th consecutive research cycle?
2. Approve bumping `redis:7.4.9-alpine` → `redis:7.4.10-alpine` in both compose files (unpatched RCE-class bug, no CVE ID yet, one-line fix)?
3. Approve raising the `starlette` floor pin to `>=1.0.1` (or `>=1.3.1`) in `backend/requirements.txt` (5 known CVEs, 2 HIGH)?
4. What's the status on the PostHog HITL gates #620/#573/#561 — schedule this week, or formally relabel #618/#570/#555 as deprioritized?
5. Should the 44-PR docs-bot backlog (oldest #512, 65 days) get a dedicated merge/triage pass, or should the automation be changed to stop opening new ones until it's cleared?
6. Advance `FEAT-114` (PostgreSQL 18 eval) to `/plan-feature` now, given it's the single longest-idle tracked item?

## 6. Risks

| Risk | Severity | Action |
|---|---|---|
| Next.js pinned 16.2.6 has 9 unpatched CVEs (4 HIGH, incl. SSRF) | High | Approve bump to 16.3.0 (Q1) |
| Redis 7.4.9-alpine has unpatched auth-RCE-class use-after-free | High | Approve bump to 7.4.10-alpine (Q2) |
| npm supply-chain worm ("ChainDrop") active in the wild | Medium | No repo action needed — lockfile confirmed clean; avoid `npm update`/lockfile regen on ESLint/Storybook deps until ecosystem confirms clean |
| Starlette below CVE-patched floor (5 CVEs, 2 HIGH) | Medium | Approve floor-pin bump (Q3) |
| 44 open docs-bot PRs, none merging (oldest 65 days) | Medium | Dedicated cleanup pass (Q5) |
| Zero code merged to `main` in 44 days | Medium | Clarify with team whether this is deliberate (HITL-gated backlog) or stalled |
| `tasks/ai-usage-log.csv` has no entries since 2026-05-29 despite ongoing nightly-research runs | Low | Usage log not being maintained — reduces visibility into agent activity; worth a fix so this report's §7 isn't blind |

## 7. Agent usage this cycle

| Agent | Tasks | Notes |
|---|---|---|
| Claude Code (nightly-research) | 1 (research 2026-08-07, PR #853) | Ran on schedule; output not yet merged to `main` |
| Claude Code (morning-report) | 1 (report 2026-08-06, PR #852) | Ran on schedule; output not yet merged to `main` |
| Codex / Jules | 0 | No implementation activity — all active issues are `needs-human`/`needs-spec`/HITL-blocked |
| **Gap** | — | `tasks/ai-usage-log.csv` last row is 2026-05-29 — not updated for ~10 weeks of subsequent nightly-research/report cycles. Treat this table as derived from PR history, not the log. |

## 8. Recommended plan for today

1. Decide Q1–Q3 (Next.js, Redis, Starlette bumps) — all three are one-line, no-code-impact fixes with patches already available; approve and hand to Codex Web.
2. Give a direct status read on the three PostHog HITL gates (#620/#573/#561) — 54–57 days idle is now the single biggest blocker to feature throughput.
3. Schedule a cleanup pass on the 44-PR docs-bot backlog (oldest #512, 65 days) before it grows further.
4. Approve or defer `/plan-feature FEAT-114` (PostgreSQL 18 eval) — longest-idle tracked item at 79 days.
5. Fix `tasks/ai-usage-log.csv` logging so nightly-research/report runs are captured — current visibility into agent usage relies on manually diffing PR history.
