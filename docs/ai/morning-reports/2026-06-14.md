# Blockbuilders Morning Report — 2026-06-14

## 1. Executive Summary

A quiet overnight cycle: Nightly Research (06-13) ran successfully and merged via #659, and PR #658 (injectable drafter rate limiter) merged with **CI green**, fixing the CI failure carried over from #656/#657. The one open carryover problem got worse: **Deploy Backend to VPS has now failed on every push to `main` for a 3rd day running** (SSH "no key found" + connection timeout), so none of yesterday's NL-wedge/rate-limiter changes are live on the VPS. The 10 stale auto-generated `docs(ai)` PRs (#512–#629) remain open and unactioned for a 3rd day. Today's top build candidate per nightly research is **#651** (golden RSI pipeline + shared helpers — foundational for #652/#653), and **#620** (PostHog `onboarding_ab` experiment) is still an unconfigured HITL gap with the routing fork live for ~2 days.

## 2. Overnight Activity

| Item | Status | Link | Notes |
|---|---|---|---|
| Nightly Research 06-13 | ✅ Ran, merged | [#659](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/659) | Golden-suite + dependency findings, see §5/§6 |
| PR #658 — injectable drafter rate limiter | ✅ Merged, CI green | [#658](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/658) | Fixed CI failure carried from #656/#657 |
| PR #657 — codex review follow-ups | ✅ Merged (CI failed on this merge) | [#657](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/657) | Fixed by #658 |
| Deploy Backend to VPS | ❌ Failing again (#657 & #658) | [run 27470758049](https://github.com/pierrickmartino/blockbuilders.tech.bmad/actions/runs/27470758049) | SSH `ssh: no key found` + `i/o timeout` — 3rd day |

## 3. PRs Ready for Your Decision

| PR | CI | Codex Review | Risk | Recommendation |
|---|---|---|---|---|
| #650 (docs research 06-12) | Vercel ✅ only, no backend/frontend CI | n/a | Low | Stale, superseded by merged #659 — close |
| #629/#575/#569/#564/#537/#527/#526/#523/#520/#512 (9 more auto docs PRs) | Vercel ✅ only, mostly `DIRTY` merge state | n/a | Low | Stale 2–11 days, 3rd day flagged — bulk close |

No new feature PRs await review — #657/#658 already merged.

## 4. Blocked Tasks

| Issue | Blocker | Decision Needed |
|---|---|---|
| Deploy Backend to VPS | SSH key invalid + TCP timeout, failing 3+ days straight | Rotate/verify SSH deploy secret + VPS reachability |
| #620 — PostHog `onboarding_ab` | HITL, experiment not configured, routing fork live ~2 days | Configure in PostHog console |
| #334 — PG18 evaluation | `needs-human`/`needs-plan`, blocked on AC-002 (prod PG version) | Grant access or defer |
| #654 — CI bootstrap | May be redundant — `ci.yml` already covers its AC | Confirm close vs. rescope |

## 5. Questions for You

1. **Deploy Backend to VPS** has now failed on every push for a 3rd day (7 in a row as of yesterday, now also #657 and #658) — OK for me to investigate the SSH deploy secret / VPS network, or is this being handled outside this repo?
2. **Bulk-close the 10 stale auto `docs(ai)` PRs** (#512–#629)? Flagged for the 3rd day, several `DIRTY`/conflicting.
3. **#654** — nightly research believes `.github/workflows/ci.yml` already satisfies its AC. Close with a pointer comment, or rescope to "verify #651–653 goldens run green"?
4. **#620** (PostHog `onboarding_ab`) still unconfigured after ~2 days live — configure now, or confirm the flag defaults safely meanwhile?
5. **Start #651** (golden RSI pipeline + shared helpers) as today's main agent build — it's foundational for #652/#653?

## 6. Risks

| Risk | Severity | Action |
|---|---|---|
| Deploy Backend to VPS failing 3rd day straight — backend changes since before 06-12 not live | HIGH | Fix SSH deploy secret / VPS reachability |
| `anthropic>=0.69.0` floor vs. latest 0.109.1, drafter surface changing daily | MEDIUM | Tighten pin, verify in CI |
| 10 stale docs PRs piling up, 3rd day flagged | LOW | Bulk close |
| `tasks/ai-usage-log.csv` last entry 2026-05-29 — ~30 merged PRs since untracked | LOW | Backfill or fix logging step |

## 7. Agent Usage This Cycle

| Agent | Tasks | Notes |
|---|---|---|
| Claude Code (Nightly Research) | 1 | 06-13 report merged via #659 |
| Codex/Claude (PR #657, #658) | 2 | Both merged; #658 fixed #657's CI failure |
| `ai-usage-log.csv` | 0 entries | Still stale since 2026-05-29 |

## 8. Recommended Plan for Today

1. Investigate & fix Deploy Backend to VPS (SSH key + timeout) — highest priority, 3rd day of failures.
2. Bulk-close the stale `docs(ai)` PR backlog (#512–#629), resolve #650.
3. Triage #654 against existing `ci.yml` — close or rescope.
4. Build #651 (golden RSI pipeline + shared helpers) — foundational for #652/#653.
5. Configure #620 PostHog `onboarding_ab` experiment (HITL, ~2 days unmeasured).
