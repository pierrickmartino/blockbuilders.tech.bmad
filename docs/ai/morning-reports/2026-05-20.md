# Blockbuilders Morning Report — 2026-05-20

## 1. Executive Summary

Two P0 security issues from yesterday's nightly research are resolved: Redis was bumped to 7.4.9-alpine (PR #373, Jules) and FEAT-113 (python-jose → joserfc migration) was implemented and merged (PR #377). One P0 remains open: Next.js may still be below 16.2.6, which is vulnerable to 13 security advisories including an unauthenticated DoS. Five issues are queued waiting for your plan-approval decision, and several dashboard frontend tasks are blocked by a missing npm runtime in the CI environment.

---

## 2. Overnight Activity

| Item | Status | Link | Notes |
|---|---|---|---|
| Redis bump 7.4.6 → 7.4.9-alpine | ✅ Merged | PR #373 | Jules; CI fix commit followed |
| FEAT-113 joserfc migration | ✅ Merged | PR #377 | Docs sync + implementation; python-jose removed |
| FEAT-113 spec correction (non-existent version) | ✅ Merged | PR #375 | Target corrected to joserfc 1.6.5 |
| Issue #372 converted | ✅ Merged | PR #376 | Codex |
| FEAT-118 spec added (FastAPI 0.136.1 upgrade) | ✅ Committed | — | pierrickmartino direct; separate from FEAT-116 plan |
| Skills added (caveman, grill-me, improve-architecture) | ✅ Merged | PR #369 | |
| Nightly research 2026-05-19 | ✅ Published | PR #368 | |

---

## 3. PRs Ready for Your Decision

| PR | CI | Codex review | Risk | Recommendation |
|---|---|---|---|---|
| — | — | — | — | No open PRs |

---

## 4. Blocked Tasks

| Issue | Blocker | Decision needed |
|---|---|---|
| Dashboard distill / hardening / clarify / flagship / FEAT-103 | `npm`/`pnpm`/`yarn` not on PATH; `node_modules` absent | Fix CI environment or accept manual-only smoke sign-off |
| FEAT-113 AC-006 | `pip show python-jose` "not found" check was unchecked at merge | Verify manually post-merge or accept as documented risk |
| #338 Canvas undo/redo | needs-plan, needs-human | Approve spec for Codex Web |
| #339 Cmd+K command palette | needs-plan, needs-human | Approve spec for Codex Web |
| #340 Backtest badges + Sheet drawer | needs-plan, needs-human | Approve spec for Codex Web |
| #331 RQ 2.x + redis-py 7.x migration | needs-plan, needs-human | Approve FEAT-115 plan for Codex Web |
| #334 PostgreSQL 18 evaluation | risk-high, needs-human | Approve research scope; target pg 18.4 |

---

## 5. Questions for You

1. **Next.js security (P0):** What is `next` in `frontend/package-lock.json`? If below 16.2.6, should a patch feature be created today (13 advisories, unauthenticated DoS)?
2. **FEAT-118 vs FEAT-116:** FEAT-116 is the approved upgrade plan; FEAT-118 is a new spec also targeting FastAPI 0.136.1. Are these the same scope or should one be closed?
3. **Issues #338 / #339 / #340:** Approve for Codex Web implementation now, or do specs need a planning review first?
4. **npm blocker:** Is the absent npm runtime a known environment gap, or should the CI setup be fixed so dashboard tasks can be verified?

---

## 6. Risks

| Risk | Severity | Action |
|---|---|---|
| `next` < 16.2.6 — 13 CVEs including unauthenticated DoS | P0 | Check lockfile today; create patch feature if unpatched |
| FEAT-113 AC-006 unchecked at merge (pip show python-jose) | Medium | Verify python-jose absent in deployed venv |
| Dashboard / FEAT-103 tasks have zero verification (npm absent) | Medium | Resolve PATH blocker or gate deployment on manual QA |
| RQ pickle serialization — architectural risk | Low | Evaluate JSONSerializer in FEAT-115 scope |
| Tailwind v3 hold while XyFlow UI ships v4 support | Low | Monitor; no action until migration decision |

---

## 7. Agent Usage This Cycle

| Agent | Tasks | Notes |
|---|---|---|
| Jules | Redis bump PR #373, FEAT-113 spec correction | Both merged; CI fix required on Redis PR |
| Codex Web | FEAT-113 docs sync (PR #377), issue #372 conversion (PR #376), FEAT-118 spec | |
| Claude Code | Nightly research 2026-05-19, morning report 2026-05-20 | |
| pierrickmartino | FEAT-118 spec commit, skills PR #369, "Migrate JWT auth to joserfc" commit | Direct commits outside agent pipeline |

---

## 8. Recommended Plan for Today

1. **Check `next` version** — run `npm ls next --depth=0` in `frontend/`; if < 16.2.6, create a risk-low feature today (Jules).
2. **Approve issues #338, #339, #340** — all are bounded frontend features with confirmed implementation references; unblock Codex Web.
3. **Clarify FEAT-118 vs FEAT-116** — close the duplicate or define the split before implementation begins.
4. **Verify FEAT-113 AC-006** — confirm `pip show python-jose` returns "not found" in the deployed venv; close the checkbox.
5. **Resolve npm PATH blocker** — unblock dashboard and FEAT-103 verification so those tasks can move to done.
