# Blockbuilders Morning Report — 2026-06-10

## 1. Executive summary

Overnight, the WhatYouLearned retention A/B initiative (ADR-0010) finished its agent-buildable slices: PR #578 wired the card to the `wjl_retention_ab` experiment, and #579/#580 landed env var docs and a HITL runbook for the two PostHog-side issues (#561, #573) that now need a human to configure in PostHog. Nightly research (#577, merged) flagged **4 critical CVEs** — checking them against the running stack: Redis (`redis:7.4.9-alpine`) is already patched for both Redis CVEs, but `postgres:16-alpine` is still exposed to CVE-2025-8715 (pg_dump RCE) and CVE-2025-8713 (RLS bypass), and the Starlette pin (`>=0.49.1`) doesn't guarantee the `>=1.0.1` needed for CVE-2026-48710. Separately, 9 `docs(ai)` bot PRs (#512–#575) are piling up unmerged back to 06-03, one with merge conflicts, and `tasks/ai-usage-log.csv` hasn't been updated since 05-29 despite ~15 merges since.

## 2. Overnight activity

| Item | Status | Link | Notes |
|---|---|---|---|
| feat: wire WhatYouLearnedCard to `wjl_retention_ab` | Merged | [#578](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/578) | Closes #572 |
| docs: `NEXT_PUBLIC_DEV_FORCE_WJL_VARIANT` env var | Merged | [#579](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/579) | Refs #571, #572 |
| docs: HITL PostHog runbook for #561 / #573 | Merged | [#580](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/580) | Issues stay open — human PostHog config still needed |
| docs(ai): nightly research 2026-06-09 | Merged | [#577](https://github.com/pierrickmartino/blockbuilders.tech.bmad/pull/577) | Surfaced 4 critical CVEs (see Risks) |
| `tasks/ai-usage-log.csv` | Stale | `tasks/ai-usage-log.csv` | Last row 2026-05-29; ~15 PRs merged since not logged |

## 3. PRs ready for your decision

| PR | CI | Codex review | Risk | Recommendation |
|---|---|---|---|---|
| #520, #523, #526, #527, #537, #564, #569, #575 (`docs(ai)` reports/research, 06-03→06-09) | ✅ green, mergeable=CLEAN | none (bot docs) | Low | Batch-merge or squash; backlog is just growing |
| #512 `docs(ai): report 2026-06-03` | ✅ green but mergeState=DIRTY (conflicts) | none | Low | Rebase or close as superseded by later reports |

No feature PRs are currently open — all of last night's feature work already merged (see §2).

## 4. Blocked tasks

| Issue | Blocker | Decision needed |
|---|---|---|
| #561 HITL: drop-off diagnostic funnel, time_to_activation, cohort cutover | Requires human PostHog configuration | Run the new runbook from #580 to set up funnel/cohorts |
| #573 HITL: configure `wjl_retention_ab` experiment & metrics | Requires human PostHog configuration | Run the new runbook from #580 to launch the experiment |
| #334 Evaluate PostgreSQL 18 for backtest workloads | `needs-plan` + `needs-human`, untouched since 05-17, now overlaps PG security CVEs (§6) | Approve a combined PG-upgrade plan or close/deprioritize |

## 5. Questions for you

1. `postgres:16-alpine` is exposed to CVE-2025-8715 (critical, pg_dump RCE) and CVE-2025-8713 (RLS bypass) — fixes are PG 17.9+ / 18.3. OK to open a `risk-high` plan-only ticket to upgrade (and fold in #334's PG18 evaluation)?
2. `backend/requirements.txt` pins `starlette>=0.49.1`, but CVE-2026-48710's fix is Starlette ≥1.0.1. Should I verify the resolved venv version and tighten the pin if it's below 1.0.1?
3. 9 `docs(ai)` bot PRs (#512–#575) are unmerged back to 06-03, one (#512) conflicting. OK to merge the 8 clean ones and close/rebase #512?
4. `tasks/ai-usage-log.csv` hasn't been updated since 05-29 (AGENTS.md requires it after every task). Backfill from PR history, or is this log being deprecated in favor of PR tracking?
5. Issue #334 (PG18 eval) has sat since 05-17 — still a priority for this sprint, or push to backlog?

## 6. Risks

| Risk | Severity | Action |
|---|---|---|
| CVE-2025-8715 / CVE-2025-8713 — `postgres:16-alpine` vulnerable (pg_dump RCE, RLS bypass) | CRITICAL | Plan upgrade to `postgres:17.9+`/`18.3` (Q1, Q5) |
| CVE-2026-48710 — Starlette host-validation bypass; pin `>=0.49.1` doesn't guarantee fixed `>=1.0.1` | HIGH | Verify resolved Starlette version; pin if needed (Q2) |
| 9 unmerged `docs(ai)` bot PRs, one conflicting | MEDIUM | Batch-merge / clean up backlog (Q3) |
| `tasks/ai-usage-log.csv` 12 days stale, ~15 merges unlogged | LOW | Backfill or formally deprecate (Q4) |
| PyJWT CVE-2026-48526/32597 (from nightly research) | N/A | No action — project uses `joserfc==1.6.5`, not PyJWT |
| Redis CVE-2025-49844 / CVE-2026-23631 (from nightly research) | N/A | Already patched — `redis:7.4.9-alpine` covers both |

## 7. Agent usage this cycle

*(derived from merged PRs/git log — `tasks/ai-usage-log.csv` not updated, see §6)*

| Agent | Tasks | Notes |
|---|---|---|
| Claude Code (chat sessions) | #576 (experiment variant reader, ADR-0010), #578 (wire WJL card), #579 (env var docs), #580 (HITL runbook) | 4 PRs merged overnight, all `claude/chat-session-*` branches |
| Claude Code Action (nightly) | #577 nightly research 2026-06-09 | Flagged 4 critical CVEs, validated against current stack above |

## 8. Recommended plan for today

1. Open a `risk-high` plan-only ticket: bump `postgres:16-alpine` → `17.9+`/`18.3` for CVE-2025-8715/8713, folding in #334's PG18 evaluation.
2. Verify the resolved Starlette version against CVE-2026-48710 (`>=1.0.1`); tighten `backend/requirements.txt` pin if needed.
3. Clean up the `docs(ai)` PR backlog: merge #520/#523/#526/#527/#537/#564/#569/#575, rebase or close #512.
4. Run the PostHog HITL runbook (#580) to unblock #561 and #573.
5. Backfill or deprecate `tasks/ai-usage-log.csv` per AGENTS.md tracking requirements.
