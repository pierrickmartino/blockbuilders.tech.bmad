# Blockbuilders Morning Report — 2026-05-21

## 1. Executive Summary

No open PRs and no CI failures to unblock. Issue #389 (BacktestService/StrategyValidation extraction) is ai-ready and awaiting assignment. Two critical security items from the May 19–20 research remain unverified: the Mini Shai-Hulud npm supply-chain worm (323 packages compromised May 19) and the node-ipc credential-stealer compromise require a lockfile audit before the next frontend build. PostgreSQL's Docker image version is unknown and may be below the patched 17.10 / 18.4 threshold (11 CVEs, including an SQL injection vector). The main human decision today: approve FEAT-389 for AI implementation and resolve the PostgreSQL security posture.

---

## 2. Overnight Activity

| Item | Status | Link | Notes |
|------|--------|------|-------|
| FEAT-389 PRD: extract BacktestService & StrategyValidation | ai-ready | [#389](https://github.com/pierrickmartino/blockbuilders.tech.bmad/issues/389) | Risk-medium; awaiting assignment |
| #334 PostgreSQL 18 evaluation | needs-human | [#334](https://github.com/pierrickmartino/blockbuilders.tech.bmad/issues/334) | Stale since 2026-05-17; now has CVE context |
| Frontend Docker build bugfix | ✅ implemented | — | Added `.dockerignore`; no PR opened |
| Mini Shai-Hulud npm worm (May 19) | ⚠️ unverified | — | `npm audit` not yet run |
| node-ipc supply-chain (May 14) | ⚠️ unverified | — | Transitive dep presence unknown |
| PostgreSQL Docker image version | ⚠️ unknown | — | May be below patched 17.10 |

---

## 3. PRs Ready for Your Decision

| PR | CI | Codex Review | Risk | Recommendation |
|----|----|-----------|----|----------------|
| — | — | — | — | No open PRs |

---

## 4. Blocked Tasks

| Issue | Blocker | Decision Needed |
|-------|---------|-----------------|
| #334 PostgreSQL 18 evaluation | needs-human, risk-high | Approve scope: full evaluation or split CVE security-check into separate urgent feature? |
| FEAT-389 BacktestService extraction | awaiting assignment | Approve for Codex Web (risk-medium, has approved spec) |

---

## 5. Questions for You

1. **Mini Shai-Hulud (P0):** Authorize Jules to run `npm audit` + check `package-lock.json` for May 19 compromised packages before the next frontend build?
2. **PostgreSQL patch (P0):** Authorize Jules to check the Docker image tag and bump to `postgres:17.10` (or `18.4`) if behind the May 14 security release?
3. **FEAT-389 assignment:** Assign to Codex Web now, or hold for a coordinated canvas/backend refactor sprint?
4. **#334 scope:** Should CVE-2026-6638 (`ALTER SUBSCRIPTION` SQL injection) be split out as an urgent security issue separate from the full PostgreSQL 18 performance evaluation?
5. **Alembic/SQLModel batch update:** Appetite for a FEAT-119 batch backend dep update (Alembic 1.18.4 + SQLModel 0.0.38) similar to FEAT-117?

---

## 6. Risks

| Risk | Severity | Action |
|------|----------|--------|
| Mini Shai-Hulud npm worm (May 19, 323 packages) | CRITICAL | Run `npm audit` before next build |
| node-ipc compromised versions (9.1.6, 9.2.3, 12.0.1) | CRITICAL | `npm ls node-ipc --depth=10` |
| PostgreSQL image may be below 17.10 (11 CVEs incl. SQL injection) | HIGH | Check `docker-compose.yml` image tag |
| Starlette pin not re-verified after FEAT-118 FastAPI upgrade | HIGH | `pip show starlette` — expect ≥0.49.1 |
| PyJWT transitive dep not confirmed absent post-FEAT-113 | MEDIUM | `pip show pyjwt` — should return not found |

---

## 7. Agent Usage This Cycle

| Agent | Tasks | Notes |
|-------|-------|-------|
| Codex (GPT-5) | Frontend Docker `.dockerignore` bugfix | No PR opened; direct commit to main |
| Codex (GPT-5) | FEAT-117, FEAT-118, FEAT-116, FEAT-112 (prior cycle) | All verified ✅ |
| Jules | — | No overnight assignments |
| Claude Code | Nightly research 2026-05-20, morning report 2026-05-21 | — |

---

## 8. Recommended Plan for Today

1. **[Jules, P0]** Run `npm audit` + `npm ls node-ipc` in `frontend/`; report results; bump if any hit.
2. **[Jules, P0]** Check PostgreSQL image tag in `docker-compose.yml`; bump to `postgres:17.10` if below patched version.
3. **[Jules, P1]** Verify `pip show starlette` ≥ 0.49.1 and `pip show pyjwt` returns not found.
4. **[Human]** Decide on FEAT-389 assignment (Codex Web, risk-medium) and #334 scope split.
5. **[Codex Web, if approved]** Begin FEAT-389 BacktestService extraction per the ai-ready spec.
