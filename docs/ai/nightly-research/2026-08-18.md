# Nightly Research — 2026-08-18

**Prepared by:** Claude Code (nightly-research)
**Stack snapshot (verified from repo, not assumed):** Next.js 16.2.6 · React/React DOM 19.2.6 · TypeScript 5.9.3 · Tailwind 3.4.19 (v3, devDependency, pinned per FEAT-117) · @xyflow/react ^12.10.0 · FastAPI 0.136.1 · Starlette >=0.49.1 (unbounded floor) · uvicorn[standard] 0.47.0 · SQLModel 0.0.38 · Alembic 1.18.4 · psycopg2-binary>=2.9.10 · redis (client) 7.1.1 · RQ 2.7.0 · rq-scheduler 0.14.0 · Redis server (Docker) `redis:7.4.9-alpine` (exact pin) · PostgreSQL (Docker) `postgres:16-alpine` (floating tag)

**Continuity note:** the last research cycle is **2026-08-17** (`docs/ai/nightly-research/2026-08-17.md`, merged as #865) — main is current, no gap in the doc trail. This report treats 2026-08-17 as the working baseline and scopes new research to **2026-08-17 → 2026-08-18** (1 day). All dependency/version claims below were freshly re-verified live today (npm registry, PyPI JSON API, Docker Hub v2 API, GitHub Advisory Database) rather than carried forward, per source notes inline.

**New this cycle: the open-PR backlog is far larger than previously scoped.** The prior report's action item was "close PR #860 (research 2026-08-12) as superseded." A full inventory today found **46 open PRs, all `docs(ai)` reports/research, 100% of open PRs in the repo**, ranging from **5 to 75 days old** (oldest: #512, "report 2026-06-03"). Cross-referencing with `docs/ai/nightly-research/` and `docs/ai/morning-reports/` on `main` shows most of these are superseded — a later cycle's report/research already merged past them out of order (the same pattern previously diagnosed only for #860). This is a repo-hygiene finding, not a security or code issue — flagged for human triage, not auto-closed, since this report does not modify source or merge/close anything.

---

## Executive Summary

**No previously-flagged fix has been applied yet, for a 2nd consecutive cycle since 08-17 (and a 5th–14th cycle depending on the item).** Verified live today: `next` still pinned `16.2.6` (vulnerable), `redis:7.4.9-alpine` still the Docker tag (vulnerable, CVE-2026-66373), `postgres:16-alpine` floating tag still unconfirmed as re-pulled since the 2026-08-13 28-CVE batch, `backend/requirements.txt` still pins `starlette>=0.49.1` with no upper bound. All four P0/P1/P2 items are unchanged in the repo.

**No new package releases or CVEs landed in the 1-day window.** Fresh live checks (npm registry `/latest`, PyPI JSON API, Docker Hub v2 API, GitHub Advisory Database, postgresql.org/support/security, github.com/redis/redis/security/advisories) confirm every tracked package — `next`, `react`, `react-dom`, `typescript`, `tailwindcss`, `@xyflow/react`, `fastapi`, `starlette`, `uvicorn`, `sqlmodel`, `alembic`, `psycopg2-binary`, `redis` (client), `rq`, `rq-scheduler` — is at the exact same "latest" version as the 08-17 report, and no new advisory was published for any of them. This is a valid "no new activity" finding for a 1-day window, not a gap in research.

**One outstanding follow-up from the prior cycle is now resolved: the `@xyflow/react 12.11.3` changelog has been reviewed.** It contains a touch/mobile-relevant fix — "fix selection box appearing when dragging the pane via touch; prefer touch panning over drag selection when `selectionOnDrag` is combined with mouse-button-specific `panOnDrag`" — directly relevant to this repo's mobile canvas work (product.md §4.11). Since the repo pins `^12.10.0` (caret range), a routine `npm install`/lockfile refresh would already pick this up; it should be scheduled and smoke-tested against the bottom action bar, minimap, and compact-node-mode interactions before being treated as "free."

**Supply-chain picture is unchanged and, on the specific-package check, reassuring.** The npm "Flooding Dropper"/WEL1DROPPER campaign (846–1,033 packages) is confirmed still tapering, not growing. The npm "ChainDrop" worm (Shai-Hulud variant, ~420–452 unique packages via `keyv`/`flat-cache`/`cache-manager`) was independently confirmed this cycle via a full fetch of The Register's 08-15 article (previously unfetchable) plus Microsoft and Elastic writeups — it is real and active, but a direct, explicit check today against this repo's full dependency list (frontend + backend + Python AI-SDK packages) found **no match** for any of `next`, `react`, `react-dom`, `typescript`, `tailwindcss`, `@xyflow/react`, `fastapi`, `starlette`, `uvicorn`, `sqlmodel`, `alembic`, `psycopg2-binary`, `redis`, `rq`, `rq-scheduler`, `anthropic`, `openai`, `instructor`, `boto3`, `httpx`, `resend`, `stripe`, `posthog`, `structlog`, `bcrypt`, `joserfc`, `pydantic-settings`. The PyPI "Mini Shai-Hulud" wave (170+ packages, active since late April 2026) shows the same result — no match. No brand-new campaign was disclosed in the 08-17→08-18 window.

**One new, genuinely relevant reference surfaced for issue #618 (NL-wedge onboarding):** an academic benchmark, *Chat2Workflow* (arXiv 2604.19667), found that even current state-of-the-art LLMs "struggle to generate correct, stable, executable workflows... especially under complex or changing requirements" when generating visual node-graphs from natural language, with an agentic self-repair loop only recovering ~5.34 percentage points of failures. This is a cautionary, not encouraging, data point for the wizard-vs-NL-box A/B design — it reinforces (does not newly establish) the existing repair-loop design already implemented for the NL wedge (issue #625's `draft_and_repair`, per `tasks/lessons.md`), and argues against loosening the existing compile→validate→repair→decline pipeline.

**One new UX reference relevant to future canvas work:** n8n's Canvas UI (v1.30, with a further node-repositioning fix on 2026-03-16) added collapsible, color-coded, labeled node groups for large workflows — a pattern worth tracking for this repo's own auto-layout/tidy-connections work (product.md §4.16) if strategies grow large enough to need visual grouping, though nothing here suggests it's needed yet.

---

## Findings for Active Issues

### Issue #618 — NL-wedge onboarding A/B (`ready-for-agent`, **blocked**)
**Blocker:** HITL #620 — configure `onboarding_ab` PostHog experiment flag, metrics, and guardrails.
**Status: no change to the issue itself.** New research this cycle: the *Chat2Workflow* benchmark (arXiv 2604.19667, ~April 2026, newly surfaced — not previously in this report's corpus) found SOTA LLMs generate incorrect/unstable node-graphs from NL a meaningful fraction of the time, especially under complex requirements, and that agentic repair loops only partially close the gap. This is corroborating evidence (not new-to-the-repo behavior) that the existing compile→validate→repair→decline design (issues #625/#626, per `tasks/lessons.md` 2026-06-12 entries) is the right shape, and argues against simplifying it. No new competitor NL-to-strategy-builder launch was found dated after 08-17; Coinrule's multi-broker MCP expansion (logged prior cycles) traces to a Feb 2026 announcement, not new activity. Opened 2026-06-11, now **68 days idle**.

### Issue #570 — WJL retention A/B (`ready-for-agent`, **blocked**)
**Blocker:** HITL #573 — configure `wjl_retention_ab` PostHog experiment, metrics, and runbook.
**Status: no change.** No new dated retention/first-run-summary-card research found this cycle; general fintech-activation commentary found (SaaS activation ~37.5% median, fintech trailing ~5%, 76% of converters activate within 7 days) is evergreen aggregator content, not a new study — not treated as a finding. Opened 2026-06-09, now **70 days idle**.

### Issue #555 — Activation drop-off cohorts (`ready-for-agent`, **blocked**)
**Blocker:** HITL #561 — build the diagnostic funnel, `time_to_activation`, and cohort cutover in PostHog.
**Status: no change.** No new material found this cycle. Opened 2026-06-08, now **71 days idle** — still the longest-idle of the three HITL-blocked pairs.

**Observation across all three:** unchanged for a 16th+ consecutive cycle. Standing recommendation unchanged: needs a direct human decision on scheduling the PostHog config work, or a formal relabel of #618/#570/#555 to reflect deprioritization.

### Issue #334 — PostgreSQL 18 evaluation (`needs-plan`, `needs-human`, `risk-high`)
**Status: no change to the issue**, but PG19's timeline advanced concretely: **Beta 3 shipped 2026-08-13** (28 security fixes, 110+ bug fixes) is confirmed on the same track logged previously, now with GA tracking to roughly September/October 2026. No new AIO/index-scan-specific behavior was called out beyond PG18's already-known scope (sequential scans, bitmap heap scans, vacuum — not the plain/aggregating index scans that dominate this repo's candle-range OHLCV queries). One benchmark not previously surfaced in this report (published 2026-06-29, pre-dates the 08-17 baseline but is new-to-corpus): a 20M-row PG17.9-vs-18 benchmark found skip-scan improved a timestamp-filter query 847ms→585ms, but a large aggregation query got **slower** under AIO defaults (4.3s→7.1s), only partially recovered with `io_method=io_uring` — direct corroboration that AIO gains are workload-specific and don't uniformly help time-series-style queries. Still no dedicated OHLCV/candle-range benchmark exists. `docs/features/FEAT-114-evaluate-postgresql-18-backtest-workloads.md` (`Status: Draft`) unchanged. GitHub issue now **93 days idle**, zero comments.

### Issues #509, #510, #511 — Backend refactors (`needs-spec`, `risk-medium`)
No change in issue state (opened 2026-06-03, now **76 days idle**). See Implementation References below.

---

## Security Risks

### P0, unchanged (3rd consecutive cycle) — PostgreSQL: 28-CVE batch (16.15, released 2026-08-13) still unapplied
Re-verified live via Docker Hub v2 API: `postgres:16-alpine` last pushed **2026-08-16T07:07:50Z**, unchanged since the 08-17 check — no new push today, and still no confirmation in the repo that the `db` service has been recreated against the current resolution of the floating tag. No new PostgreSQL security release since 08-13 (postgresql.org/support/security confirmed, most recent entry still Aug 13, 2026). Same CVE set as prior cycles (~28 CVEs, most CVSS 8.8: `pg_dump` heap overflow, `EXTRACT()`-based SQL injection, cursor `CLOSE`/`DECLARE` type confusion, `to_char`/`plperl` buffer overruns; plus a CVSS 5.3 SCRAM timing oracle and a CVSS 4.3 `ascii()` OOB read).
**Action (unchanged):** pull `postgres:16-alpine` (resolves to 16.15) and recreate the `db` service in dev + prod; confirm via `SELECT version();` post-deploy.

### P1, unchanged (9th consecutive cycle) — Redis: `redis:7.4.9-alpine` vulnerable, fix is `7.4.10-alpine`, CVE-2026-66373 (CVSS 7.5 High)
Re-confirmed via Docker Hub v2 API today: current pin `redis:7.4.9-alpine` last pushed 2026-05-09 (still the resolvable tag); upgrade target `redis:7.4.10-alpine` exists, last pushed 2026-07-26 — available now, no blocker. No new Redis security advisory since 08-17 (github.com/redis/redis/security/advisories most recent entry still dated 2026-05-05). CVE-2026-66373 mechanism re-confirmed: a Streams `RESTORE` payload can make two consumers reference the same NACK entry; deleting both via `XGROUP DELCONSUMER` double-frees, corrupting heap metadata (authenticated RCE risk for clients with `RESTORE` access). It is an incomplete-fix follow-up to the earlier CVE-2026-25243. Fixed in 6.2.23, 7.2.15, **7.4.10**, 8.2.8, 8.4.5, 8.6.5.
**Action (unchanged):** bump `redis:7.4.9-alpine` → `redis:7.4.10-alpine` (exact pin) in `docker-compose.yml` + `docker-compose.prod.yml`. One-line, no-code-impact fix.

### P1, unchanged (14th consecutive cycle) — Next.js: CVEs fixed in 16.2.11+/16.3.0+; repo still on vulnerable 16.2.6
No new Next.js release since `16.3.1` (2026-08-13). This cycle independently re-confirmed two specific CVE IDs directly via the GitHub Advisory Database (not just carried forward): **CVE-2026-64649** (GHSA-89xv-2m56-2m9x, High, SSRF in Server Actions on custom servers, 2026-07-22) and **CVE-2026-64648** (GHSA-68g3-v927-f742, Moderate, cache confusion of response bodies, 2026-07-22). This partially, not fully, corroborates the prior report's flagged-as-unverified `CVE-2026-64641`–`64649` range claim — two of the nine IDs in that range are now independently confirmed real and current; the other seven were not individually re-checked this cycle and remain unverified as a range.
**Action (unchanged):** bump `"next": "16.2.6"` to `16.3.1`.

### P2, unchanged (6th tracked cycle) — Starlette: floor-pin gap persists
Latest release remains `1.6.0` (2026-08-08); no new release or advisory since. CVE-2026-48710 (GHSA-86qp-5c8j-p5mr, CVSS 6.5, Host-header → `request.url.path` auth-bypass) re-confirmed as affecting 0.8.3–1.0.0, fixed in 1.0.1. Since `starlette>=0.49.1` has no upper bound, the actually-installed/locked version should be confirmed ≥1.0.1 at minimum, ideally ≥1.3.1 to close the other 4 previously-cited CVEs (2 HIGH: CVE-2026-48818, CVE-2026-54283; 1 MODERATE: CVE-2026-48817; 1 LOW: CVE-2026-54282).
**Action (unchanged):** pin `starlette>=1.3.1` (closes all 5) or `>=1.6.0` (tracks current) in `backend/requirements.txt`.

### UNCHANGED — supply-chain campaigns, now with a specific dependency-list cross-check
- **npm "Flooding Dropper"/WEL1DROPPER:** 846–1,033 packages depending on source (Sonatype: 846; OpenSourceMalware: 1,033); confirmed still **tapering** ("velocity has definitely slowed down since last week" — OpenSourceMalware co-founder). Typosquat/AI-slop package names fingerprint the OS via `require()` and pull a platform-specific RAT/infostealer from Cloudflare Workers or a `wel1.ru` DNS-TXT fallback.
- **npm "ChainDrop" (keyv/flat-cache/cache-manager worm, a Shai-Hulud variant):** fully verified this cycle (The Register's 08-15 article was fetched successfully, unlike the prior cycle's failed attempt) — first identified 2026-08-04, ~420–452 unique packages (1,300–2,251 republished versions), 2B+ combined monthly downloads for the seed packages, plants Claude Code/VS Code hooks for persistence, harvests npm/GitHub/cloud credentials, designed to evade standard scanning. **Explicit check against this repo's full dependency list (25 packages spanning frontend, backend, and AI SDKs) found no match.**
- **PyPI "Mini Shai-Hulud":** active since late April 2026 (TeamPCP), 170+ packages / 518M+ downloads across TanStack, UiPath, Mistral AI, OpenSearch JS client, Squawk, Guardrails AI; confirmed technique is OIDC-token extraction from GitHub Actions runner memory + Sigstore signing to forge SLSA Build Level 3 provenance. **No match against this repo's dependency list.** No material change since 08-17.
- **No brand-new campaign** disclosed 2026-08-17→08-18 was found for either npm or PyPI.

### MONITOR — routine, non-security movement only (all re-verified live today via PyPI JSON API / npm registry, not carried forward)
`fastapi` 0.141.1 (unchanged since 07-29), `starlette` 1.6.0 (unchanged since 08-08), `uvicorn` 0.52.3 (unchanged since 08-13), `sqlmodel` 0.0.39 (unchanged since 06-25), `alembic` 1.19.1 (unchanged since 08-08), `psycopg2-binary` 2.9.12 (unchanged since 04-20), `redis` Python client 8.1.0 (unchanged since 07-30, still a 7.x→8.x major gap vs. the repo's pinned `7.1.1`), `rq` 2.11.0 (unchanged since 08-17, no newer release today), `next` 16.3.1, `react`/`react-dom` 19.2.8, `typescript` 7.0.2, `tailwindcss` (v4 line) 4.3.3, `@xyflow/react` 12.11.3 / `@xyflow/system` 0.0.80 (unchanged since 08-12) — all confirmed identical to the 08-17 report, zero movement in the 1-day window.

### UNCHANGED — `rq-scheduler` maintenance-risk flag
Still pinned at `0.14.0`, still last released 2024-10-29 (~22 months stale). No CVE, bus-factor observation only.

---

## Implementation References

### #509 — Block Catalogue dispatch-table refactor
No change. No new cross-language behavioral validator generator surfaced this cycle. Standing recommendation unchanged: JSON Schema as canonical block-definition format, feeding both the Python interpreter/validator and a generated frontend schema (see #510).

### #510 — Pydantic → Zod codegen seam
No change of substance. `hey-api`'s tracked package (`@hey-api/openapi-ts`) latest release remains `0.80.0`; its notable Zod-4/Zod-Mini support milestone traces to 2026-06-08, not new this cycle. `openapi-zod-client` remains stale (~1 year since last npm publish), reinforcing `hey-api` as the preferred pipeline. Recommendation unchanged: target the FastAPI-OpenAPI → `hey-api`/`openapi-zod-client` pipeline over a direct Pydantic→Zod converter.

### #511 — Atomic `submit()` / commit-then-enqueue seam
No change to the standing "not viable" verdict on `hyzyla/outbox-streaming` (dormant 3+ years, explicit "not recommended for production use" README warning, only 10 total commits). Two new-to-corpus supplementary references surfaced this cycle, neither dated post-08-17 so treated as background reading rather than a new finding: `gmhafiz/transactional-outbox-pattern` (Go, not directly portable, but a clean worked example of atomic DB-write + queue-publish with row-locking) and `python-cqrs` (CQRS + Transactional Outbox + Saga, FastAPI/FastStream-native — closer to this stack, worth a look if/when #511 moves to spec). Standing default (hand-roll an outbox table + relay worker on FastAPI/SQLModel/Redis-RQ) unchanged.

### #334 — PostgreSQL 18/19 evaluation
See Findings for Active Issues above — PG19 Beta 3 timeline confirmed, and a new-to-corpus 20M-row PG17.9-vs-18 benchmark (published 2026-06-29) reinforces that AIO gains are workload-specific, sometimes *regressing* aggregation-heavy queries, and don't demonstrate a win for this repo's dominant access pattern (indexed candle-range scans).

### #618 — NL-wedge onboarding
*Chat2Workflow* (arXiv 2604.19667) is the one new, citable reference this cycle — see Executive Summary and Findings above for the specific takeaway (reinforces the existing repair-loop design, argues against simplifying it). n8n's Canvas UI node-grouping feature (v1.30, Feb 2026, refined 2026-03-16) is a new-to-corpus UX reference more relevant to future large-strategy canvas organization (#4.16 auto-layout) than to #618 specifically, logged here for visibility. No material Coinrule/3Commas/TradingView UX change found this cycle beyond routine template/broker additions.

### @xyflow/react touch-canvas fix (new implementation reference, relevant to product.md §4.11 mobile canvas work)
`12.11.3`'s "prefer touch panning over drag selection" fix directly addresses a touch-interaction ambiguity between panning and selection — worth a scheduled lockfile refresh + smoke test against the bottom action bar, minimap, and gesture-based connection flow, rather than an unplanned incidental pickup. A related open upstream issue (`xyflow/xyflow#5341`) shows pan-on-touch conflicts are still being iterated on generally, so treat this as one fix in an ongoing area, not a final resolution.

---

## Questions for Human Review

1. **The docs(ai) PR backlog is far larger than previously scoped:** 46 open PRs, 100% of the repo's open PRs, ranging 5–75 days old, almost all superseded by later out-of-order merges (the same pattern previously diagnosed only for #860). Should these be triaged/batch-closed as superseded, or is there a reason they're being kept open (e.g., an audit trail)? This report does not close or merge anything — flagging for a human or a dedicated cleanup pass.
2. **Still-unapplied P0/P1/P2 fixes (Postgres 16.15 pull, Redis 7.4.10 bump, Next.js 16.3.1 bump, Starlette floor pin):** now flagged across 3–14 consecutive cycles with no repo change. Is there a blocker to approving these, or should they be batched into one dependency-bump PR and scheduled explicitly?
3. **Next.js CVE range re-check:** two of the nine previously-cited CVE IDs (`CVE-2026-64648`, `CVE-2026-64649`) are now independently confirmed via the GitHub Advisory Database; the remaining seven in the `64641`–`64649` range are still unverified individually. Worth a dedicated pass before citing the full range externally?
4. **HITL gate timeline (#620, #573, #561):** now 68–71 days idle, zero comments, unchanged across 16+ consecutive cycles. Schedule the PostHog config work, or formally relabel #618/#570/#555 as deprioritized?
5. **`@xyflow/react` lockfile refresh:** the 12.11.3 touch-panning fix is available now via the existing `^12.10.0` caret range — should this be picked up in a routine lockfile-refresh PR with a mobile-canvas smoke test, given it's a genuine (if minor) UX bug fix rather than a security item?
6. **#511 outbox candidate:** given `hyzyla/outbox-streaming` remains confirmed not-viable and `python-cqrs` is a closer-fit reference than previously surfaced, should the standing hand-roll recommendation move from implementation-reference to an actual spec (`needs-spec` → drafted)?
7. **Starlette actual resolved version:** can someone confirm what version is actually installed in CI/prod today (likely via FastAPI's own transitive pin)? If it's already ≥1.0.1, the effective exposure may be narrower than the floor-pin gap suggests.
8. **`redis` Python client major-version gap** (pinned `7.1.1`, upstream at `8.1.0`, unchanged for several cycles now): worth a deliberate compatibility-check pass, or intentionally held back?
9. **`tasks/ai-usage-log.csv`:** confirmed still stale — last row 2026-05-29, now **81 days**, 11 data rows total. Deprecate formally or reactivate?
10. **Issue #334 progression:** `FEAT-114` draft spec unreferenced by the issue thread for 10+ consecutive cycles, now 93 days idle. Should `/plan-feature FEAT-114` be scheduled, using the current 16.15 baseline and PG19 Beta 3 timeline as context, with an internal pgbench-style run against representative OHLCV queries as the plan's first deliverable?

---

## Next Actions

| Priority | Action | Owner | Blocking |
|---|---|---|---|
| P0 | Pull `postgres:16-alpine` (resolves to 16.15) and recreate the `db` service in dev + prod — 28 CVEs fixed, several CVSS 8.8, still unapplied after 3 consecutive cycles | Human (approve) + agent (implement) | None — image already published |
| P1 | Bump `redis:7.4.9-alpine` → `redis:7.4.10-alpine` in both compose files — CVE-2026-66373 (CVSS 7.5) | Human (approve) + agent (implement) | None — fix exists |
| P1 | Bump `next` off `16.2.6` to `16.3.1` — 14th cycle flagging this | Human (approve) + agent (implement) | None — patch exists |
| P1 | Triage the 46-PR `docs(ai)` backlog (5–75 days old, all superseded or near-superseded) — batch-close superseded ones rather than merge stale reports out of order | Human | Docs-history cleanliness |
| P2 | Pin `starlette>=1.3.1` (or confirm effective installed version first) in `backend/requirements.txt` | Human (approve) + agent (implement) | 5 known CVEs incl. 2 HIGH |
| P2 | Confirm actually-resolved Starlette version in CI/prod lockfiles before treating the floor-pin gap as fully open | Human/Claude | Accurate risk sizing |
| P2 | Get a direct status read on PostHog HITL gates #620/#573/#561 — 68–71 days idle, 16+ cycles unchanged | Human | #618, #570, #555 |
| P2 | Advance `FEAT-114` (PostgreSQL 18/19 evaluation draft) toward `/plan-feature`, using the current 16.15 + PG19-Beta-3 baseline; first deliverable should be an internal benchmark against representative OHLCV/candle-range queries | Claude (plan only) | PostgreSQL 18/19 eval |
| P3 | Schedule an `@xyflow/react` lockfile refresh (picks up 12.11.3's touch-panning fix) with a mobile-canvas smoke test against the bottom action bar, minimap, and gesture connections | Codex Web / agent (implement) | Next implementation wave |
| P3 | Spec issues #509, #510, #511 as previously recommended (JSON-Schema-as-canonical; hey-api pipeline; hand-rolled outbox, `python-cqrs` as a closer reference) — no new blockers found | Codex Web | Next implementation wave |
| P3 | Evaluate `redis` Python client major-version gap (7.1.1 pinned vs 8.1.0 upstream) | Human/Codex | None — informational |
| P3 | Reactivate or formally deprecate `tasks/ai-usage-log.csv` — stale since 2026-05-29, now 81 days | Human | AI-usage visibility |
| P4 (no urgency) | Batch routine (non-security) version bumps: fastapi, sqlmodel, alembic, psycopg2-binary | Human/Codex | None — informational |
| P4 (no urgency) | Verify the remaining seven Next.js CVE IDs in the `CVE-2026-64641`–`64649` range not individually re-checked this cycle | Human/Claude | Citation accuracy |

---

*Sources:*
- npm registry `/latest` live checks for next, react, react-dom, typescript, tailwindcss, @xyflow/react (queried live, 2026-08-18): https://registry.npmjs.org/next/latest, /react/latest, /react-dom/latest, /typescript/latest, /tailwindcss/latest, /@xyflow%2freact/latest
- GitHub Advisory Database: https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm+next · GHSA-89xv-2m56-2m9x (CVE-2026-64649) · GHSA-68g3-v927-f742 (CVE-2026-64648)
- [xyflow/xyflow releases](https://github.com/xyflow/xyflow/releases) (12.11.3, 2026-08-12) · [xyflow/xyflow#5341](https://github.com/xyflow/xyflow/issues/5341)
- PyPI JSON API live checks for fastapi, starlette, uvicorn, sqlmodel, alembic, psycopg2-binary, redis, rq, rq-scheduler (queried live, 2026-08-18)
- Docker Hub v2 API live checks for `postgres:16-alpine`, `redis:7.4.9-alpine`, `redis:7.4.10-alpine` (queried live, 2026-08-18)
- [PostgreSQL security page](https://www.postgresql.org/support/security/) · [PostgreSQL 18.6/17.11/16.15/15.19/14.24 + 19 Beta 3 announcement](https://www.postgresql.org/about/news/postgresql-186-1711-1615-1519-1424-and-19-beta-3-released-3365/)
- [Redis security advisories](https://github.com/redis/redis/security/advisories) · [Tenable CVE-2026-66373 writeup](https://www.tenable.com/plugins/nessus/331358) · [Canadian Cyber Centre AV26-748](https://www.cyber.gc.ca/en/alerts-advisories/redis-security-advisory-av26-748) · [GHSA-86qp-5c8j-p5mr / CVE-2026-48710](https://github.com/advisories/GHSA-86qp-5c8j-p5mr)
- [Sonatype: Flooding Dropper](https://www.sonatype.com/blog/flooding-dropper-hits-npm-with-850-malicious-packages) · [The Hacker News: Flooding Dropper](https://thehackernews.com/2026/08/nearly-800-malicious-npm-packages.html)
- [The Register: ChainDrop](https://www.theregister.com/security/2026/08/15/chaindrop-worm-crawls-into-npm-supply-chain-evades-standard-defenses/5287958) · [Microsoft Security Blog: ChainDrop](https://www.microsoft.com/en-us/security/blog/2026/08/04/chaindrop-supply-chain-compromise-anatomy-self-propagating-worm/) · [Elastic Security Labs: ChainDrop](https://www.elastic.co/security-labs/shai-hulud-chaindrop-npm-supply-chain)
- [The Hacker News: Mini Shai-Hulud](https://thehackernews.com/2026/05/mini-shai-hulud-worm-compromises.html) · [OpenSSF: Mini Shai-Hulud / SLSA](https://openssf.org/blog/2026/06/10/mini-shai-hulud-where-slsas-boundaries-fall/) · [CyberScoop](https://cyberscoop.com/supply-chain-attack-malware-mini-shai-hulud-teampcp/)
- [Chat2Workflow, arXiv 2604.19667](https://arxiv.org/abs/2604.19667) · [StartupHub.ai coverage](https://www.startuphub.ai/ai-news/ai-research/2026/automating-visual-workflows-with-llms)
- [n8n changelog/release notes](https://docs.n8n.io/changelog/release-notes) · [n8n 2026 feature roundup](https://www.softomatesolutions.com/blog/n8n-updates-2026-whats-new/) · [n8n sticky notes 2026](https://ryanandmattdatascience.com/n8n-sticky-notes/) · [Zapier Feb 2026 product updates](https://zapier.com/blog/february-2026-product-updates/)
- [hey-api/hey-api releases](https://github.com/hey-api/hey-api/releases) · [hyzyla/outbox-streaming](https://github.com/hyzyla/outbox-streaming) · [gmhafiz/transactional-outbox-pattern](https://github.com/gmhafiz/transactional-outbox-pattern)
- [kkm-mako PG17.9-vs-18 benchmark](https://kkm-mako.com/en/blog/articles/postgresql-18-upgrade-benchmark/)
- `gh pr list` / `gh pr view 860` — live open-PR inventory and age computation (queried live, 2026-08-18): 46 open PRs, all `docs(ai)`, 5–75 days old
- Direct inspection of `frontend/package.json`, `backend/requirements.txt`, `docker-compose.yml`, `docker-compose.prod.yml`, `docs/features/FEAT-114-evaluate-postgresql-18-backtest-workloads.md`, `tasks/ai-usage-log.csv`, `tasks/lessons.md`, `tasks/todo.md`, `AGENTS.md`, `docs/product/product.md` in this repo (2026-08-18)
- Prior-cycle report consulted for continuity: [docs/ai/nightly-research/2026-08-17.md](https://github.com/pierrickmartino/blockbuilders.tech.bmad/blob/main/docs/ai/nightly-research/2026-08-17.md) (merged baseline, PR #865)
