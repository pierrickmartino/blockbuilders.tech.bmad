# Nightly Research — 2026-06-17

**Project:** Blockbuilders (no-code crypto strategy lab)
**Stack snapshot (as installed today):** Next.js 16.2.6 · React 19.2.6 · TypeScript 5.9.3 · Tailwind CSS 3.4.19 · @xyflow/react ^12.10.0 | FastAPI 0.136.1 · SQLModel 0.0.38 · Alembic 1.18.4 · redis-py 7.1.1 · RQ 2.7.0 · rq-scheduler 0.14.0 · joserfc 1.6.5 · bcrypt 4.2.0 · anthropic>=0.69.0 / instructor 1.15.1 · starlette >=0.49.1 · postgres:16-alpine · redis:7.4.9-alpine

---

## Executive Summary

**Three new issues landed today (#726, #727, #728), all `ready-for-agent`**, implementing ACTIONS #18 (freeze profiles, badges & digests behind a flag per ADR-0023). They are the most actionable items in the queue: #728 is fully independent (remove the dead digest toggle from `/preferences`), and #727 covers the end-to-end profiles/badges gating. Both can be started immediately.

**A critical security advisory requires immediate investigation:** CVE-2026-48710 "BadHost" in Starlette is an authentication bypass via Host header validation failure described as affecting "400,000+ dependent projects." The project pins `starlette>=0.49.1` with no upper bound — the fix landed in Starlette 1.0.1, so the floor must be raised. This is the highest-priority security item in the queue.

On carried-forward items: **React 19.2.7 is still not bumped** (5th consecutive report; released 2026-06-01). **FastAPI 0.137.0** has now had 3 days to settle since its 2026-06-14 GA; the router-tree refactor was already confirmed safe in last report's grep. **TypeScript 6.0** (March 2026) and **Tailwind v4.3** continue to widen the version gap.

No movement on #618/#620 (NL-wedge A/B — PostHog experiment still unconfigured), #509/#510/#511 (architecture refactors, still `needs-spec`), #334/FEAT-114 (PG18 evaluation), or the activation/retention HITL backlog (#555, #561, #570, #573).

---

## 1. Findings for Active Issues

### 1.1 #726 / #727 / #728 — ACTIONS #18: Freeze profiles, badges & digests (ADR-0023)

**New today (all `ready-for-agent`, all opened 2026-06-17).**

- **#728 — Remove dead digest toggles from `/preferences`** (independent, no blockers): Delete the "Email Digest" / "Weekly Strategy Digest" card and its toggle handler from the preferences page. The `digest_email_enabled` columns on `users` and `strategies` stay dormant — no migration, no flag, no API schema change. Acceptance criteria are minimal: one frontend render test, lint and `tsc --noEmit` green. **Safest entry point in the queue.**
- **#727 — Freeze profiles & badges behind `social_features_enabled` (end-to-end)**: The full M1–M4 implementation. Add `social_features_enabled: bool = False` to `config.py`, gate `/profiles/*` to return `404` when the flag is off, remove the Public Profile card from `/profile`, remove the `show_badges` section, remove the `/u/[handle]` frontend route, and update tests.
- **#726** is the parent specification issue; ADR-0023 is the design record. Both #727 and #728 implement slices of #726 and can run in parallel.

The key constraint from ADR-0023: `compute_badges` in `services/badges.py` is **not modified** — it becomes unreachable simply because its only consumer (the gated profile response) is 404'd when the flag is off. No badge logic changes, no migration.

### 1.2 #618 / #620 — NL-wedge onboarding A/B (ADR-0014)

No change since 2026-06-14. The routing fork (#619, merged 2026-06-12, PR #622) has now been live for ~5 days with no PostHog experiment configured to receive it. **#620 remains `ready-for-human`** — the gap between code spine and live experiment is widening.

### 1.3 #555 / #561 / #570 / #573 — Activation/retention HITL backlog

No movement. Still `ready-for-agent`/`ready-for-human`/`enhancement`. Priority unchanged from prior reports.

### 1.4 #509 / #510 / #511 — Architecture refactors (`needs-spec`, `risk-medium`)

No movement. Still blocked on specs. The sequencing argument from 06-14 (spec before golden suite accumulates more re-capture cost) remains valid but unresolved.

### 1.5 #334 / FEAT-114 — PostgreSQL 18 evaluation (`needs-plan`, `needs-human`, `risk-high`)

No change. PostgreSQL 18.4 (released 2026-05-14) remains the evaluation target. Still blocked on AC-002 (production version confirmation). PG 19 is now projected for September 2026.

---

## 2. Security Risks

### New / escalated this cycle

| Item | Component | Severity | Status / Action |
|---|---|---|---|
| **CVE-2026-48710 "BadHost" — authentication bypass via Host header validation** | Starlette (FastAPI's underlying ASGI framework) | **CRITICAL** | Starlette misvalidates the `Host` header, allowing request routing to bypass authentication middleware in specific proxy configurations. Fix landed in **Starlette 1.0.1**. Project currently pins `starlette>=0.49.1` with no upper bound — minimum must be raised to `>=1.0.1`. FastAPI 0.137.0 (released 2026-06-14) bundles a patched Starlette; adopting 0.137.0 is the most straightforward remediation path. **Investigate before next deploy.** |
| **CVE-2026-48526 / CVE-2026-48525 / CVE-2026-32597 — PyJWT algorithm confusion, DoS, crit header bypass** | PyJWT (not installed; project uses `joserfc==1.6.5`) | N/A | These CVEs do not apply. `joserfc` is the JWT implementation. No action, but flag: if `joserfc 1.6.5` is behind its latest release, verify it addresses the same `crit` header and algorithm-confusion classes. Check PyPI for joserfc ≥1.7.x release notes. |
| **CVE-2026-23479 / CVE-2026-25243 / CVE-2026-23631 — Redis RCE bundle (UAF, RESTORE, Lua)** | Redis OSS/CE server | HIGH (post-auth) | **Project already patched** via `redis:7.4.9-alpine`. No action — confirmed safe from 06-14 report. Carry-forward note only. |

### Re-confirmed safe (no action)

| Item | Component | Notes |
|---|---|---|
| Next.js security release (13 advisories, May 2026) | Next.js | Project is on **16.2.6** — the patched version. CVE-2026-23870, SSRF/cache-poisoning/XSS bundle are all covered. Safe. |
| CVE-2025-55182 "React2Shell" (CVSS 10.0) | React | Project on **19.2.6**, well past the 19.2.1+ patch. Not exploitable. |
| Redis server CVE bundle | Redis server | Patched by `redis:7.4.9-alpine`. No new server release this cycle. |

### Carried-forward watch items

| Item | Component | Notes |
|---|---|---|
| React 19.2.7 (FormData/Server-Actions fix) | React | Released 2026-06-01. **5th consecutive report** — still on `19.2.6`. See Q2. |
| FastAPI 0.136.1 → 0.137.0 | FastAPI | 0.137.0 released 2026-06-14. Router-tree refactor confirmed safe (no `.routes` iteration in `backend/app/`). Now 3 days old. See Q3. |
| CVE-2026-41305 (PostCSS XSS via `</style>`) | PostCSS (bundled by Next.js) | Low practical risk (build-time only, no user-submitted CSS path). Tracking for the Next.js patch that ships PostCSS ≥8.5.10. Upstream issue `vercel/next.js#93234` still unresolved. |
| redis-py 7.1.1 vs 8.0.0 GA | redis-py | RESP3-by-default, type-hint breaking changes in 8.0.0 (2026-05-28). Planned migration item; not urgent. |
| Tailwind CSS 3.4.19 vs 4.3 | Frontend | Gap continues to widen. Planned major migration — not a routine bump. |
| `anthropic>=0.69.0` floor vs 0.109.1 latest | Backend | No change since 06-09. Drift concern unchanged. |
| `postgres:16-alpine` / `python:3.12-slim` floating tags | Docker Compose | Digest-freshness question unresolved from prior reports. |

---

## 3. Implementation References

| Topic | Pattern / Reference | Applicability |
|---|---|---|
| **Backtest narrative generation** (product.md §5.4, spec ready) | Template-based sentence generation is validated by 2026 industry research: no AI found in the space — all platforms use deterministic metric-to-sentence mapping (e.g. `"Strategy won ${winRate}% of ${tradeCount} trades over ${dateRange}"`). The template-based approach in `backend/app/backtest/narrative.py` is the correct pattern. | Confirms existing spec direction. No implementation change needed. |
| **#727 profiles gate — `404` guard pattern** | Mirrors the existing `strategy_drafter_enabled` / `scheduler_enabled` boolean-flag pattern in `backend/app/core/config.py`. Each guarded route returns `404` when the flag is off (no `403`/`410` leak). The guard on `/profiles/*` sits at the router level, not in each handler, per ADR-0023. | Implementation guide for #727 M1. |
| **Canvas auto-layout (product.md §4.16, spec ready)** | React Flow / XyFlow official example uses **dagre** for directed-graph layout. The `reactflow/examples/layout/auto-layout` reference covers Left→Right and Top→Bottom layout directions — the same two flows called out in §4.16. Available as `@dagrejs/dagre` (no new major dependency needed; already common in the XyFlow ecosystem). | Reference for future #4.16 implementation. |
| **Color-coded port type validation** | Flume (flume.dev) and XyFlow's `awesome-node-based-uis` list show port-type coloring as the primary defensive pattern against invalid node connections. Applicable to the canvas `BlockPalette` connection rules. Currently Blockbuilders validates server-side at `/validate`; port-type coloring would add a client-side first line of defense. | Optional UX enhancement; log as future canvas improvement. |
| **Behavioral nudges in trading UX (2026)** | "9 UX Patterns for Behavioral Design in Trading Apps 2026" identifies: confirmation delays after losses, conservative default risk values, portfolio-impact previews before execution. Most relevant to the Take Profit / Stop Loss block defaults and the backtest runner's fee/slippage defaults (already configurable). | Directional; no immediate implementation action. |
| **joserfc CVE surface check** | Before closing the PyJWT CVE flag: verify `joserfc` changelog for the same `crit` header validation class (CVE-2026-32597 analog). PyPI: `joserfc` latest is currently 1.6.5 — check release notes for any `crit` or algorithm-confusion mentions. | Security verification task for this week. |
| **#728 preferences cleanup** | Pattern: delete the component and its toggle handler from `/profile/page.tsx` (or the relevant preferences component), update or remove the snapshot test that currently renders it. The `digest_email_enabled` `User`/`Strategy` API fields and DB columns stay untouched (dormant). | Implementation guide for #728. |

---

## 4. Questions

1. **CVE-2026-48710 "BadHost" (Starlette):** Is the project's current `starlette>=0.49.1` floor below the patched version (1.0.1)? Adopting `fastapi==0.137.0` is the cleanest path to pull in the patched Starlette — confirmed safe per 06-14 grep. Should this be treated as an emergency bump or scheduled for the next deployment window?
2. **React 19.2.7 bump (5th ask):** Released 2026-06-01. `react`/`react-dom` `19.2.6` → `19.2.7` is a one-line `package.json` change with no API breaks. Is this being intentionally held, or has it fallen through the backlog?
3. **FastAPI 0.137.0 adoption:** Now 3 days since GA (2026-06-14). The router-tree refactor is confirmed safe (`backend/app/` has zero `.routes` iteration). Practical blocker to adopting 0.137.0 this cycle? (Extra urgency if it's the path to the BadHost Starlette fix — Q1.)
4. **joserfc CVE surface:** The PyJWT CVEs (algorithm confusion, `crit` header bypass) do not apply to `joserfc` directly, but the same vulnerability classes could theoretically exist. Has `joserfc 1.6.5` been checked against these patterns in its changelog?
5. **#728 vs #727 ordering:** Both are `ready-for-agent` today. #728 is self-contained (preferences UI only, no flag, no API change) and can merge faster. Confirm: #728 first, then #727 in parallel or series?
6. **#620 PostHog experiment (5th-ish ask):** The NL-wedge routing fork has been live unmeasured for ~5 days. Is there a target date for configuring the `onboarding_ab` experiment in PostHog?

---

## 5. Next Actions

### Immediate (security)
- [ ] **SEC-1 (NEW, CRITICAL):** Investigate CVE-2026-48710 "BadHost" (Starlette). Raise `starlette` floor to `>=1.0.1` in `requirements.txt`. Preferred path: bump `fastapi` to `==0.137.0` simultaneously (safe per 06-14 grep, bundles patched Starlette). Deploy before next public-facing release.
- [ ] **SEC-2:** Verify `joserfc==1.6.5` is not affected by `crit`-header or algorithm-confusion classes analogous to CVE-2026-32597 / CVE-2026-48526. Check joserfc release notes on PyPI.

### Immediate (agent-ready issues)
- [ ] **#728 (NEW, independent):** Remove the "Email Digest" / "Weekly Strategy Digest" card from `/preferences`. No migration, no flag, no API schema change. Safest issue in the queue — can start immediately.
- [ ] **#727 (NEW):** Freeze profiles & badges behind `social_features_enabled: bool = False`. Implement M1 (backend flag + `/profiles/*` 404 guard), M2 (remove Public Profile card from `/profile`), M3 (remove `/u/[handle]` frontend route), and update tests. Can run in parallel with #728.

### Carried-forward dependency bumps
- [ ] **DEP-1 (5th time):** Bump `react`/`react-dom` `19.2.6` → `19.2.7` (Q2).
- [ ] **DEP-3:** Decide FastAPI bump target — `0.136.1` → `0.137.0` (now 3 days settled, router-tree refactor safe, and the preferred BadHost remediation path per Q1/Q3).

### Watch / plan
- [ ] **#620 (HITL):** Configure the `onboarding_ab` PostHog experiment — code spine has been live unmeasured for ~5 days. (Q6)
- [ ] **redis-py major gap:** `7.1.1` → `8.0.0` — consider as a planned migration item.
- [ ] **CVE-2026-41305 (PostCSS via Next.js):** Low practical risk; track for the Next.js patch that bundles PostCSS ≥8.5.10.
- [ ] **#509 / #510 / #511:** Still `needs-spec`; sequencing vs. accumulated goldens still unresolved (Q from 06-14 carried forward).
- [ ] **#334 / FEAT-114:** No change — blocked on AC-002. PG 19 due September 2026.
- [ ] **Tailwind v3 → v4:** Gap at 3.4.19 vs 4.3. Planned major migration, not a routine bump.
- [ ] **TypeScript 5.9.3 → 6.0:** Released March 2026. 40–60% faster rebuild times, stage 3 decorators, import attributes. No breaking changes reported for typical app code. Candidate for a planned upgrade window.

---

*Generated by Claude Code Action (claude-sonnet-4-6) on 2026-06-17. Sources: GitHub issue tracker (issues #726–#728, #618, #620, #573, #570, #561, #555, #511–#509, #334), PyPI (fastapi 0.137.0, sqlmodel 0.0.38, alembic 1.18.4, redis-py 8.0.0, joserfc 1.6.5), npm (react 19.2.7, typescript 6.0, tailwindcss 4.3.1, @xyflow/react), security advisories (CVE-2026-48710 "BadHost" / badhost.org, CVE-2026-23870 / vercel Next.js May 2026 security release, CVE-2026-48526/48525/32597 PyJWT, Redis CVE bundle), ADR-0023 (docs/adr/0023-freeze-profiles-badges-digests-behind-a-flag.md), docs/product/product.md, lunefi.com "Best No-Code Strategy Builders 2026", openwebsolutions.in "Behavioral Design in Trading Apps 2026", reactflow.dev auto-layout/copy-paste examples, flume.dev.*
