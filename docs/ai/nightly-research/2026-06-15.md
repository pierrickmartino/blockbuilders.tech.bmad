# Nightly Research — 2026-06-15

**Project:** Blockbuilders (no-code crypto strategy lab)
**Stack snapshot (as installed today):** Next.js 16.2.6 · React 19.2.6 · TypeScript 5.9.3 · Tailwind CSS 3.4.19 · @xyflow/react ^12.10.0 | FastAPI 0.136.1 · Starlette ≥0.49.1 (resolves 1.3.1) · uvicorn 0.47.0 · SQLModel 0.0.38 · Alembic 1.18.4 · redis-py 7.1.1 · RQ 2.7.0 · rq-scheduler 0.14.0 · anthropic>=0.69.0 (resolves 0.109.1) / instructor 1.15.1 · postgres:16-alpine · redis:7.4.9-alpine

---

## Executive Summary

Today was the biggest shipping day in recent memory, and it closes out **both** of the long-running carried-forward asks from the golden-backtest thread. **#653 (Bollinger Breakout golden + look-ahead test) merged via PR #664** at 04:03 UTC, completing the **3/3 golden template set** from ADR-0018 (`test_golden_backtest_regression.py` now covers RSI Oversold Bounce, MA Crossover, and Bollinger Breakout, all reusing `run_golden_pipeline`/`mutate_tail`/`assert_prefix_unchanged`). **#654 (CI bootstrap) is now CLOSED** — the 4th-consecutive-report flag is resolved, and the existing `.github/workflows/ci.yml` is now running the complete golden suite green on every PR.

Separately, **ACTIONS #13 (Shareable Shared-backtest artifact, ADR-0019) shipped in full** across six merged PRs today: #678 (server-component refactor with `generateMetadata`/OG/Twitter tags), #679 (dynamic per-result OG image with an equity-curve sparkline via `ImageResponse`), #680 (narrative card on the landed share page, closing out issue #675), #681 (cost-honesty disclosure, closing issue #676 and ADR-0017 §6's deferred leak), and #669/#670 (the felt-dollar buy-and-hold delta on the narrative and What-you-learned cards — ACTIONS #12's resolved scope from #665/ADR-0010 amendment). The "Wordle result" social artifact described in the 2026-06-05 brainstorm is now live end-to-end. **ACTIONS #14 (llms.txt agent-legibility) also shipped today** via #683 (resolved-scope doc, no ADR) and #684 (`/llms.txt` Next.js route handler).

On dependencies, **FastAPI 0.137.1 was released today** (2026-06-15T11:28 UTC), literally hours after yesterday's 0.137.0 — superseding yesterday's "0.136.3 vs 0.137.0" question entirely. `anthropic` (0.109.1 → **0.109.2**) and `instructor` (1.15.1 → **1.15.3**) both also released same-day patches. **React 19.2.7 is confirmed as the latest** (still 19.2.6 in the repo — **5th consecutive report** recommending this bump, no newer 19.x exists). A quieter but bigger item resurfaces: **TypeScript 6.0** (released March 2026, last flagged 2026-06-02, then silent for two weeks) — project remains on 5.9.3 with no migration plan drafted, and TS 6.0.3 is now current. New this cycle: **`stripe` is now four major versions behind** (11.2.0 vs latest 15.2.1) with real breaking changes (StripeObject no longer dict-like, `Decimal`-typed money fields, new pinned API version) — billing-adjacent, so any move here is `needs-human` per the risk policy. `bcrypt` (4.2.0 → 5.0.0) and `redis-py` (7.1.1 → 8.0.0, unchanged) round out the major-version watch list. The PostCSS CVE-2026-41305 bundled by Next.js remains unresolved upstream.

No movement on **#618/#620** (NL-wedge onboarding A/B — #620's PostHog experiment config now ~84 hours unmeasured since #619 went live), **#509/#510/#511** (architecture refactors, still `needs-spec` — now sharper given the golden suite is locked at 3/3), **#334/FEAT-114** (PG18 evaluation, still blocked on AC-002), or **#555/#561/#570/#573** (activation/retention HITL backlog).

---

## 1. Findings for Active Issues

### 1.1 #649 (parent) / #651–#654 — Golden-backtest regression suite (ADR-0018): **COMPLETE**

- **#653 (Bollinger Breakout) — merged** via PR #664 (`88c83d7` carried into `main`, "test: add Bollinger Breakout golden regression + look-ahead test (#653)"), 249 lines, reusing `run_golden_pipeline`/`mutate_tail`/`assert_prefix_unchanged` exactly as #651/#652 did. This resolves last cycle's Q1 (the orphaned-branch question) — a PR was opened and merged the same morning. `test_golden_backtest_regression.py` now covers all **3 of 3** seed templates (RSI Oversold Bounce, MA Crossover, Bollinger Breakout) at roughly **1,050 lines**.
- **#654 (CI bootstrap) — CLOSED.** As predicted for four consecutive reports, `.github/workflows/ci.yml` already satisfied the acceptance criteria (Python 3.12 setup, `pip install -r backend/requirements.txt`, `pytest tests/ -v` from `backend/`, plus a frontend job). With all three goldens now merged, this workflow is the CI gate for the full golden suite.
- **Implication for #509/#510 (see 1.5):** the golden suite's "re-capture cost" is now a known, fixed quantity (3 templates) rather than a moving target — this materially changes the sequencing calculus raised in the 06-13/06-14 reports.

### 1.2 ACTIONS #13 / ADR-0019 — Shared backtest as a social-card artifact: **SHIPPED**

All three gaps ADR-0019 identified (no OG metadata, no narrative, the ADR-0017 §6 cost-honesty leak) closed today:

| PR | What shipped |
|---|---|
| #671 | ADR-0019 design doc (resolved scope, merged 09:42 UTC) |
| #678 | Server-rendered `/share/backtests/[token]/page.tsx` with `generateMetadata`, OG/Twitter tags (server fetch via `API_BASE_URL`, recharts chart extracted to a client child) |
| #679 | Dynamic per-result `opengraph-image` route — asset, timeframe, headline return, max drawdown, equity-curve sparkline (~60 points) via `ImageResponse` |
| #680 | Narrative card on the landed share page (closes #675) — `PublicBacktestView.narrative` now populated via the same pure `generate_narrative(summary)` path as the in-app result |
| #681 | Cost-honesty disclosure (closes #676) — `PublicBacktestView` now exposes `fee_rate`/`slippage_rate`/`spread_rate` as required (non-nullable) floats |

Combined with **#665/#669/#670** (ACTIONS #12 — felt-dollar buy-and-hold delta, merged earlier today), a shared link now unfurls with the actual honest result — return %, drawdown, sparkline — plus narrative prose and the felt-dollar framing, with the costs that produced it disclosed. This is the first fully-closed item in ACTIONS Group D (Distribution).

### 1.3 ACTIONS #14 — `llms.txt` agent-legibility: **SHIPPED**

- #683 (resolved-scope doc, no ADR — "a deletable file fails the hard-to-reverse bar") and #684 (`/llms.txt` Next.js route handler, `text/plain`, absolute links via `FRONTEND_URL`) both merged today.
- Per the resolved scope, **known follow-ups remain unbuilt and unticketed**: `robots.txt`/`sitemap.xml`, and promoting `/metrics-glossary` + `/strategy-guide` out of the `(app)` auth gate to enrich the index (see Q6).

### 1.4 #618 / #620 — NL-wedge onboarding A/B (ADR-0014)

No change since 06-13/06-14. #619's `resolveOnboardingArm` routing fork has been live since 2026-06-12; **#620 (configure the `onboarding_ab` PostHog experiment) remains open and `ready-for-human`**, now roughly **84 hours** with the code spine live and unmeasured.

### 1.5 #509 / #510 / #511 — architecture-review refactors (`needs-spec`, `risk-medium`)

No spec movement, but the calculus described in 06-13/06-14 is now concrete rather than hypothetical: the golden suite (1.1) is **complete and stable at 3/3 templates**. Re-capturing all three goldens after a #509 (block-evaluator dispatch) or #510 (shared Python↔TS validation seam) refactor is now a bounded, known cost rather than a growing one — see Q1.

### 1.6 #334 / FEAT-114 — PostgreSQL 18 evaluation (`needs-plan`, `needs-human`, `risk-high`)

No new stable PostgreSQL release (still 18.4/17.10/16.14/15.18/14.23, 2026-05-14). Noted in passing: **PostgreSQL 19 Beta 1** was released 2026-06-04 (dev track only, not relevant to the production-pin decision). Still blocked on AC-002.

### 1.7 #555 / #561 / #570 / #573 — activation-cohort / retention-A/B HITL items

Still open (`ready-for-agent`/`ready-for-human`), no new information this cycle. Worth noting: with ACTIONS #12/#13 now both live (1.2), the retention A/B in #570/#573 has more "felt severity" surface to actually measure once configured.

---

## 2. Security Risks

### New this cycle

| Item | Component | Severity | Status / Action |
|---|---|---|---|
| FastAPI 0.137.1 (2026-06-15) | FastAPI | None (patch on yesterday's 0.137.0 router-tree refactor) | Released same-day as 0.137.0. `backend/app/` has zero `.routes` usages (confirmed 06-14), so either 0.137.0 or 0.137.1 is safe to adopt — see Q4 for which to target. |
| `stripe` 11.2.0 → 15.2.1 (4 major versions) | Backend billing (`stripe==11.2.0`) | None (breaking-change risk, billing-adjacent) | **New finding.** stripe-python v15 migration guide: `StripeObject` no longer subclasses `dict` (no `.get()`/`.items()`/`.update()` — use `.to_dict()`), `decimal_string` fields change `str` → `decimal.Decimal`, pinned API version becomes `2026-03-25.dahlia`, Python ≥3.9 required (project is on 3.12, fine). Per AGENTS.md risk policy, any billing-logic change is `needs-human` — flagging for awareness only, not proposing action. |
| `bcrypt` 4.2.0 → 5.0.0 | Backend auth (password hashing) | None (major version) | New major available; auth-adjacent (password hashing) so should get explicit review before bumping even though no CVE motivates it. |
| `anthropic` 0.109.1 → 0.109.2 (2026-06-15) | Backend (`strategy_drafter.py`) | None | Same-day patch release. Floor remains `>=0.69.0`; no breaking changes expected. |
| `instructor` 1.15.1 → 1.15.3 (2026-06-15) | Backend (`strategy_drafter.py`) | None | Two patch versions behind the pin, both released today. `instructor`'s current line requires `openai>=2.0.0,<3.0.0` — already satisfied since `openai>=1.0.0` resolves to 2.41.1 today (no conflict). |
| `joserfc` 1.6.5 → 1.7.1 (2026-06-08) | Backend auth (JWT, replaces PyJWT per FEAT-113) | Unconfirmed | Auth-critical library, one minor version behind. Worth a changelog check before bumping given its security role. |
| `rq` 2.7.0 → 2.9.1 (2026-06-06) | Backend worker/queue | None found | Two minor versions behind. |
| Routine patch drift: `boto3` 1.35.0→1.43.30, `pydantic-settings` 2.6.1→2.14.1, `httpx` 0.27.0→0.28.1, `numpy` 2.4.4→2.4.6, `pandas` 3.0.2→3.0.3, `pandas-ta-classic` 0.5.44→0.6.20 | Backend | None found | No CVEs identified for any of these; listed for completeness as routine version drift. |

### Re-confirmed safe (no action)

| Item | Component | Notes |
|---|---|---|
| CVE-2026-48710 "BadHost" (Starlette/FastAPI host-header bypass) | Starlette/FastAPI | Confirmed already remediated as of 2026-06-11 (FEAT-118/105): `fastapi==0.136.1` pulls in a Starlette version satisfying the ≥1.0.1 fix; current latest Starlette is 1.3.1. No action — re-surfaced only because a research pass flagged it for re-verification. |
| CVE-2025-49844 "RediShell" / CVE-2026-23631 "DarkReplica" | Redis OSS/CE | Still fully patched by `redis:7.4.9-alpine`. No new Redis server release this cycle. |
| GHSA-g7cv-rxg3-hmpx (`@tanstack/*` npm supply-chain compromise, 2026-05-11) | `@tanstack/react-query` | `@tanstack/react-query` (project dep, latest 5.101.0) is **not** in the 42-package affected list (react-router, react-start, solid-router, vue-router, etc.). No action, first flagged 2026-05-20. |
| CVE-2025-55182 "React2Shell" | React/Next.js | Project on React 19.2.6/Next 16.2.6, well past the fixed versions. |

### Carried-forward / unchanged watch items

| Item | Component | Notes |
|---|---|---|
| React 19.2.6 → 19.2.7 | React | **5th consecutive report.** Confirmed 19.2.7 (FormData/Server-Actions fix, released 2026-06-01) remains latest — no newer 19.x exists. |
| Next.js 16.2.6 → 16.2.9 | Frontend | **New delta:** three patch releases (16.2.7/16.2.8/16.2.9) shipped since the baseline; no changelog entries found describing contents, but routine upgrade recommended. |
| CVE-2026-41305 (PostCSS via Next.js, GHSA-qx2v-qp2m-jg93) | PostCSS (bundled by `next`) | **Still unresolved upstream** — `vercel/next.js#93234` remains closed without a substantive fix (auto-closed for missing repro, not because PostCSS was bumped). Low practical risk (no runtime user-CSS path) but still tracked. |
| TypeScript 5.9.3 vs 6.0.3 | Frontend | **Re-surfaced after ~2 weeks quiet** (last detailed flag: 2026-06-02). TS 6.0 (March 2026, "last JS-based release") changes 9 default settings (strict/ESM/module/target/types/rootDir defaults, removes `moduleResolution: "classic"` and `amd`/`umd`/`systemjs`/`none` module targets) — a real migration item, not a routine bump. Now at patch 6.0.3. Migration plan still not drafted — see Q3. |
| Tailwind CSS 3.4.19 vs 4.x | Frontend | Upstream remains at **4.3.1** (no change since 06-12/06-14). Still correctly deferred as a planned major migration, not a routine bump. |
| `@xyflow/react` ^12.10.0 vs 12.11.0 | Frontend | Unchanged. 12.11.0 adds `autoPanOnSelection` (edge-of-viewport drag panning), fixes node-resize-beyond-extents, `onNodeDrag` typing, and a `useNodeConnections` guard — all relevant to the drag-and-drop canvas. No advisories. |
| `redis-py` 7.1.1 → 8.0.0 | Backend (worker infra) | Unchanged since 06-14. Still a planned-migration item (RESP3-by-default, type-hint split), not urgent — project doesn't install the `jwt` extra so CVE-2026-32597 remains N/A. |
| `postgres:16-alpine` / `python:3.12-slim` floating tags | Docker Compose | Unchanged; digest-freshness question still unresolved from prior reports. |

---

## 3. Implementation References

| Topic | Pattern / Library | Source |
|---|---|---|
| #509 — Block Catalogue dispatch refactor | Python decorator-based registry pattern (self-registering `@register("SMA")` evaluators replace if/elif chains) | [Stop writing giant if/else chains: the Python registry pattern](https://dev.to/dentedlogic/stop-writing-giant-if-else-chains-master-the-python-registry-pattern-ldm), [Registration decorators](https://www.dontusethiscode.com/blog/2024-05-22_registration-decorators.html) |
| #509 — alternative framing | `pyvisitor` — `@visitor.on()`/`@visitor.when()` type-dispatch decorators, if a visitor-pattern framing fits the interpreter+validator pairing better than a plain dict | [pyvisitor](https://github.com/realistschuckle/pyvisitor) |
| #510 — Pydantic → Zod codegen seam | `pydantic2zod` — actively maintained, generates Zod schemas directly from Pydantic v1/v2 models incl. field constraints (closest fit for the ~20 block-type parameter ranges) | [pydantic2zod](https://github.com/argyle-engineering/pydantic2zod), [Zod & Pydantic in Action](https://kashw1n.com/blog/zod-pydantic/) |
| #510 — alternative seam | JSON-Schema/OpenAPI intermediary via FastAPI's existing OpenAPI generation + `openapi-zod-client` | [openapi-zod-client](https://github.com/astahmer/openapi-zod-client) |
| #618/#620 — `onboarding_ab` PostHog experiment (SRM guardrail, 50/50 sticky) | PostHog's own SRM-as-guardrail guidance, plus a known bug where mid-experiment rollout-% changes shift bucketing and produce false SRM alarms — configure rollout once at creation | [Guardrail metrics for A/B tests](https://posthog.com/product-engineers/guardrail-metrics), [PostHog issue #43804](https://github.com/PostHog/posthog/issues/43804), [Sample size & running time docs](https://posthog.com/docs/experiments/sample-size-running-time) |
| #334 — PG18 for OHLCV/time-series workloads | Async I/O subsystem + B-tree skip-scan (helps multicolumn indexes like `(asset, timeframe, timestamp)` when leading column is omitted) | [Crunchy Data: Get Excited About Postgres 18](https://www.crunchydata.com/blog/get-excited-about-postgres-18), [Xata: Postgres 18 Features](https://xata.io/blog/going-down-the-rabbit-hole-of-postgres-18-features) |
| #334 — production benchmarks | 2026 async-I/O production benchmark claiming 20–35% throughput gains on NVMe for I/O-bound workloads — closest available analog, but project-specific OHLCV benchmarking still recommended before any decision | [PostgreSQL 18 Async I/O in Production (2026)](https://postgresqlhtx.com/postgresql-18-async-i-o-in-production-real-world-benchmarks-configuration-patterns-and-storage-performance-in-2026/) |
| `stripe` v11→v15 migration (if/when scoped) | Official migration guide covering `StripeObject`/`Decimal`/API-version changes | [stripe-python Migration guide for v15](https://github.com/stripe/stripe-python/wiki/Migration-guide-for-v15) |

---

## 4. Questions

1. **#509/#510/#511 sequencing (sharper now):** the golden suite is complete and stable at 3/3 templates (1.1/1.5). Does that tip the balance toward spec'ing #509 (block-evaluator dispatch) and/or #510 (shared validation seam) *now*, while the re-capture cost is a known fixed quantity, rather than waiting?
2. **`stripe` v11→v15 (new, 4 majors behind):** Given the breaking changes (`StripeObject` not dict-like, `Decimal` money fields, new API-version pin) and that billing changes are `needs-human`, should this become its own tracked planning item — similar to the redis-py and Tailwind major-version gaps?
3. **TypeScript 6.0 migration (re-surfaced):** Quiet for ~2 weeks since the 2026-06-02 detailed flag. Is a migration plan still wanted at some point, or has it been deprioritized against the ACTIONS-driven feature work (which has clearly taken precedence this week)?
4. **FastAPI version target (again):** With 0.137.1 out same-day as 0.137.0, and `.routes` usage already confirmed absent in `backend/app/`, is there any reason not to take 0.137.1 directly in the next dependency-bump pass?
5. **#618/#620 (6th-ish ask, carried forward):** ~84 hours with #619's code spine live and the `onboarding_ab` PostHog experiment still unconfigured. Is there a blocker beyond bandwidth, or should this get explicit priority given today's ACTIONS #12/#13 progress gives the retention/activation work more to measure?
6. **ACTIONS #14 follow-ups (new):** the resolved scope explicitly leaves `robots.txt`/`sitemap.xml` and promoting `/metrics-glossary` + `/strategy-guide` to public as known gaps. Worth opening tracked low-risk issues for these, or intentionally deferred?

---

## 5. Next Actions

### Low-risk / housekeeping
- [ ] **DEP-1 (carried forward, 5th time):** Bump `react`/`react-dom` `19.2.6` → `19.2.7`.
- [ ] **DEP-3 (updated):** Adopt FastAPI `0.137.1` (supersedes both `0.136.1` and yesterday's `0.137.0` question — see Q4).
- [ ] **DEP-4 (new):** Bump `anthropic` floor and `instructor` pin to `0.109.2` / `1.15.3` (both released today, routine).
- [ ] **DEP-5 (new):** Next.js `16.2.6` → `16.2.9` (routine patch chain).
- [ ] Routine patch bumps: `rq` → `2.9.1`, `joserfc` → `1.7.1` (review changelog first, auth-adjacent), `httpx` → `0.28.1`, `numpy` → `2.4.6`, `pandas` → `3.0.3`, `pandas-ta-classic` → `0.6.20`, `pydantic-settings` → `2.14.1`, `boto3` → `1.43.30`.

### Near-term (issue-driven)
- [ ] **#620 (HITL, carried forward — now ~84h):** Configure the `onboarding_ab` PostHog experiment.
- [ ] Consider opening tracked issues for ACTIONS #14's known follow-ups (`robots.txt`/`sitemap.xml`, public glossary/strategy-guide) — Q6.

### Watch / plan
- [ ] **TypeScript 5.9.3 → 6.0.3:** Migration plan still not drafted, re-surfaced after 2-week quiet (Q3).
- [ ] **`stripe` 11.2.0 → 15.2.1:** New 4-major gap, billing-adjacent, `needs-human` (Q2).
- [ ] **`bcrypt` 4.2.0 → 5.0.0:** New major, auth-adjacent — review before bumping.
- [ ] **`redis-py` 7.1.1 → 8.0.0:** Unchanged, still a planned-migration item.
- [ ] **CVE-2026-41305 (PostCSS via Next.js):** Still unresolved upstream.
- [ ] **#509/#510/#511:** Still `needs-spec`; sequencing question now more concrete (Q1).
- [ ] **#334 / FEAT-114:** No change — still blocked on AC-002.
- [ ] **Tailwind v3→v4:** No change (3.4.19 vs 4.3.1).

---

*Generated by Claude Code Action (claude-sonnet-4-6) on 2026-06-15. Sources: GitHub (issue/PR/commit history, ADR-0018, ADR-0019, ACTIONS.md), PyPI JSON API (fastapi, starlette, redis, rq, rq-scheduler, sqlmodel, alembic, anthropic, openai, instructor, boto3, stripe, pandas, pandas-ta-classic, numpy, pydantic-settings, httpx, bcrypt, joserfc), npm registry (next, react, react-dom, typescript, tailwindcss, @xyflow/react, recharts, posthog-js, @tanstack/react-query, lightweight-charts, storybook, vitest), GitHub Advisory Database (GHSA-qx2v-qp2m-jg93, GHSA-86qp-5c8j-p5mr, GHSA-g7cv-rxg3-hmpx), `vercel/next.js#93234`, stripe-python migration guide, PostHog docs/issue #43804, TypeScript 6.0 release notes (devblogs.microsoft.com), PostgreSQL 18 vendor posts (Crunchy Data, Xata, postgresqlhtx.com).*
