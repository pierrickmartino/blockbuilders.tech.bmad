# Nightly Research — 2026-08-17

**Prepared by:** Claude Code (nightly-research)
**Stack snapshot (verified from repo, not assumed):** Next.js 16.2.6 · React/React DOM 19.2.6 · TypeScript 5.9.3 · Tailwind 3.4.19 (v3, devDependency, pinned per FEAT-117) · @xyflow/react ^12.10.0 · FastAPI 0.136.1 · Starlette >=0.49.1 (unbounded floor) · uvicorn[standard] 0.47.0 · SQLModel 0.0.38 · Alembic 1.18.4 · psycopg2-binary>=2.9.10 · redis (client) 7.1.1 · RQ 2.7.0 · rq-scheduler 0.14.0 · Redis server (Docker) `redis:7.4.9-alpine` (exact pin) · PostgreSQL (Docker) `postgres:16-alpine` (floating tag)

**Continuity note:** the last research cycle is **2026-08-14** (`docs/ai/nightly-research/2026-08-14.md`, merged directly to main as #863, alongside the 2026-08-15 morning report as #864) — main is current, no gap in the doc trail. However, **PR #860** (research 2026-08-12) is **still open**, now created **76 days** ago by wall-clock-of-repo-age framing but really just **stuck since 2026-08-12** — it was superseded when 08-14's research merged out of order without it. It should be closed as superseded, not merged, since merging it now would insert a stale report after a newer one in the docs history. This report treats 2026-08-14 as the working baseline and scopes new research to **2026-08-14 → 2026-08-17** (3 days).

---

## Executive Summary

**No previously-flagged fix has been applied yet.** Verified live against the repo today: `next` is still pinned at `16.2.6` (vulnerable), `redis:7.4.9-alpine` is still the Docker tag (vulnerable, now with a formally assigned CVE — see below), `postgres:16-alpine` is still a floating tag with no confirmation it has been re-pulled since the 2026-08-13 security batch, and `backend/requirements.txt` still pins `starlette>=0.49.1` with no upper bound. All four P0/P1/P2 items from the 08-14 report carry forward unchanged in the repo, now flagged for a consecutive cycle.

**One item got sharper, not new: the Redis bug now has a CVE.** The previously-un-CVE'd Streams "shared NACK" use-after-free/double-free bug (fixed in `7.4.10`) has been assigned **CVE-2026-66373**, CVSS **7.5 (High)**. No newer 7.4.x tag exists beyond `7.4.10` — the fix and the action (bump `redis:7.4.9-alpine` → `redis:7.4.10-alpine` in both compose files) are unchanged, but this is no longer a "wait for a CVE number" situation.

**PostgreSQL 16.15 is confirmed still current** (Docker Hub `postgres:16.15-alpine` pushed 2026-08-16, no 16.16 since) — the 28-CVE security batch from 08-13 remains the actionable target, unchanged from the prior report. No further Postgres security release landed in this window.

**No new frontend or backend package releases of consequence.** Next.js, React, TypeScript, Tailwind, and @xyflow/react are all unchanged since 08-12/08-13 (only prerelease/canary churn). Backend packages saw routine, non-security bumps only: `rq` → `2.11.0` (published today, 2026-08-17), `redis` (Python client) → `8.1.0` (2026-07-30, a 7.x→8.x major the repo hasn't picked up, still pinned at `7.1.1`), `uvicorn` → `0.52.3` (already logged 08-13). None carry new CVEs.

**Supply-chain picture is mixed, not worsening on net.** The npm "Flooding Dropper"/WEL1DROPPER typosquat campaign appears to be **tapering** (one source explicitly notes velocity slowing since last week, package counts unchanged at ~846–1,033). The npm "ChainDrop" worm has an unverified follow-up report (The Register, 2026-08-15) suggesting continued activity, but the article could not be fetched to confirm scope growth — flagged as unverified rather than asserted. On PyPI, "Mini Shai-Hulud" continues spreading with newly-noted SLSA-provenance-forgery and OIDC-token-theft techniques, and a separate LiteLLM PyPI-compromise timeline was revised **earlier** than previously understood — neither is confirmed to touch this repo's direct dependencies (fastapi/starlette/sqlmodel/alembic/psycopg2-binary/redis/rq/anthropic/openai/instructor).

**One standing implementation-reference recommendation should change:** `hyzyla/outbox-streaming` (the "worth a future spike" candidate for #511) is now confirmed dormant for **3+ years** (last commit 2023-02-05, 0 open issues, 19 stars) — this is a stronger, direct-verification signal than the prior "not yet re-verified" framing. Recommend downgrading it from "spike candidate" to "not viable," reinforcing the hand-rolled outbox default. Separately, Coinrule's MCP surface has materially expanded (now live across 7 equity brokers alongside crypto, with both staged and one-step NL-to-strategy flows) — new evidence that the NL-wedge pattern underlying issue #618 is scaling across the competitor set, not staying crypto-niche.

**The three HITL-blocked issues (#618/#620, #570/#573, #555/#561) remain untouched**, now **67–70 days idle**, zero comments, for a **15th+ consecutive cycle**. `tasks/ai-usage-log.csv` is confirmed still stale (last row 2026-05-29, **80 days**, 11 data rows total).

---

## Findings for Active Issues

### Issue #618 — NL-wedge onboarding A/B (`ready-for-agent`, **blocked**)
**Blocker:** HITL #620 — configure `onboarding_ab` PostHog experiment flag, metrics, and guardrails.
**Status: no change to the issue**, but the underlying competitive evidence strengthened this cycle. Coinrule's MCP integration is now confirmed live across 7 equity brokers (Tradier, Trading 212, Tastytrade, eTrade, Public, Webull) in addition to crypto, offering both a staged (describe → validate → backtest → launch) and a one-step NL workflow — a materially larger surface than the previously-logged crypto-only reference. n8n's August roundups describe unified onboarding empty-states and hardware-accelerated canvas rendering for 100+-node workflows (exact ship date within this window unconfirmed — the source aggregates the whole month). `@xyflow/react` shipped `12.11.3`/`@xyflow/system 0.0.80` on 2026-08-12 (just before this window); no detailed changelog review was done — flagging for a follow-up touch/mobile-canvas diff check next cycle, given this repo's own mobile-canvas work (product.md §4.11). Opened 2026-06-11, now **67 days idle**.

### Issue #570 — WJL retention A/B (`ready-for-agent`, **blocked**)
**Blocker:** HITL #573 — configure `wjl_retention_ab` PostHog experiment, metrics, and runbook.
**Status: no change.** Opened 2026-06-09, now **69 days idle**. No new dated retention/micro-copy material found this cycle.

### Issue #555 — Activation drop-off cohorts (`ready-for-agent`, **blocked**)
**Blocker:** HITL #561 — build the diagnostic funnel, `time_to_activation`, and cohort cutover in PostHog.
**Status: no change.** Opened 2026-06-08, now **70 days idle** — still the longest-idle of the three HITL-blocked pairs.

**Observation across all three:** unchanged for a 15th+ consecutive cycle. Standing recommendation unchanged: needs a direct human decision on scheduling the PostHog config work, or a formal relabel of #618/#570/#555 to reflect deprioritization.

### Issue #334 — PostgreSQL 18 evaluation (`needs-plan`, `needs-human`, `risk-high`)
**Status: no change**, and the underlying ground did not shift further this cycle (unlike 08-14, which had the fresh 16.15 batch to report). Corroborating context found this cycle (not a new benchmark, but reinforces the standing finding): a PlanetScale "Postgres 17 vs 18" discussion thread and a "PostgreSQL 18 Performance Improvements Explained" writeup both confirm PG18's io_uring/AIO gains target **sequential scans, bitmap heap scans, and vacuum** — not plain index scans, which remain the dominant access pattern for candle-range OHLCV queries. No PG16-vs-18/19 time-series-specific index-scan benchmark exists as of today. `docs/features/FEAT-114-evaluate-postgresql-18-backtest-workloads.md` (`Status: Draft`) unchanged. GitHub issue now **92 days idle**, zero comments.

### Issues #509, #510, #511 — Backend refactors (`needs-spec`, `risk-medium`)
No change in issue state (opened 2026-06-03, now **75 days idle**). See Implementation References below — #511's candidate library assessment changed materially this cycle.

---

## Security Risks

### P0, unchanged — PostgreSQL: 28-CVE batch (16.15, released 2026-08-13) still unapplied; confirmed still the current point release
Re-verified live: `postgres:16.15-alpine` was pushed to Docker Hub on **2026-08-16** and remains the newest 16.x tag — no 16.16 has shipped. The repo's `postgres:16-alpine` floating tag will resolve to 16.15 on the next pull, but nothing in the repo confirms that pull has happened. Same CVE set as 08-14 (≈28 CVEs, most CVSS 8.8, e.g. `pg_dump` heap overflow, `EXTRACT()`-based SQL injection, cursor `CLOSE`/`DECLARE` type confusion, `to_char`/`plperl` buffer overruns), plus lower-severity items (e.g. a SCRAM timing oracle, CVSS 5.3; an `ascii()` OOB read, CVSS 4.3).
**Action (unchanged):** pull `postgres:16-alpine` (resolves to 16.15) and recreate the `db` service in dev + prod; confirm via `SELECT version();` post-deploy. The prior cycle's "audit enabled extensions to scope real exposure" action is still outstanding and unstarted.

### P1, now CVE-assigned (8th consecutive cycle) — Redis: `redis:7.4.9-alpine` vulnerable, fix is `7.4.10-alpine`, bug is now **CVE-2026-66373** (CVSS 7.5 High)
The Streams "shared NACK" use-after-free/double-free bug tracked since prior cycles now has a formal identifier: **CVE-2026-66373** (CVSS 3.1 vector AV:N/AC:H/PR:L/UI:N/S:U/C:H/I:H/A:H = 7.5 High). NVD lists the upstream fix as landing in `8.8.0`; Redis's own `7.4.10` release notes confirm the same fix was backported to the 7.4 branch. No 7.4.x tag newer than `7.4.10` exists.
**Action (unchanged, now with a citable CVE):** bump `redis:7.4.9-alpine` → `redis:7.4.10-alpine` (exact pin) in `docker-compose.yml` + `docker-compose.prod.yml`. One-line, no-code-impact fix.

### P1, unchanged (13th consecutive cycle) — Next.js: CVEs fixed in 16.2.11+/16.3.0+; repo still on vulnerable 16.2.6
No new Next.js release or advisory since `16.3.1` (2026-08-13, non-security). One research pass this cycle surfaced specific CVE identifiers (`CVE-2026-64641` through `CVE-2026-64649`) for the July 2026 batch that prior reports only ever counted as "9 CVEs" without listing IDs — **treat this ID range as unverified/needs a second look next cycle**, since it wasn't independently cross-checked and the tidy consecutive numbering is worth a sanity check before citing it externally. The underlying fact (9 CVEs, 4 HIGH/5 MODERATE, fixed 16.2.11+/16.3.0+) is unchanged and was previously confirmed via nextjs.org's own security-release blog post.
**Action (unchanged):** bump `"next": "16.2.6"` to `16.3.1`.

### P2, unchanged (5th tracked cycle) — Starlette: floor-pin gap persists
Latest release remains `1.6.0` (2026-08-08); no new Starlette release or advisory this cycle. Re-confirmed one of the five previously-cited CVEs directly: **CVE-2026-48710** (GHSA-86qp-5c8j-p5mr, CVSS 6.5, Host-header → `request.url.path` auth-bypass), affecting 0.8.3–1.0.0, fixed in `1.0.1`. Since the repo's `starlette>=0.49.1` has no upper bound, the actually-installed/locked version should be confirmed ≥1.0.1 at minimum, and ideally ≥1.3.1 to close the other 4 previously-cited CVEs (2 HIGH: CVE-2026-48818, CVE-2026-54283; 1 additional MODERATE: CVE-2026-48817; 1 LOW: CVE-2026-54282).
**Action (unchanged):** pin `starlette>=1.3.1` (closes all 5) or `>=1.6.0` (tracks current) in `backend/requirements.txt`.

### UNCHANGED-TO-MIXED — supply-chain campaigns
- **npm "Flooding Dropper"/WEL1DROPPER typosquat campaign:** package counts unchanged (~846–1,033 depending on source), and one source explicitly notes velocity has slowed since last week — read as **tapering**, not growing.
- **npm "ChainDrop"/keyv-cacheable worm:** a Register follow-up dated 2026-08-15 suggests continued activity/evasion, but the article could not be fetched to confirm details or updated package-count scope — **flagged as unverified**, not asserted as a new finding. No source found tying ChainDrop-affected packages to this repo's own Next.js/React/Tailwind/XyFlow dependency tree.
- **PyPI "Mini Shai-Hulud" wave:** still active (ongoing since late April 2026 per this cycle's sources, a longer running window than previously logged), now reported using SLSA provenance forgery and OIDC token theft. Separately, a LiteLLM PyPI-compromise timeline was revised **earlier** than previously understood in one analysis. Neither is confirmed to touch this repo's direct dependencies.

### MONITOR — routine, non-security movement only
`fastapi` 0.141.1 (unchanged since 07-29), `uvicorn` 0.52.3 (unchanged since 08-13), `sqlmodel` 0.0.39 (unchanged since 06-25 — correction to 08-14's "unchanged since [implied recent]," actually stale for ~7 weeks), `alembic` 1.19.1 (unchanged since 08-08), `psycopg2-binary` 2.9.12 (unchanged since 04-20), `rq` bumped to **2.11.0** (published **today**, 2026-08-17, no CVE — this resolves the 08-14 report's "not fully re-verified" flag on RQ's version), `redis` Python client at **8.1.0** upstream (2026-07-30) vs repo's pinned `7.1.1` — a major-version gap worth a deliberate look, not urgent. React 19.2.8, TypeScript 7.0.2 GA, Tailwind v3 capped at 3.4.19 / v4 at 4.3.3, `@xyflow/react` 12.11.3 — all unchanged from 08-12/08-14.

### UNCHANGED — `rq-scheduler` maintenance-risk flag
Still pinned at `0.14.0`, still last released 2024-10-29 (~22 months stale). No CVE, bus-factor observation only.

---

## Implementation References

### #509 — Block Catalogue dispatch-table refactor
**No change.** No cross-language *behavioral* (not just typed-shape) validator generator surfaced this cycle. Standing recommendation unchanged: JSON Schema as canonical block-definition format, feeding both the Python interpreter/validator and a generated frontend schema (see #510).

### #510 — Pydantic → Zod codegen seam
**No change, verified directly via GitHub API rather than search snippets.** `hey-api/hey-api`'s `pushed_at` is 2026-08-13T20:11Z but `git log --since=2026-08-14` returns zero commits — nothing landed in this window. Latest tagged release is still 2026-06-22 (no new release in ~8 weeks, though the repo has 553 open issues and appears generally healthy, not abandoned). Recommendation unchanged: target the FastAPI-OpenAPI → `openapi-zod-client`/`hey-api` pipeline over a direct Pydantic→Zod converter.

### #511 — Atomic `submit()` / commit-then-enqueue seam
**Recommendation should change.** Direct GitHub API check on `hyzyla/outbox-streaming` shows `pushed_at: 2023-02-05`, most recent commit is from that date ("Add basic Celery+SQLAlchemy tests"), 0 open issues, 19 stars — **dormant for 3+ years**, not merely "unverified this cycle" as previously framed. Downgrade this candidate from "worth a future spike" to **"not viable, do not adopt."** No competing outbox library surfaced. Standing default (hand-roll an outbox table + relay worker on FastAPI/SQLModel/Redis-RQ) is now the only sound path, with higher confidence than before.

### #334 — PostgreSQL 18/19 evaluation
No time-series/OHLCV-specific PG16-vs-18(19) index-scan benchmark found this cycle (re-confirmed). Corroborating (not new) evidence found: PlanetScale's PG17-vs-18 discussion and a general "PostgreSQL 18 Performance Improvements Explained" writeup both reinforce that io_uring/AIO gains target sequential/bitmap-heap scans and vacuum, not the plain index scans that dominate this repo's candle-range queries. No PG19 benchmark exists yet (PG19 is still pre-release, at Beta 3 as of the 08-14 baseline).

### #618 — NL-wedge onboarding
Coinrule MCP's expansion to 7 equity brokers alongside crypto (staged + one-step NL flows) is new, dated evidence (source: Medium/coinrule.com, July 2026) that the NL-wedge pattern is scaling across the competitor set. n8n's August roundups (unified onboarding empty-states, hardware-accelerated canvas for 100+ nodes) are relevant precedent but not precisely dated within this 3-day window — treat as background, not a fresh in-window finding. `@xyflow/react 12.11.3` shipped 2026-08-12 (just outside window); recommend a follow-up changelog diff next cycle for touch/mobile-canvas-relevant changes, given this repo's active mobile-canvas work.

---

## Questions for Human Review

1. **Still-unapplied P0/P1/P2 fixes (Postgres 16.15 pull, Redis 7.4.10 bump, Next.js 16.3.1 bump, Starlette floor pin):** these have now been flagged across 4–13 consecutive cycles with no repo change. Is there a blocker to approving these, or should they be batched into one dependency-bump PR and scheduled explicitly?
2. **PR #860 (research 2026-08-12):** still open, now superseded by the out-of-order merge of 08-14's research. Should it be closed as superseded rather than merged, to avoid inserting a stale report after a newer one in the docs history?
3. **Redis CVE-2026-66373 (CVSS 7.5):** now has a formal identifier — does this change the urgency/owner for the previously-approved-in-principle `7.4.10-alpine` bump?
4. **Next.js CVE ID range (`CVE-2026-64641`–`64649`):** surfaced this cycle but not independently cross-checked — worth a dedicated verification pass before citing these IDs in any external-facing security documentation?
5. **HITL gate timeline (#620, #573, #561):** now 67–70 days idle, zero comments, unchanged across 15+ consecutive cycles. Schedule the PostHog config work, or formally relabel #618/#570/#555 as deprioritized?
6. **#511 outbox candidate:** given `hyzyla/outbox-streaming` is now confirmed dormant 3+ years (not just "not yet re-verified"), should the standing hand-roll recommendation be moved from implementation-reference to an actual spec (`needs-spec` → drafted), since the "worth a spike" alternative is now closed off?
7. **Starlette actual resolved version:** the repo pins `starlette>=0.49.1` with no lockfile-confirmed upper bound — can someone confirm what version is actually installed in CI/prod today? If it's already ≥1.0.1 via FastAPI's own pin, the effective exposure may be narrower than the floor-pin gap suggests.
8. **`redis` Python client major-version gap** (pinned `7.1.1`, upstream at `8.1.0`): worth a deliberate compatibility-check pass, or intentionally held back?
9. **`tasks/ai-usage-log.csv`:** confirmed still stale — last row 2026-05-29, now **80 days**, 11 data rows total. Deprecate formally or reactivate?
10. **Issue #334 progression:** `FEAT-114` draft spec unreferenced by the issue thread for 9+ consecutive cycles, now 92 days idle. Should `/plan-feature FEAT-114` be scheduled, with an internal pgbench run against the now-current 16.15 baseline as the plan's first deliverable?

---

## Next Actions

| Priority | Action | Owner | Blocking |
|---|---|---|---|
| P0 | Pull `postgres:16-alpine` (resolves to 16.15) and recreate the `db` service in dev + prod — 28 CVEs fixed, several CVSS 8.8, still unapplied after 2 consecutive cycles | Human (approve) + agent (implement) | None — image already published |
| P1 | Bump `redis:7.4.9-alpine` → `redis:7.4.10-alpine` in both compose files — now formally CVE-2026-66373 (CVSS 7.5) | Human (approve) + agent (implement) | None — fix exists |
| P1 | Bump `next` off `16.2.6` to `16.3.1` — 13th cycle flagging this | Human (approve) + agent (implement) | None — patch exists |
| P1 | Close PR #860 (research 2026-08-12) as superseded rather than merge — 08-14 research already merged out of order | Human | Docs-history cleanliness |
| P2 | Pin `starlette>=1.3.1` (or confirm effective installed version first) in `backend/requirements.txt` | Human (approve) + agent (implement) | 5 known CVEs incl. 2 HIGH |
| P2 | Confirm actually-resolved Starlette version in CI/prod lockfiles before treating the floor-pin gap as fully open | Human/Claude | Accurate risk sizing |
| P2 | Get a direct status read on PostHog HITL gates #620/#573/#561 — 67–70 days idle, 15+ cycles unchanged | Human | #618, #570, #555 |
| P2 | Advance `FEAT-114` (PostgreSQL 18/19 evaluation draft) toward `/plan-feature`, using the current 16.15 baseline; first deliverable should be an internal pgbench run | Claude (plan only) | PostgreSQL 18/19 eval |
| P3 | Downgrade `hyzyla/outbox-streaming` from "spike candidate" to "not viable" in any #511 spec work; proceed directly to hand-rolled outbox design | Codex Web | Next implementation wave |
| P3 | Spec issues #509, #510 as previously recommended (JSON-Schema-as-canonical; hey-api/openapi-zod-client pipeline) — no new blockers found | Codex Web | Next implementation wave |
| P3 | Follow-up `@xyflow/react` 12.11.3 changelog diff for touch/mobile-canvas-relevant changes (relevant to product.md §4.11 mobile work) | Human/Codex | None — informational |
| P3 | Evaluate `redis` Python client major-version gap (7.1.1 pinned vs 8.1.0 upstream) | Human/Codex | None — informational |
| P3 | Reactivate or formally deprecate `tasks/ai-usage-log.csv` — stale since 2026-05-29, now 80 days | Human | AI-usage visibility |
| P4 (no urgency) | Batch routine (non-security) version bumps: fastapi, sqlmodel, alembic, rq (2.11.0), psycopg2-binary | Human/Codex | None — informational |
| P4 (no urgency) | Verify the specific Next.js CVE ID range (`CVE-2026-64641`–`64649`) surfaced this cycle before citing externally | Human/Claude | Citation accuracy |

---

*Sources:*
- npm registry `/latest` + version-history checks for next, react, react-dom, typescript, tailwindcss, @xyflow/react (queried live, 2026-08-17)
- GitHub Advisory Database API (`/advisories?ecosystem=npm`) for next.js, react, typescript, tailwindcss, xyflow · [nextjs.org July 2026 security release](https://nextjs.org/blog/july-2026-security-release) (queried live, 2026-08-17)
- ChainDrop follow-up: [The Register, 2026-08-15](https://www.theregister.com/) (title/date confirmed, full text fetch failed — unverified) · [The Hacker News](https://thehackernews.com/2026/08/) · [Sonatype blog](https://sonatype.com/blog) · [Security Boulevard](https://securityboulevard.com/2026/08/)
- PyPI project pages for fastapi, starlette, uvicorn, sqlmodel, alembic, psycopg2-binary, redis, rq, rq-scheduler (queried live, 2026-08-17)
- [GHSA-86qp-5c8j-p5mr / CVE-2026-48710](https://github.com/advisories/GHSA-86qp-5c8j-p5mr) · [CVE Record](https://www.cve.org/CVERecord?id=CVE-2026-48710)
- [PostgreSQL 16.15 release notes](https://www.postgresql.org/docs/release/16.15/) · [PostgreSQL 18.6/17.11/16.15/15.19/14.24 + 19 Beta 3 announcement](https://www.postgresql.org/about/news/postgresql-186-1711-1615-1519-1424-and-19-beta-3-released-3365/) · [Docker Hub postgres tags](https://hub.docker.com/_/postgres/tags) (queried live, 2026-08-17)
- [Redis 7.4.10 release notes](https://github.com/redis/redis/releases/tag/7.4.10) · CVE-2026-66373 writeup (basefortify.eu) · [Docker Hub redis tags](https://hub.docker.com/_/redis/tags) (queried live, 2026-08-17)
- [Tenable: Mini Shai-Hulud FAQ](https://www.tenable.com/blog/mini-shai-hulud-frequently-asked-questions) (queried live, 2026-08-17)
- PostgreSQL 16-vs-18 performance context: PlanetScale HN discussion thread, kkm-mako.com PG18 upgrade benchmark writeup
- GitHub API direct `pushed_at`/commit checks: `hey-api/hey-api`, `hyzyla/outbox-streaming` (queried live, 2026-08-17)
- Coinrule MCP: Medium (`medium.com/coinrule`, Jul 2026) · coinrule.com/mcp · n8n August 2026 roundups (blog.mean.ceo and others)
- `gh issue list` / `gh pr list` / `gh pr view 860` — live open-PR, open-issue, and merge-state check for the repo (queried live, 2026-08-17, before GitHub API rate limit was hit)
- Direct inspection of `frontend/package.json`, `backend/requirements.txt`, `docker-compose.yml`, `docker-compose.prod.yml`, `docs/features/FEAT-114-evaluate-postgresql-18-backtest-workloads.md`, `tasks/ai-usage-log.csv`, `tasks/lessons.md`, `tasks/todo.md` in this repo (2026-08-17)
- Prior-cycle report consulted for continuity: [docs/ai/nightly-research/2026-08-14.md](https://github.com/pierrickmartino/blockbuilders.tech.bmad/blob/main/docs/ai/nightly-research/2026-08-14.md) (merged baseline)
