# Design System — Action Plan Todo

**Source:** `design-system-audit-20260430.md`
**Date:** 2026-04-30

---

## P0 — Token gaps

- [ ] **Define `--info` independently from `--primary`**
  Both resolve to `204 75% 40%` (light) / `204 76% 63%` (dark). Pick a distinct sky-blue hue (e.g. `210 80% 45%`).
  Files: `globals.css`, `docs/design/tokens.md`, `docs/design/audit/phase4-contrast-matrix.md` (regen)

- [ ] **Fix hardcoded hex colors in `backtest/page.tsx:219`**
  Replace `"#ffffff"`, `"#166534"`, `"#991b1b"` with `hsl(var(--primary-foreground))`, `hsl(var(--success))`, `hsl(var(--destructive))`.
  Extract a `getCellColor()` helper for dark-mode safety.
  File: `src/app/(app)/strategies/[id]/backtest/page.tsx`

- [ ] **Define shadow tokens**
  Add `--shadow-sm`, `--shadow-md`, `--shadow-overlay` to `globals.css` and wire into `tailwind.config.ts`.

---

## P1 — Structural debt

- [ ] **Reconcile `docs/design-system.json` ↔ `globals.css`**
  Designate one as authoritative (recommended: `globals.css` as runtime truth; auto-generate JSON from it).

- [ ] **Extract `EmptyState` component**
  Consolidate inline empty state patterns across `dashboard`, `strategies`, `market` pages.
  Target: `src/components/ui/empty-state.tsx`

- [ ] **Extract `ErrorFallback` component**
  Consolidate inline error states across pages.
  Target: `src/components/ui/error-fallback.tsx`

- [ ] **Extract `LoadingCard` component**
  Compose `Skeleton` primitive into a reusable loading state.
  Target: `src/components/ui/loading-card.tsx`

- [ ] **Fix `profile-settings-section.tsx` native `<input>`**
  Replace with shadcn `Input`. Removes the `ring-2 ring-offset-2` focus inconsistency.
  File: `src/app/(app)/profile/profile-settings-section.tsx:167`

- [ ] **Define motion tokens**
  Add `--duration-fast: 150ms`, `--duration-normal: 250ms`, `--ease-default: cubic-bezier(0.16, 1, 0.3, 1)` to `globals.css`.
  Sweep ~30 inline `duration-150 / duration-200 / duration-300` classes and replace with token-based utilities.

- [ ] **Split `strategies/[id]/page.tsx`** (~1600 lines, limit 800)
  Extract sub-components: `StrategyHeader`, `StrategyRunBar`, `ConditionEditor`.
  File: `src/app/(app)/strategies/[id]/page.tsx`

---

## P2 — Accessibility

- [ ] **Canvas keyboard navigation audit**
  Audit and implement keyboard nav for `BlockPalette`, `InlinePopover`, `InspectorPanel`.
  Files: `src/components/canvas/BlockPalette.tsx`, `InlinePopover.tsx`, `InspectorPanel.tsx`

- [ ] **`StatusBadge` aria audit**
  Confirm icon-only cells carry `aria-label` per Phase 6.3 color-blind safety rule.
  File: `src/components/backtest/StatusBadge.tsx`

- [ ] **Landmark audit**
  Verify `<main>`, `<nav>`, `<aside>` presence in `(app)/layout.tsx` and key pages.
  Files: `src/app/(app)/layout.tsx`, `dashboard/page.tsx`, `strategies/page.tsx`

---

## P3 — Cosmetic consistency

- [ ] **Replace 26 hardcoded `rounded-*` class strings in `(app)/` pages**
  Use `rounded-lg / rounded-md / rounded-sm` (already mapped to `--radius`) instead of raw strings.
  Scope: all files under `src/app/(app)/`

- [ ] **Promote `CANVAS_CATEGORIES` to CSS vars**
  Move static HSL node-type color literals in `chart-theme.ts` into `globals.css` tokens so they participate in the token system.
  File: `src/lib/chart-theme.ts`

- [ ] **Merge `NarrativeCard` + `WhatYouLearnedCard`**
  Single component with configurable title and content slot.
  Files: `src/components/NarrativeCard.tsx`, `src/components/WhatYouLearnedCard.tsx`

---

## Deferred (separate PRs)

- [ ] Canvas wrapper components `NodeSocket`, `NodeIcon` — 184 atomic-class callsites (Phase 3.X)
- [ ] `docs/design-system.json` authoritative source decision (product sign-off required)
