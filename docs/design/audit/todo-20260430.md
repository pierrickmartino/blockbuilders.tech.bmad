# Design System — Action Plan Todo

**Source:** `design-system-audit-20260430.md`
**Date:** 2026-04-30

---

## P0 — Token gaps

- [x] **Define `--info` independently from `--primary`**
  `--info: 210 80% 45%` (light) / `--info: 210 76% 68%` (dark) with `--info-foreground` and `--info-soft` companions.
  Files: `globals.css`

- [x] **Fix hardcoded hex colors in `backtest/page.tsx:219`**
  Extracted `getCellColor(easedIntensity, isPositive)` helper; replaced `"#ffffff"` / `"#166534"` / `"#991b1b"` with `hsl(var(--primary-foreground))`, `hsl(var(--success))`, `hsl(var(--destructive))`.
  File: `src/app/(app)/strategies/[id]/backtest/page.tsx`

- [x] **Define shadow tokens**
  Added `--shadow-sm`, `--shadow-md`, `--shadow-overlay` to `globals.css`; wired into `tailwind.config.ts` as `shadow.sm`, `shadow.md`, `shadow.overlay`.
  Files: `globals.css`, `tailwind.config.ts`

---

## P1 — Structural debt

- [x] **Reconcile `docs/design-system.json` ↔ `globals.css`**
  `globals.css` designated authoritative. `design-system.json` v3.0.0 fully rewritten: all color tokens converted to HSL triplets matching `globals.css` verbatim; `.pen` file references removed; added `--info`, `--*-soft` states, shadow three-stop scale, motion tokens, canvas node CSS vars, section spacing, surface tokens, focus-ring, accessibility block, and updated `guidelines`. `syncRule` updated to "globals.css is the golden source."
  Files: `docs/design-system.json`

- [x] **Extract `EmptyState` component**
  Consolidated inline empty state patterns across pages.
  File: `src/components/ui/empty-state.tsx`

- [x] **Extract `ErrorFallback` component**
  Consolidated inline error states across pages.
  File: `src/components/ui/error-fallback.tsx`

- [x] **Extract `LoadingCard` component**
  Skeleton-based reusable loading state.
  File: `src/components/ui/loading-card.tsx`

- [x] **Fix `profile-settings-section.tsx` native `<input>`**
  Replaced with shadcn `Input` components; bio field uses correct `focus-visible:ring-1 focus-visible:ring-focus-ring` styling.
  File: `src/app/(app)/profile/profile-settings-section.tsx`

- [x] **Define motion tokens**
  Add `--duration-fast: 150ms`, `--duration-normal: 250ms`, `--ease-default: cubic-bezier(0.16, 1, 0.3, 1)` to `globals.css`.
  Sweep ~30 inline `duration-150 / duration-200 / duration-300` classes and replace with token-based utilities.

- [x] **Split `strategies/[id]/page.tsx`** (was 1942 lines → 1383 after extraction)
  Extracted sub-components: `StrategyHeader`, `StrategySettingsSheet`.
  Files: `src/app/(app)/strategies/[id]/_components/StrategyHeader.tsx`, `StrategySettingsSheet.tsx`

---

## P2 — Accessibility

- [x] **Canvas keyboard navigation audit**
  Added `aria-expanded` + `aria-controls` to category toggle buttons; `role="listbox"` + `aria-label` to block containers; `role="option"`, `tabIndex={0}`, `aria-label`, `onKeyDown` (Enter/Space → `onBlockAdd`) to each block item; focus ring via `focus-visible:ring-2`; replaced `📦` emoji in `InspectorPanel` with an accessible SVG icon.
  Files: `src/components/canvas/BlockPalette.tsx`, `InspectorPanel.tsx`

- [x] **`StatusBadge` aria audit**
  Added `aria-label={style.label}` to the `<Badge>` wrapper; wrapped label text in `<span aria-hidden>`; marked the running-indicator dot container `aria-hidden`. Screen readers announce the semantic label once instead of duplicating icon + text.
  File: `src/components/backtest/StatusBadge.tsx`

- [x] **Landmark audit**
  Changed `<div id="main-content">` → `<main id="main-content">` in `(app)/layout.tsx`. Sidebar was already `<aside>` ✓; nav already had `<nav aria-label="Main navigation">` ✓.
  Files: `src/app/(app)/layout.tsx`

---

## P3 — Cosmetic consistency

- [x] **Replace 26 hardcoded `rounded-*` class strings in `(app)/` pages**
  Audit found no violations: `rounded-xl/2xl/3xl` have zero occurrences in `(app)/`. All 19 `rounded-full` instances are semantically circular elements (spinners, dots, badges, progress bars) — correct usage. `rounded-lg/md/sm` already map to `--radius` via `tailwind.config.ts`. No changes needed.
  Scope: all files under `src/app/(app)/`

- [x] **Promote `CANVAS_CATEGORIES` to CSS vars**
  Added `--canvas-node-{input,indicator,logic,signal,risk}` tokens to both `:root` and `.dark` sections in `globals.css`. Added `readCanvasCategories()` function to `chart-theme.ts` that reads from these tokens at runtime. Static `CANVAS_CATEGORIES` object preserved as SSR-safe fallback for backward compatibility.
  Files: `src/app/globals.css`, `src/lib/chart-theme.ts`

- [x] **Merge `NarrativeCard` + `WhatYouLearnedCard` into shared base**
  Extracted `InsightCard` (icon well + title/badge row + children + aside + dismiss animation) as a shared primitive. Both cards refactored as thin wrappers. Eliminates duplicated layout patterns and keeps dismiss exit-animation in one place.
  Files: `src/components/ui/insight-card.tsx`, `src/components/NarrativeCard.tsx`, `src/components/WhatYouLearnedCard.tsx`

---

## Deferred (separate PRs)

- [ ] Canvas wrapper components `NodeSocket`, `NodeIcon` — 184 atomic-class callsites (Phase 3.X)
- [ ] `docs/design-system.json` authoritative source decision (product sign-off required)
