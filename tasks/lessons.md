- 2026-06-16 (Issue #689): A "static registry validated at import time" is the right shape when the data is authored in code and the validation rules are well-defined — import-time `validate_registry(CURRICULUM, _KNOWN_TEMPLATE_NAMES)` in `curriculum_registry.py` catches misconfiguration at server startup with a clear `ValueError`, rather than deferring to runtime or relying on tests alone. The known-template set is derived directly from `strategy_templates.TEMPLATES` (the single source of truth for seed data) so the validation stays in sync with any future seed additions without manual updates.
- 2026-06-15 (Issue #682): `docker-compose.prod.yml` has no `frontend` service at all (only `api`/`worker`/`scheduler`/`db`/`redis`/`storage`/`nginx`) — a PRD assumption that an env var needs "propagating into the frontend container in both dev and prod compose files" can be only half-true. When a resolved-scope PRD's infra assumption doesn't match the actual compose files, verify with `grep`/structure check before editing, and surface the mismatch to the user rather than inventing a new service to satisfy the PRD literally.
- 2026-06-15 (Issue #682): For a "pure function produces the exact body" deliverable (`buildLlmsTxt(origin) -> string`), the route handler itself needs no test (PRD said so explicitly) — `npm run build` is the cheapest way to confirm a brand-new `app/<dotted-name>/route.ts` (e.g. `app/llms.txt/route.ts`) is recognized by Next's router (shown as `ƒ /llms.txt` in the build's route table) without spinning up a dev server.
- 2026-06-15 (Issue #674): Next.js `opengraph-image` routes render via Satori (`ImageResponse`), which cannot resolve CSS variables (`hsl(var(--success))`) — `docs/design-system.json` tokens must be inlined as literal `hsl(h, s%, l%)` strings. Since the rendered image has no access to the viewer's light/dark preference, pick one fixed palette (used the **dark**-theme token values) so the card has a stable, deliberate look in any unfurl context, rather than trying to "match" the app's current theme. A presentational card component with only inline `style` objects (explicit `display: "flex"`, no Tailwind classes, no CSS vars) renders identically via `ImageResponse` *and* as a normal React component in Storybook — one component serves both the OG image and the async design-review artifact.
- 2026-06-14 (Issue #653): For the Bollinger Breakout golden (`compare entry: close > upper`, `compare exit: close < middle`, period 20), a flat warmup series converges `upper == middle == lower == price` (std=0) so neither comparison fires while `>`/`<` are strict — only an actual price move can trigger a signal. A reliable recipe: flat warmup (≥20 candles) → sharp ascend (breaks `close > upper`, entry) → sharp descend (crosses `close < middle`, signal exit) → flat (re-narrows the bands with no open position) → sharp ascend again (second entry) → flat tail (no exit signal, forces `end_of_data`). Same "run the real pipeline, capture actual output, hardcode it" approach as #651 — install `backend/requirements.txt` (plus `pandas-ta-classic`, not pulled in by a partial install) and call `app.backtest.indicators.bollinger(closes, period, std_dev)` / `run_golden_pipeline` directly on candidate series rather than hand-computing rolling stddev.
- 2026-06-14 (Issue #651): For a full-pipeline look-ahead test (`mutate_tail` + `assert_prefix_unchanged`), the naive "replace candle at `cut_index` onward" reading breaks itself: the engine's signal-exit reads the *cut candle's own close* (`exit_time` == the candle where `exit_long[i]` first fires, `exit_price` derived from that candle's `close`), so mutating that candle changes the very trade `assert_prefix_unchanged` requires to stay identical. The fix is an "anchor" design — `mutate_tail` preserves the candle *at* `cut_index` by value (new object, same OHLCV) and only reverses the trend for candles *after* it, starting the reversed walk from the anchor's `close`. This keeps `cut_index = index of trades[0].exit_time` (as literally specified) while making the prefix-unchanged property provable.
- 2026-06-14 (Issue #651): To derive an RSI(14) candle series that reliably crosses both the oversold (<30) and overbought (>70) thresholds for a golden test, don't compute Wilder's RSI by hand — install `backend/requirements.txt` in the sandbox and call `app.backtest.indicators.rsi(closes, period)` directly on candidate synthetic series. A 20-candle descend (RSI→0) + 10-candle ascend (RSI→~77) + 20-candle descend (RSI→~29) + 3 more candles (to execute the second entry and force `end_of_data`) reliably produces both an entry/exit pair and a final open trade. Capture the full `BacktestResult` (`dataclasses.fields(trade)`) from this *real* run as the hardcoded golden baseline — never hand-derive the expected floats.
- 2026-06-12 (Issue #642): React 19 pages using `use(params)` on a `Promise<{...}>` (the App Router param pattern) have no working render-test pattern in this codebase — `<Suspense>` + `findByRole`, `act(async () => render(...))`, and `act(async () => { await params })` all either hang or leave the DOM as an empty `<div />` with "component suspended... act call was not awaited" warnings, with no prior-art test files for any of the 4 pages using this pattern. The established workaround (prior art: `BacktestPageHeader`/`PageHeader.test.tsx`) is to extract the part under test into a small presentational component that takes plain props (no `use(params)`, no `fetch`) — e.g. `ComparePageHeader`, `SharedBacktestHeader` — and test that component directly. This satisfies "test the externally observable X" when the component is rendered unconditionally (no extra gating) by its parent page.
- 2026-06-12 (Issue #632): For "a counter increments on entry regardless of outcome" requirements (every request — success, declined, infra-failure — burns one ceiling unit), the guard should be checked exactly once, as early as possible (right after the `enabled` gate, before any pipeline work), and the guard itself should be outcome-agnostic — `allow(user_id)` just does an atomic `INCR`+compare. This makes "draft-plus-repair counts as one" free: the repair pass happens entirely *after* the single `allow()` call, so it never touches the counter again. No need to thread a "did this consume a unit yet" flag through the pipeline.
- 2026-06-12 (Issue #632): `market_data/circuit_breaker.py`'s `CircuitBreaker(redis_client)` — a small class taking a `Redis` client in its constructor with one Redis-backed method — is the cleanest "deep module" template for a new Redis-backed guard, better prior art than `auth.py:_check_rate_limit` (a bare function using a module-level `_get_redis_client()`). Constructor injection makes the guard trivially testable with `fakeredis.FakeRedis()` and a `RaisingRedis` stub for the fail-open case, with no monkeypatching of a redis factory function.
- 2026-06-11 (Issue #603): When an issue is logically gated on a not-yet-built upstream slice (#603 needs #6's auto-backtest-on-draft to create the real "land on result page in running→verdict" flow), a self-contained sessionStorage flag (`markDraftUnderReview`/`isDraftUnderReview`/`resolveDraftReview`, keyed by strategy id, ADR-0005 UI-only) lets the dependent state machine (`draftReviewReducer` + `useDraftReviewState`) be built and fully tested today. The flag is set at the one integration point that already exists (`draft/page.tsx`'s success handler) and read by the result/canvas pages — once the upstream slice ships and redirects to the result page, the surface activates with zero additional wiring.
- 2026-06-11 (Issue #603): For a disposition state machine where the reducer already handles all 4 outgoing actions (`ACCEPT`/`EDIT`/`KEEP`/`REJECT`) via one shared `Record<action, outcome>` lookup and a uniform "no-op unless `isUnderReview`" guard, writing the first disposition test (`ACCEPT`) and its minimal `switch` branch made all three sibling tests (`EDIT`/`KEEP`/`REJECT`) pass immediately — expected when the branch table, not the test, is what generalizes.
- 2026-06-10 (Issue #587): When an issue's acceptance criteria describe a contract (discriminated `drafted | declined` IR, no-partial-persist, `success: true` refusal envelope, frontend reason rendering) and the prior 3 slices already shipped that exact shape, the remaining gap can be a single string. Here, `collect_validation_errors` already returns `ValidationError.user_message` (the plain-language vocabulary the issue calls to "reuse"), but the endpoint's validator-invalid branch still built its own generic "did not pass validation" string — `errors[0].user_message or errors[0].message` was the entire functional fix. Always re-derive the current state from the actual code (read the endpoint + its tests) before assuming a slice needs new modules; closed sibling issues (#584–#586) are strong evidence the seam already exists.
- 2026-06-10 (Issue #586): `typing.Literal[tuple(values)]` is equivalent to `Literal[*values]` — Python's multi-subscript `obj[a, b, c]` syntax already passes a tuple to `__getitem__`, so `Literal[("a","b","c")]` and `Literal["a","b","c"]` produce the identical type. This lets `app.schemas.strategy_draft_ir.BlockType` be computed at import time from `drafter_vocabulary.vocabulary_block_types()` (catalogue ∪ inline risk blocks) instead of a hand-listed `Literal`, with Pydantic/`instructor` generating the same enum-shaped JSON schema as before.
- 2026-06-10 (Issue #586): The six inline risk/exit blocks (`position_size`, `take_profit`, `stop_loss`, `max_drawdown`, `time_exit`, `trailing_stop`) have no `BlockHandler`/`BlockSpec` in `CATALOGUE` (CONTEXT.md: deliberately out of catalogue scope) — their bounds exist only as inline magic numbers in `strategy_validation.validate_block_params`. To project them alongside catalogue blocks, hand-define `BlockSpec`s for them (`category="risk"`, `inputs=()`, `outputs=()`) with `ParamSpec` bounds copied from the validator (e.g. `stop_loss_pct: float 0.1-100`). Keeping these specs in the *same* `BlockSpec`/`ParamSpec` shape as catalogue blocks lets one projection function (`_all_specs()` = `CATALOGUE` specs ∪ `RISK_BLOCK_SPECS`) serve both the IR enum and the prompt vocabulary uniformly.
- 2026-06-10 (Issue #586): For the "new catalogue block becomes draftable automatically" property, `from app.backtest.catalogue import CATALOGUE` binds a reference to the *same* dict object used everywhere else — `monkeypatch.setitem(CATALOGUE, "widget", FakeHandler())` in a test mutates that shared dict in place, so a projection function that re-reads `CATALOGUE` on every call (not a module-level snapshot taken at import time) picks up the new block with zero drafter code change. The snapshot-vs-live-read distinction is the difference between this property being testable at all and being structurally untestable.
- 2026-06-10 (Issue #584): A "drafted | declined" discriminated-union IR (ADR-0011) plus a pure `GraphCompiler` makes the existing `strategy_validation.collect_validation_errors` directly reusable as the trust boundary for AI-authored content — no new validation logic needed. The compiler's only jobs are mechanical (mint ids, resolve Ports from the Block Catalogue, assign layout, build the `{from_port,to_port}` connection shape); semantic correctness is still enforced entirely by the existing validator, so a bad/declined draft never reaches persistence.
- 2026-06-10 (Issue #584): For a flag-gated endpoint with three outcomes (`success`/`declined`/`disabled`) where "nothing partial persists" is a hard requirement, structure the handler as a sequence of early-return guards *before* any `session.add()` — flag check, drafter outcome, `compile_graph` (catch `GraphCompilationError`), then `collect_validation_errors`. Only the final branch touches the DB, so there's no rollback/cleanup logic needed for the decline paths.
- 2026-06-10 (Issue #584): When adding the first multi-line text input to a frontend feature, check `frontend/CLAUDE.md`'s "Installed" shadcn/ui list first — `Textarea` wasn't listed (only Button/Input/Select/Dialog/Card/Tabs/Badge/DropdownMenu/Checkbox/Label/Tooltip). Adding it as a new `src/components/ui/textarea.tsx` mirroring `input.tsx`'s exact class list (border/shadow/focus-ring/disabled/aria-invalid) keeps it visually consistent without inventing new tokens.
- 2026-06-09 (Issue #572): The `react-hooks/refs` ESLint rule (React Compiler hooks plugin) forbids reading or writing `ref.current` during render — including the "lazy ref init" `if (ref.current === null) { ref.current = ... }` pattern when wrapped in an extra condition (e.g. `if (eligible && ref.current === null)`). For "compute once when a condition first becomes true" logic, use `useMemo(() => {...}, [condition])` instead — it's recomputed only when the dependency changes, and reading external synchronous sources (PostHog flags, localStorage-backed helpers) inside `useMemo` is fine since it's a read, not a mutation.
- 2026-06-09 (Issue #572): The `react-hooks/set-state-in-effect` rule flags `setState` called directly (unconditionally reachable) inside a `useEffect` body as "cascading renders" — even when gated by an early-return. Prefer deriving the value via `useMemo` (no state, no effect) and reserve `useEffect` purely for external side effects (`localStorage` writes, callbacks into the parent) keyed off the memoized value.
- 2026-06-08 (Issue #558): When a creation route already returns the persisted entity (here `clone_template` → `StrategyResponse` carrying `entry_path: "template_clone"`), the milestone event should read that field straight off the response rather than hardcoding the literal or reaching for the cohort resolver — the resolver's job (deriving `authoring_mode` and normalizing `null`/garbage into `unknown`) is a separate, explicitly-scoped slice (#560). Emitting `{ entry_path: strategy.entry_path }` directly satisfies "carries `entry_path: \"template_clone\"` (read from the persisted value)" without anticipating work the next issue owns.
- 2026-06-08 (Issue #558): Adding the first PostHog `trackEvent` call to a page that previously had none requires wiring both `useAuth()` (for `user?.id`) and `trackEvent` — mirror the exact pattern from sibling creation surfaces (`new-strategy-modal.tsx`, `strategy-wizard.tsx`: `trackEvent("strategy_created", { asset, timeframe, source }, user?.id)`) rather than inventing a new payload shape; the only required addition was the `entry_path` field the acceptance criteria calls for.
- 2026-06-08 (Issue #557): Expanding a shared union type to retire a value (`ResultsViewedEntryPath`'s `"manual"` → `"unknown"` per ADR-0009) is a one-line type edit but `npx tsc --noEmit` immediately surfaces every literal that depended on the retired value — both production code (the manual backtest page's hardcoded fallback) and test fixtures (`it.each([...])` lists, `as const` literals, `rerender({ entryPath: "manual" })`). Budget for fixing all of them in the same slice; an "isolated module" issue can still ripple into a handful of consuming files when its acceptance criteria explicitly call for a shared type to change.
- 2026-06-08 (Issue #557): When an issue says a type "expands to" a new value set that *retires* an old value (here `manual` is explicitly retired by ADR-0009, split into `blank_canvas`/`template_clone`), prefer the most honest interim replacement for any now-invalid literal over inventing a new one — e.g. the manual backtest page's `"manual"` fallback became `"unknown"` (it genuinely doesn't know the persisted path yet) rather than guessing `"blank_canvas"`. This keeps the slice both compiling and semantically correct, and gives the next slice (#559, which wires the true persisted path) an honest baseline to improve on.
- 2026-06-08 (Issue #557): For a small pure mapping function with a fully-specified branch table (here: 4 known inputs + null + garbage → 6 distinct outcomes), the vertical-slice TDD loop collapses naturally — writing the minimal `switch` for the first test case (`nl_wedge`) already implements the remaining branches, so subsequent `it`/`it.each` blocks go straight to GREEN. That's expected, not a smell: the interface was simple enough that "minimal code for this test" and "minimal code for the whole spec" coincided. Add a regression case for the *retired* value (`"manual"` → now `unknown`, not passed through) so the union-narrowing isn't silently lost in a future refactor.
- 2026-05-31 (Impeccable live): When a live-picked element uses generic utility classes shared across repeated containers, the wrap helper can land on the first sibling even with a text hint. Verify the wrapper location before authoring variants; if it wrapped the wrong section, move the live wrapper before replying `done` so accept/discard touches the intended source.
- 2026-05-31 (Issue #490): Binance `/api/v3/ticker/24hr` rejects whitespace in the `symbols` array param → HTTP 400, error -1100 "Illegal characters". `json.dumps(symbols)` defaults to a `", "` item separator, so the multi-symbol batch always fails; the fix is `json.dumps(symbols, separators=(",", ":"))`. A single-symbol request has no comma/space, so naive one-symbol smoke tests pass and hide the bug — always exercise the real multi-asset request, and write the regression test against the *serialized request* (`mock_client.get.call_args.kwargs["params"]["symbols"]`), not the live API.
- 2026-05-31 (Issue #490): The 400 above tripped the transient circuit breaker every refresh cycle; with CryptoCompare also quota-tripped, `PriceRouter.get_spot_prices` skipped both providers and returned `{}` *without raising* (skips never set `last_error`). `_fetch_full_ticker_items` then filled 51 zero-price placeholders, so logs read "wrote 51 prices to cache" while the market page showed all zeros. Lesson: an empty/all-skipped router result should not be persisted as real prices over good cache.
- 2026-05-31 (Issue #490, follow-up): A "missing data" sentinel must be falsy or omitted, never a zero-valued object. `CryptoCompareProvider` returned `SpotPrice(price=0)` for assets it couldn't price; because that object is truthy, the router's `remaining = [a for a in remaining if a not in merged]` treated the asset as satisfied — silently defeating the Binance fallback AND injecting a fake 0.0 into the cache. Binance already omitted missing assets; the fix made CryptoCompare match. Rule: providers omit on miss; the router only ever holds real prices.
- 2026-05-31 (Issue #490, follow-up): Spot-cache refresh now merges over last-known-good (`_merge_with_cached`): fresh prices win per pair, pairs absent this cycle retain their previous value, and an empty fetch leaves the cache untouched. A periodic single-writer cache should degrade by preserving the last good snapshot, not by overwriting it with a partial/empty result. Dedup/merge by a stable key (pair), not by list position.
- 2026-05-31 (Issue #490): The Compose stack bakes backend source into the image (no bind mounts for `api`/`worker`/`scheduler`). Editing host files and running `docker compose up -d` only restarts the *old* image — backend hotfixes require `docker compose build worker scheduler api && up -d`. Verify the running code with `inspect.getsource()` inside the container, not just the host file.
- 2026-05-29: Query-derived edit forms need an explicit snapshot/hydration path before entering edit mode. If local form state defaults are left untouched after async data loads, a no-op save can PATCH default values over an existing record. Add a regression test that loads an existing entity, opens edit mode, saves without field changes, and asserts the original values are sent.
- 2026-05-29: Alert-list load failures must remain distinguishable from a legitimate zero-alert account. When `data = []` is used as a render fallback, pair it with the query's `isError` state for the empty-state branch and retry controls so `/alerts/` failures do not invite users to create a new alert as if loading succeeded.
- 2026-05-29: When migrating primary page loads to TanStack Query, never destructure only `data = []` and `isLoading` for required list data. Also consume `isError`/`error` and render a load-failure path; otherwise failed initial loads are indistinguishable from a legitimate empty list.
- 2026-05-27 (FEAT-459): When publishing promotes a draft row in-place (mutating `version_number` + `status`), the max published version number must be computed against `status=PUBLISHED` rows only — otherwise the draft's `version_number=0` slot pollutes the max and the sequential numbering breaks. Always scope aggregate queries to the target status.
- 2026-05-27 (FEAT-459): After a successful publish, clearing `lastPersistedAt` in the reducer naturally disables the Publish button (no draft exists) without any extra boolean flag. State machine design: let one piece of state (the timestamp) serve double duty as both the "relative time" display and the "has draft" signal.
- 2026-05-26 (FEAT-454): When a parent renders a `CanvasStateProvider`, it cannot call `useCanvasState()` itself (hook must be inside the provider). The fix is the "bridge component" pattern: create small child components (`CanvasBootstrapper`, `ContextDispatchBridge`) that call the hook and populate parent-owned `useRef` callbacks, which the parent uses in its async handlers without direct context access.
- 2026-05-26 (FEAT-454): `CanvasFlags` (from `getCanvasFlags()`) is typed as `Record<CanvasFlagKey, boolean>` where `CanvasFlagKey` is the string-value type of `CANVAS_FLAGS`. Flag access must use `flags[CANVAS_FLAGS.inlinePopover]`, not `flags.inlinePopover` — the latter is a TypeScript error because the key is a string literal value, not a named property.
- 2026-05-26 (FEAT-454): When refactoring a hook's callback signature (e.g., `setNodes: Dispatch<SetStateAction<Node[]>>` → `onNodesChange: (nodes: Node[]) => void`), always update both the implementation and every test that instantiates the hook with the old API. TypeScript will catch these at `tsc --noEmit` time if the tests are `.ts`/`.tsx` files.
- 2026-05-20: Frontend Docker builds must exclude host `node_modules` and `.next` from the build context. If `COPY . .` runs after `npm ci`, a missing `.dockerignore` can overwrite clean image dependencies with stale local packages, causing mismatched Next.js and `@next/swc` versions even when `package.json` and `package-lock.json` are correct.
- 2026-05-20 (FEAT-113): `python-jose` is abandoned (CVE-2024-33663). Replacement is `joserfc==1.6.5`. Key differences: (1) `joserfc.jwt.decode` does NOT validate `exp`/`nbf`/`iat` automatically — call `JWTClaimsRegistry().validate(decoded.claims)` explicitly after decode; (2) the key must be a JWK object — use `joserfc.jwk.import_key("secret-string", "oct")` for HMAC and `joserfc.jwk.RSAKey.import_key(pem)` for RSA; (3) `exp` must be an int unix timestamp in claims (datetime objects also accepted by joserfc but int is explicit and safe); (4) `algorithms=[...]` is still a supported parameter on `jwt.decode` for explicit algorithm allowlisting (FEAT-106 pattern unchanged).
- 2026-05-18 (FEAT-108): Shell smoke-test snippets run under `zsh` should not assign to `status`; it is a read-only shell parameter. Use a neutral variable such as `rc` when preserving a command exit code after `curl`.
- 2026-05-18 (FEAT-117): Next.js builds can patch missing optional `@next/swc-*` entries into `frontend/package-lock.json` even when `next` itself is unchanged. If `next build` prints "Found lockfile missing swc dependencies, patching", run `npm install` afterward and keep the SWC optional lockfile entries if the subsequent build is clean.
- 2026-05-18 (FEAT-116): Dependency-upgrade planning should inspect package metadata for the exact target version rather than infer compatibility from release notes alone. For FastAPI, downloading the exact wheel and reading `Requires-Dist: starlette...` gives a concrete constraint to compare with `backend/requirements.txt` and record in the plan.
- 2026-05-18 (FEAT-112): Treat upstream source releases and container image tags as separate evidence. For Redis security verification, cite official Redis release notes or `redis/redis` releases for the patched source tag, then check Docker registry availability for the exact Compose candidate tag before recommending a follow-up image update.
- 2026-05-16 (FEAT-107): Container image security fixes should verify both source definitions and rendered Compose output. Use `rg` to prove floating tags are gone and `docker compose config` to prove the effective service image resolves to the patched pinned tag.
- 2026-05-16 (FEAT-106): Security hardening specs can be verification-only when the code is already in the desired state. Still add regression tests that exercise both the helper and an authenticated endpoint, so future edits cannot remove the explicit JWT algorithm allowlist unnoticed.
- 2026-05-16 (FEAT-105): For transitive security fixes, pin the vulnerable package directly in `backend/requirements.txt` next to the parent dependency and verify with both an exact `rg` requirement check and `pip show` from the project virtualenv. The resolver may install a newer patched version than the floor; record the resolved version in task tracking.
- 2026-05-13: CI workflows must not call `npm test` directly for the frontend until a `test` script exists. Use `npm run test --if-present` so lint/build remain enforced and a future test script starts running automatically.
- 2026-05-04 (FEAT-102): pandas-ta-classic normalizes MACD periods by swapping `fast` and `slow` when `slow < fast`. Wrapper column lookup must use normalized `min(fast, slow)` / `max(fast, slow)` names, otherwise valid reversed strategy params look for nonexistent `MACD_26_12_9` columns and raise KeyError.
- 2026-05-04 (FEAT-102): pandas-ta Bollinger column names encode std_dev with full float precision (e.g. `BBU_20_2.25`), not one decimal place. Column lookup must use prefix discovery (`next(c for c in df.columns if c.startswith(f"BBU_{period}_"))`) rather than `:.1f` format, which silently rounds 2.25 → "2.2" producing a KeyError at runtime.
- 2026-05-04 (FEAT-102): Test references must always bypass the implementation under test. When both the endpoint and the test reference call the same `ind.*` wrapper, a bug in the wrapper makes both wrong identically and the test passes. Use raw `ta.*` calls (or a pure-math formula) as the independent oracle. If the indicator is a custom implementation (e.g. Fibonacci), write a standalone `_fib_ref()` function that replicates the math without calling the production function.
- 2026-05-04 (FEAT-102): When `pytest.ini` uses `pytest-env` to set `DATABASE_URL`, any lazy import of `app.main` inside a test function body will trigger `Settings()` with the pytest.ini value — even if conftest.py set `os.environ` at module level. Fix: set `os.environ` at the very top of the test file (before all imports) and import `app.main` at module level so `Settings()` sees the override at collection time, not run time.
- 2026-05-04 (FEAT-102): Parity tests for the backtest path must call `interpret_strategy()` + `run_backtest()`, not just raw indicator functions. Only calling indicators directly leaves the interpreter's `_compare(None)→False` guard and the engine's trade logic completely untested. The signal parity assertion pattern is: `assert entry == (val is not None and val <op> threshold)`.
- 2026-05-04 (FEAT-102): Chart-data parity tests must call `GET /market/chart-data` via the HTTP client, not indicator functions directly. The endpoint adds auth, candle fetching, series serialization, and key-naming layers — all of which are invisible to direct function calls. Use the `client` fixture and `_series_values(body["indicators"], key)` to extract per-series values for comparison.
- 2026-05-04 (FEAT-102): Determinism tests (`TC-07`, `TC-08`) must repeat the full computation path (both `interpret_strategy+run_backtest` or both HTTP calls) and compare output metrics, not just raw indicator arrays. Comparing `BacktestResult.num_trades`, `total_return_pct`, `final_balance`, `win_rate_pct`, and `Trade.pnl/entry_price/exit_price` gives meaningful determinism coverage.
- 2026-05-03 (FEAT-102): pandas-ta-classic (`pandas_ta_classic`) is the maintained Python 3.12 + numpy 2.x compatible fork of pandas-ta. Column name convention: `SMA_{p}`, `EMA_{p}`, `RSI_{p}`, `ATRr_{p}`, `OBV`, `MACD_{f}_{s}_{sig}`/`MACDs_…`/`MACDh_…`, `BBU_{p}_{sd}`/`BBM_…`/`BBL_…`, `STOCHk_{k}_{d}_{sk}`/`STOCHd_…`, `ADX_{p}`/`DMP_{p}`/`DMN_{p}`, `ITS_{t}`/`IKS_{k}`/`ISA_{t}`/`ISB_{k}`.
- 2026-05-03 (FEAT-102): pandas-ta RSI and ATR warmup is `period-1` candles (first valid value at index `period-1`, not `period`). Tests asserting warmup should use `range(period-1)`, not `range(period)`.
- 2026-05-03 (FEAT-102): pandas-ta `stoch()` returns a trimmed DataFrame starting at index `k_period-1`. Use `s.reindex(range(n))` (as in `_ta_adapter.from_series`) to pad missing leading indices with NaN → None.
- 2026-05-03 (FEAT-102): pandas-ta OBV seeds at `volume[0]` with no warmup period (index 0 is valid). Old hand-rolled OBV returned None at index 0 — update tests that assert OBV[0] is None.
- 2026-05-03 (FEAT-102): Ichimoku ISB (Span B) is displaced by `kijun` periods. First valid ISB index is `senkou + kijun - 1` (e.g., 52+26-1=77 for default params), not just `senkou`.
- 2026-05-07 (FEAT-103): When a component uses a single `error` state for both action-scoped errors (e.g. clone failure) and page-scoped errors (e.g. strategies load failure), the render guard must check a secondary flag (`strategiesLoadFailed`) to decide whether to show a retry button — creating an always-paired condition that is easy to forget. Routing action errors to `toast.error()` and keeping `error` solely for blocking load failures eliminates the secondary flag from the render: every remaining `{error && (...)}` case now unconditionally shows the retry button, and no inline banner is reserved for action failures.
- 2026-05-03 (FEAT-101): Transient feedback (backtest start confirmation, shortcut guidance) was previously stored in React state as an inline banner. Replacing it with Sonner toast removes the state variable entirely — no clearing on re-submit needed, no layout disruption, and no risk of stale banners persisting across navigation events.
- 2026-05-03 (FEAT-101): The frontend has no `npm test` script. All FEAT-101 TCs are verified manually in the dev server; the test plan documents this gap. Do not attempt to run `npm test` as part of the build step.
- 2026-05-02: Before committing local agent setup, check `git diff --cached --name-status` for tool state such as `.codex/`, auth files, sessions, and logs. Keep those paths ignored and out of repository history.
- 2026-05-03 (FEAT-100): Multi-output indicators (MACD, Bollinger, Stochastic, ADX, Ichimoku, Fibonacci) each return multiple named series from a single backend indicator key. Any future indicator feature touching these must account for series key namespacing (e.g. `macd`, `macd_signal`, `macd_hist`) rather than assuming one key = one series.
- 2026-05-03 (FEAT-100): The frontend has no test runner. Backend TCs pass automatically via pytest; frontend TCs are manual-only until a Playwright suite is introduced. Document this gap in the test plan coverage table rather than leaving it implicit.
- 2026-05-03 (FEAT-100): The indicator catalog in the chart panel is derived from `BLOCK_REGISTRY` (canvas blocks). When adding new strategy indicator blocks, the chart panel automatically gains the new indicator — no separate sync needed.
- 2026-05-03 (FEAT-100): Async chart fetch hooks must guard state commits with a latest-request token, not just an is-mounted check. Dependency changes can keep the component mounted while older `/market/chart-data` responses are still in flight.
- 2026-05-03: UI readout state derived from chart interactions must reset on chart context changes such as asset or timeframe changes. Keeping hover/crosshair state above the chart canvas can otherwise display stale candle data after the data source changes.
- 2026-05-03 (FEAT-101): Global keyboard shortcut handlers that trigger toasts must guard `KeyboardEvent.repeat` or use stable toast ids. A held shortcut can otherwise enqueue duplicate transient notifications faster than users can dismiss them.
- 2026-05-03: Daily chart candles should use date-only `YYYY-MM-DD` / BusinessDay values in lightweight-charts. Offset-less ISO datetimes must be parsed as UTC for intraday candles, otherwise browsers interpret them as local time and can shift UTC chart labels by one day.
- 2026-05-15 (spec-from-issue): Test-plan commands must reflect commands that actually exist in this repo. Do not invent `npm test -- --testPathPattern=...` entries when no frontend test script/spec files exist; prefer validated repo-level commands (`npm run lint`, `npm run build`) plus explicit manual verification steps where automation is unavailable.
- 2026-05-28 (Issue #475): `useMutation.mutate()` returns `void` — the mutation runs in a Promise chain the caller cannot await. In tests, use `waitFor` to check that API mocks were called; direct synchronous assertions after `act(() => mutate(...))` will fail for network-level checks. Optimistic state updates that use direct `setState` calls (before `mutate`) remain synchronous and can still be checked synchronously.
- 2026-05-28 (Issue #475): For hooks with manual pagination/accumulation (load-more pattern), use `queryClient.fetchQuery` (imperative) rather than `useQuery` (declarative). `useQuery` re-fetches whenever its key changes, which breaks debounced search — changing the search input state immediately changes the key and fires a fetch before the debounce timer fires.
- 2026-05-28 (Issue #475): Place `setQueryData` before the first `await` in a `useMutation` `onMutate` handler so the optimistic update is synchronous in the JS call stack. If `setQueryData` comes after an `await`, React may not see the update until a later microtask tick, requiring `waitFor` in tests rather than a synchronous assertion.
- 2026-06-08 (Issue #556): When two frontend routes share one backend create endpoint (blank-canvas modal and wizard both call `POST /strategies/`) but need to be distinguished server-side, the simplest fix is a client-supplied enum field on the request schema (`entry_path`), validated server-side against a deny-list (`CLIENT_STAMPABLE_ENTRY_PATHS`) that excludes values only a dedicated route may stamp (`template_clone` stays 422 from the shared endpoint, stamped unconditionally only inside `clone_template`). No new endpoint or routing logic needed.
- 2026-06-08 (Issue #556): Adding a required field to a shared TypeScript domain type (`Strategy.entry_path`) immediately surfaces every test fixture that builds a `Strategy` object literal as a `TS2741` "missing property" error — `npx tsc --noEmit` finds them all in one pass. Fix by adding the field to each mock (`entry_path: null` mirrors the nullable-no-backfill semantics) rather than loosening the type.
- 2026-06-08 (Issue #556): To prove a large `tsc --noEmit` error count is pre-existing noise (e.g. missing `node_modules` in a remote env) rather than a regression, `git stash` the change, re-run, and diff the count — identical totals before/after confirm the baseline is untouched. `frontend/node_modules` can be entirely absent in this remote environment, which also makes `npx vitest run` fail at config-load time (`Cannot find module 'vitest/config'`); `tsc --noEmit` is then the only available frontend correctness check.
- 2026-06-08 (Issue #556): Forward-written documentation (ADR-0009, runbook §7, ANALYTICS_SETUP) can describe a multi-slice initiative's *target end state* in present tense before every slice ships — e.g. the runbook claims the manual backtest page "no longer hard-codes `manual`" even though that depends on a later frontend slice this issue doesn't touch. Don't "fix" such text mid-initiative; verify it doesn't contradict what *this* slice shipped, and leave end-state descriptions for the slice that actually delivers them.
- 2026-06-10 (Issue #588): `instructor`'s `from_anthropic`/`from_openai` return an `Instructor` wrapping the underlying provider SDK client at `.client` — tests can assert provider/base_url selection (`isinstance(drafter._client.client, openai.OpenAI)`, `str(drafter._client.client.base_url)`) by constructing real (but key-less/fake-keyed) provider clients, with zero network calls. No need to mock `instructor` itself.
- 2026-06-10 (Issue #588): `openai` was already a transitive dependency of `instructor` (present in the environment) but not declared in `requirements.txt`. Once a module imports it directly (`import openai`), add it as an explicit top-level dependency — relying on a transitive install is fragile across dependency upgrades.
- 2026-06-10 (Issue #588): For provider-agnostic config with one "selected" provider, a single `PROVIDER -> key_field_name` dict (`STRATEGY_DRAFTER_PROVIDER_KEYS`) shared between the client-factory (`getattr` lookup) and the startup validator (fail-fast on the selected provider only) avoids duplicating the provider list in two places.
- 2026-06-10 (Issue #589): Both `anthropic.APIError` and `openai.APIError` (the latter also covers OpenRouter, since it uses the OpenAI SDK) are base classes for timeout/5xx/rate-limit/auth errors in their respective SDKs — catching just these two plus `instructor.core.InstructorRetryException` (exhausted schema-retries) covers the full "infra failure" class without per-error-type branching. `instructor.exceptions` is deprecated in favor of `instructor.core` (same symbols, just a warning if imported from the old path).
- 2026-06-10 (Issue #589): To unit-test that a caught provider exception's *message* doesn't leak into the user-facing error, construct the real exception with a fake "secret" string as its message (e.g. `anthropic.APIError("sk-ant-...", request=..., body=None)`) and assert the wrapped `StrategyDrafterError`'s message does not contain it — proves the handler discards `str(exc)` rather than forwarding it.
- 2026-06-10 (Issue #589): `httpx.Request("POST", url)` and `httpx.Response(status, request=request)` are enough to construct real `anthropic`/`openai` SDK exceptions (`APITimeoutError(request=...)`, `RateLimitError(msg, response=..., body=None)`) in tests — no need to mock the exception classes themselves.
- 2026-06-10 (Issue #589): When an endpoint already has two non-error outcome shapes (`success`/`declined` in one `200` envelope) and a new *error* outcome is added, raising `HTTPException` (caught by the frontend's existing `ApiError` path) is simpler than adding a third value to the response union — the frontend already has a separate, visually-distinct `error` state from its `declined`/`disabled` `status` state, so the two failure classes fall out naturally without new state shapes.
- 2026-06-10 (Issue #590): The NL-wedge response has four client-visible outcomes (`success`/`declined`/`disabled`/thrown infra error), but the PRD's draft-funnel taxonomy only defines three (success/refusal/infra-failure). `disabled` (feature flag off) is a config state, not a drafting outcome — `nl_draft_requested` fires for it (a real submit happened) but none of `nl_draft_drafted`/`nl_draft_declined`/`nl_draft_errored` do, since no draft was actually attempted server-side. Compute `resolveCohort("nl_wedge")` once per submit and spread it into all four `trackEvent` payloads to keep `entry_path`/`authoring_mode` consistent with `strategy_created`/`results_viewed`.
- 2026-06-11 (Issue #604): When a follow-up slice (#604) needs a disposition hook from a prior slice (#603's `useDraftReviewState.reject()`) to fire only at a *deferred* commit point rather than on the initiating click, don't modify the prior hook's timing — keep `reject()` synchronous (flips `isUnderReview` + logs `nl_draft_outcome` + clears storage in one call) and simply defer *calling* it until the grace window elapses. A generic `useDeferredDelete({ onCommit, onUndo })` hook (timer + sonner Undo toast, cleared on unmount) sits in front of it: `onCommit` runs `StrategiesApiClient.delete()` then `draftReview.reject()`, `onUndo`/unmount runs neither. Zero changes needed to `draft-review-reducer.ts` or `useDraftReviewState.ts`.
- 2026-06-11 (Issue #604): "Resolve to keep on interrupt" (ADR-0012 §6/§7) for a setTimeout-based grace window is just a `useEffect` cleanup that clears the pending timeout on unmount — no extra state or persistence needed. A hard browser exit naturally resolves to keep too, since an unfired `setTimeout` never runs after the page unloads.
- 2026-06-11 (Issue #605): Next.js App Router has no router-level "navigation start" event to cancel, so an "are you leaving?" guard for `<Link>`-based in-app navigation is best implemented as a single `document.addEventListener("click", handler, true)` (capture phase) effect: call `event.preventDefault()` before the bubble-phase reaches `Link`'s own click handler, which already checks `event.defaultPrevented` before calling `router.push`. This intercepts all anchor-based navigation (sidebar, back links, etc.) from one place with no per-`Link` changes, at the cost of not covering `router.push(...)` calls from non-anchor buttons (acceptable — those are explicit-disposition actions in this feature).
- 2026-06-11 (Issue #605): For hook tests that dispatch synthetic DOM events (`window.dispatchEvent(new Event("beforeunload"...))`, `anchor.dispatchEvent(new MouseEvent("click"...))`) which trigger `setState` inside the hook's listener, wrap the dispatch in `act(...)` — otherwise `result.current` from `renderHook` reflects the pre-event state and assertions on it (e.g. `isModalOpen`) fail even though the handler ran correctly.
- 2026-06-11 (Issue #614): When a shared helper (`startAutoBacktest`) hardcodes a telemetry field (`source: entryPath`) but a second caller (the wizard) needs to preserve a *different* pre-existing literal (`source: "wizard_first_run"`) for the same `entryPath` ("wizard"), add an optional override param (`source?: string | null`, defaulting to `entryPath`) rather than forcing both callers to the same value or duplicating the helper. Keeps the cohort-derivation (`resolveCohort(entryPath)`) single-sourced while letting `source` diverge per caller.
- 2026-06-11 (Issue #614): To regression-test "caller now routes through shared helper X, with behavior unchanged", mock the helper's module with `vi.mock(path, async (importOriginal) => ({ ...await importOriginal(), helperName: vi.fn(actual.helperName) }))` — this lets the test assert the helper was *called* with the right params (`toHaveBeenCalledWith`) while still running its real implementation, so downstream effects (API client calls, `trackEvent` payloads) are observable in the same test without a separate integration test.
- 2026-06-12 (Issue #619): Extending lesson #10's "compute once via `useMemo`" pattern to a fork with an *early-exit* branch: when one branch (`already-onboarded`) must never call an external read (`getExperimentVariant`) that other branches need, put the branch check *first* inside the `useMemo` and short-circuit to the pure decider with a placeholder input (`variant: undefined`) for that branch — the placeholder is safe because the decider's own branch order ignores `variant` once `hasCompletedOnboarding` is true. This keeps "variant read at most once, only for users who need it" provable from the hook alone, without the decider needing an extra "skip read" flag.
- 2026-06-12 (Issue #625): A bounded "compile -> validate [-> redraft]*" loop is cleanest as `while True` with a manual `attempt` counter, not `for attempt in range(max_repairs + 1)` — every branch (success, decline-via-compile-error, decline-via-exhausted-repairs, decline-via-model) returns inside the loop, so `while True` reads as `NoReturn` to mypy with no dead "unreachable after loop" code, whereas a `for` loop still wants a trailing return/raise.
- 2026-06-12 (Issue #625): To unit-test "decline reason comes from the *repaired* attempt, not the original" without mocking, give the fake drafter's `redraft` return an IR that fails the validator for a *different* reason than the original (e.g. original is missing an exit -> `MISSING_EXIT`; repaired has an exit but an out-of-range `position_size` -> `INVALID_PERCENT`). If both attempts produced the same error, a test could pass even if the code accidentally cached the first attempt's `errors[0]`.
- 2026-06-12 (Issue #625): For a Strategy-drafter repair prompt, reuse the existing `_SYSTEM_PROMPT` + original `nl_text` user turn and append a *third* message (the repair instructions: prior IR as `model_dump_json()` + each error's internal `message`) rather than building a second full system prompt — keeps the vocabulary/rules single-sourced between `draft()` and `redraft()`.
- 2026-06-12 (Issue #626): When a pipeline result already encodes "which branch produced this" via the existing `attempt` counter (#625's `draft_and_repair`), derive a new telemetry dimension (`resolution: clean | repaired | declined`) as a field on the existing result dataclasses rather than threading a second return value or recomputing state at the call site — `"clean" if attempt == 0 else "repaired"` on the success path, and a `"declined"` default on the decline dataclass covers all three decline branches (model-declined, compile-error, repair-exhausted) for free.
- 2026-06-12 (Issue #634): The draft page already has two non-error "status" kinds (`declined`/`disabled`) sharing one amber alert plus a separate red `error` state for thrown `ApiError`s. A third outcome (429 "slow down") fits cleanest as a third `DraftStatus.kind` (`rate_limited`) reusing the same alert element with conditional styling (`--info`/`--info-soft` tokens vs. the amber classes) — "distinct from declined/disabled/error" doesn't require a fourth state variable, just a kind-based className branch, and skipping the `declined`-only "Try rephrasing" hint for the new kind.
- 2026-06-12 (Issue #626): To assert a structured `logger.info(event, extra={...})` call from an endpoint test, use `caplog.at_level("INFO", logger="<module path>")` scoped to the producing module (e.g. `"app.api.strategies"`) and read custom `extra` fields directly off `record.<field_name>` (stdlib `LogRecord` exposes `extra` kwargs as attributes) — no need to parse JSON output or mock the logger.
- 2026-06-13 (Codex follow-up #1): "Read a PostHog flag once at the routing fork" is only race-free if the read is deferred until flags load. `useState(() => getFeatureFlag(...))` and `useMemo(..., [ready])` both freeze the value at first render, before `PostHogBootstrap`'s `useEffect` has loaded flags — locking new signups into the default arm. Fix: a `onExperimentFlagsReady(cb)` helper wrapping `posthog.onFeatureFlags` (resolving synchronously when consent isn't accepted / SSR / SDK error so callers never hang), plus a timeout safety-net, and resolve *all* flag reads (variant AND kill-switch) inside that callback. Reading only one of the two flags late still leaves the bug.
- 2026-06-13 (Codex follow-up #6): A server-side analytics path (`track_backend_event` in the worker) that fires regardless of consent needs the user's choice persisted server-side — the client's `localStorage` consent never reaches the backend on its own. Minimal slice: nullable tri-state `analytics_consent` column (None=undecided, preserves prior emit-everything behavior; False=declined suppresses), a `PUT /users/me/analytics-consent` endpoint, a best-effort post-auth sync from `auth.tsx` (so the "decide consent before login" flow lands), and a `consent_declined` gate computed once per job (one `session.get(User)`), passed to each `track_backend_event` call.
- 2026-06-13 (Codex follow-up #4): To emit the canonical `signup_completed` only for *new* OAuth users (the frontend can't tell new from returning), return `is_new_user` from the OAuth callback (`AuthResponse`, set where the `User` row is created) and branch in `completeOAuth`: new → `signup_completed`, returning → `login_completed`, mirroring the email signup/login split. Keeps the activation funnel's step-1 cohort inclusive of OAuth without redefining the funnel.
- 2026-06-15 (Issue #675): When a pure response builder (`build_public_view`) already computes a `BacktestSummary` it needs internally (via `_build_summary`), and a sibling builder (`build_status_response`) takes `narrative` as a caller-supplied param only because *its* caller also needs session/I/O for other fields (data quality), don't propagate that pattern — call the pure `generate_narrative(summary)` directly inside the builder that has no I/O dependency. Keeps the public endpoint's signature/call site unchanged and avoids redundant narrative computation at the route layer.
- 2026-06-15 (Issue #676): When a public response schema (`PublicBacktestView`) needs fields that are *always* present on the source model with non-null defaults (`BacktestRun.fee_rate`/`slippage_rate`/`spread_rate` all have `Field(default=...)`), declare them as required `float` (not `Optional`) on the schema — avoids `Optional`-handling on the frontend for data that can never actually be missing, unlike the cost USD totals (`total_fees_usd` etc.) which are genuinely nullable for older runs.
