- 2026-06-08 (Issue #557): Expanding a shared union type to retire a value (`ResultsViewedEntryPath`'s `"manual"` → `"unknown"` per ADR-0009) is a one-line type edit but `npx tsc --noEmit` immediately surfaces every literal that depended on the retired value — both production code (the manual backtest page's hardcoded fallback) and test fixtures (`it.each([...])` lists, `as const` literals, `rerender({ entryPath: "manual" })`). Budget for fixing all of them in the same slice; an "isolated module" issue can still ripple into a handful of consuming files when its acceptance criteria explicitly call for a shared type to change.
- 2026-06-08 (Issue #557): When an issue says a type "expands to" a new value set that *retires* an old value (here `manual` is explicitly retired by ADR-0009, split into `blank_canvas`/`template_clone`), prefer the most honest interim replacement for any now-invalid literal over inventing a new one — e.g. the manual backtest page's `"manual"` fallback became `"unknown"` (it genuinely doesn't know the persisted path yet) rather than guessing `"blank_canvas"`. This keeps the slice both compiling and semantically correct, and gives the next slice (#559, which wires the true persisted path) an honest baseline to improve on.
- 2026-06-08 (Issue #557): For a small pure mapping function with a fully-specified branch table (here: 4 known inputs + null + garbage → 6 distinct outcomes), the vertical-slice TDD loop collapses naturally — writing the minimal `switch` for the first test case (`nl_wedge`) already implements the remaining branches, so subsequent `it`/`it.each` blocks go straight to GREEN. That's expected, not a smell: the interface was simple enough that "minimal code for this test" and "minimal code for the whole spec" coincided. Add a regression case for the *retired* value (`"manual"` → now `unknown`, not passed through) so the union-narrowing isn't silently lost in a future refactor.
- 2026-05-31 (Impeccable live): When a live-picked element uses generic utility classes shared across repeated containers, the wrap helper can land on the first sibling even with a text hint. Verify the wrapper location before authoring variants; if it wrapped the wrong section, move the live wrapper before replying `done` so accept/discard touches the intended source.
- 2026-05-31 (Issue #490): Binance `/api/v3/ticker/24hr` rejects whitespace in the `symbols` array param → HTTP 400, error -1100 "Illegal characters". `json.dumps(symbols)` defaults to a `", "` item separator, so the multi-symbol batch always fails; the fix is `json.dumps(symbols, separators=(",", ":"))`. A single-symbol request has no comma/space, so naive one-symbol smoke tests pass and hide the bug — always exercise the real multi-asset request, and write the regression test against the *serialized request* (`mock_client.get.call_args.kwargs["params"]["symbols"]`), not the live API.
- 2026-05-31 (Issue #490): The 400 above tripped the transient circuit breaker every refresh cycle; with CryptoCompare also quota-tripped, `PriceRouter.get_spot_prices` skipped both providers and returned `{}` *without raising* (skips never set `last_error`). `_fetch_full_ticker_items` then filled 51 zero-price placeholders, so logs read "wrote 51 prices to cache" while the market page showed all zeros. Lesson: an empty/all-skipped router result should not be persisted as real prices over good cache.
- 2026-05-31 (Issue #490, follow-up): A "missing data" sentinel must be falsy or omitted, never a zero-valued object. `CryptoCompareProvider` returned `SpotPrice(price=0)` for assets it couldn't price; because that object is truthy, the router's `remaining = [a for a in remaining if a not in merged]` treated the asset as satisfied — silently defeating the Binance fallback AND injecting a fake 0.0 into the cache. Binance already omitted missing assets; the fix made CryptoCompare match. Rule: providers omit on miss; the router only ever holds real prices.
- 2026-05-31 (Issue #490, follow-up): Spot-cache refresh now merges over last-known-good (`_merge_with_cached`): fresh prices win per pair, pairs absent this cycle retain their previous value, and an empty fetch leaves the cache untouched. A periodic single-writer cache should degrade by preserving the last good snapshot, not by overwriting it with a partial/empty result. Dedup/merge by a stable key (pair), not by list position.
- 2026-05-31 (Issue #490): The Compose stack bakes backend source into the image (no bind mounts for `api`/`worker`/`scheduler`). Editing host files and running `docker compose up -d` only restarts the *old* image — backend hotfixes require `docker compose build worker scheduler api && up -d`. Verify the running code with `inspect.getsource()` inside the container, not just the host file.
- 2026-05-29: Query-derived edit forms need an explicit snapshot/hydration path before entering edit mode. If local form state defaults are left untouched after async data loads, a no-op save can PATCH default values over an existing record. Add a regression test that loads an existing entity, opens edit mode, saves without field changes, and asserts the original values are sent.
- 2026-05-29: Alert-list load failures must remain distinguishable from a legitimate zero-alert account. When `data = []` is used as a render fallback, pair it with the query's `isError` state for the empty-state branch and retry controls so `/alerts/` failures do not invite users to create a new alert as if loading succeeded.
- 2026-05-29: When migrating primary page loads to TanStack Query, never destructure only `data = []` and `isLoading` for required list data. Also consume `isError`/`error` and render a load-failure path; otherwise failed initial loads are indistinguishable from a legitimate empty list.
- 2026-05-27 (FEAT-459): When publishing promotes a draft row in-place (mutating `version_number` + `status`), the max published version number must be computed against `status=PUBLISHED` rows only — otherwise the draft's `version_number=0` slot pollutes the max and the sequential numbering breaks. Always scope aggregate queries to the target status.
- 2026-05-27 (FEAT-459): After a successful publish, clearing `lastPersistedAt` in the reducer naturally disables the Publish button (no draft exists) without any extra boolean flag. State machine design: let one piece of state (the timestamp) serve double duty as both the "relative time" display and the "has draft" signal.
- 2026-05-26 (FEAT-454): When a parent renders a `CanvasStateProvider`, it cannot call `useCanvasState()` itself (hook must be inside the provider). The fix is the "bridge component" pattern: create small child components (`CanvasBootstrapper`, `ContextDispatchBridge`) that call the hook and populate parent-owned `useRef` callbacks, which the parent uses in its async handlers without direct context access.
- 2026-05-26 (FEAT-454): `CanvasFlags` (from `getCanvasFlags()`) is typed as `Record<CanvasFlagKey, boolean>` where `CanvasFlagKey` is the string-value type of `CANVAS_FLAGS`. Flag access must use `flags[CANVAS_FLAGS.inlinePopover]`, not `flags.inlinePopover` — the latter is a TypeScript error because the key is a string literal value, not a named property.
- 2026-05-26 (FEAT-454): When refactoring a hook's callback signature (e.g., `setNodes: Dispatch<SetStateAction<Node[]>>` → `onNodesChange: (nodes: Node[]) => void`), always update both the implementation and every test that instantiates the hook with the old API. TypeScript will catch these at `tsc --noEmit` time if the tests are `.ts`/`.tsx` files.
- 2026-05-20: Frontend Docker builds must exclude host `node_modules` and `.next` from the build context. If `COPY . .` runs after `npm ci`, a missing `.dockerignore` can overwrite clean image dependencies with stale local packages, causing mismatched Next.js and `@next/swc` versions even when `package.json` and `package-lock.json` are correct.
- 2026-05-20 (FEAT-113): `python-jose` is abandoned (CVE-2024-33663). Replacement is `joserfc==1.6.5`. Key differences: (1) `joserfc.jwt.decode` does NOT validate `exp`/`nbf`/`iat` automatically — call `JWTClaimsRegistry().validate(decoded.claims)` explicitly after decode; (2) the key must be a JWK object — use `joserfc.jwk.import_key("secret-string", "oct")` for HMAC and `joserfc.jwk.RSAKey.import_key(pem)` for RSA; (3) `exp` must be an int unix timestamp in claims (datetime objects also accepted by joserfc but int is explicit and safe); (4) `algorithms=[...]` is still a supported parameter on `jwt.decode` for explicit algorithm allowlisting (FEAT-106 pattern unchanged).
- 2026-05-18 (FEAT-108): Shell smoke-test snippets run under `zsh` should not assign to `status`; it is a read-only shell parameter. Use a neutral variable such as `rc` when preserving a command exit code after `curl`.
- 2026-05-18 (FEAT-117): Next.js builds can patch missing optional `@next/swc-*` entries into `frontend/package-lock.json` even when `next` itself is unchanged. If `next build` prints "Found lockfile missing swc dependencies, patching", run `npm install` afterward and keep the SWC optional lockfile entries if the subsequent build is clean.
- 2026-05-18 (FEAT-116): Dependency-upgrade planning should inspect package metadata for the exact target version rather than infer compatibility from release notes alone. For FastAPI, downloading the exact wheel and reading `Requires-Dist: starlette...` gives a concrete constraint to compare with `backend/requirements.txt` and record in the plan.
- 2026-05-18 (FEAT-112): Treat upstream source releases and container image tags as separate evidence. For Redis security verification, cite official Redis release notes or `redis/redis` releases for the patched source tag, then check Docker registry availability for the exact Compose candidate tag before recommending a follow-up image update.
- 2026-05-16 (FEAT-107): Container image security fixes should verify both source definitions and rendered Compose output. Use `rg` to prove floating tags are gone and `docker compose config` to prove the effective service image resolves to the patched pinned tag.
- 2026-05-16 (FEAT-106): Security hardening specs can be verification-only when the code is already in the desired state. Still add regression tests that exercise both the helper and an authenticated endpoint, so future edits cannot remove the explicit JWT algorithm allowlist unnoticed.
- 2026-05-16 (FEAT-105): For transitive security fixes, pin the vulnerable package directly in `backend/requirements.txt` next to the parent dependency and verify with both an exact `rg` requirement check and `pip show` from the project virtualenv. The resolver may install a newer patched version than the floor; record the resolved version in task tracking.
- 2026-05-13: CI workflows must not call `npm test` directly for the frontend until a `test` script exists. Use `npm run test --if-present` so lint/build remain enforced and a future test script starts running automatically.
- 2026-05-04 (FEAT-102): pandas-ta-classic normalizes MACD periods by swapping `fast` and `slow` when `slow < fast`. Wrapper column lookup must use normalized `min(fast, slow)` / `max(fast, slow)` names, otherwise valid reversed strategy params look for nonexistent `MACD_26_12_9` columns and raise KeyError.
- 2026-05-04 (FEAT-102): pandas-ta Bollinger column names encode std_dev with full float precision (e.g. `BBU_20_2.25`), not one decimal place. Column lookup must use prefix discovery (`next(c for c in df.columns if c.startswith(f"BBU_{period}_"))`) rather than `:.1f` format, which silently rounds 2.25 → "2.2" producing a KeyError at runtime.
- 2026-05-04 (FEAT-102): Test references must always bypass the implementation under test. When both the endpoint and the test reference call the same `ind.*` wrapper, a bug in the wrapper makes both wrong identically and the test passes. Use raw `ta.*` calls (or a pure-math formula) as the independent oracle. If the indicator is a custom implementation (e.g. Fibonacci), write a standalone `_fib_ref()` function that replicates the math without calling the production function.
- 2026-05-04 (FEAT-102): When `pytest.ini` uses `pytest-env` to set `DATABASE_URL`, any lazy import of `app.main` inside a test function body will trigger `Settings()` with the pytest.ini value — even if conftest.py set `os.environ` at module level. Fix: set `os.environ` at the very top of the test file (before all imports) and import `app.main` at module level so `Settings()` sees the override at collection time, not run time.
- 2026-05-04 (FEAT-102): Parity tests for the backtest path must call `interpret_strategy()` + `run_backtest()`, not just raw indicator functions. Only calling indicators directly leaves the interpreter's `_compare(None)→False` guard and the engine's trade logic completely untested. The signal parity assertion pattern is: `assert entry == (val is not None and val <op> threshold)`.
- 2026-05-04 (FEAT-102): Chart-data parity tests must call `GET /market/chart-data` via the HTTP client, not indicator functions directly. The endpoint adds auth, candle fetching, series serialization, and key-naming layers — all of which are invisible to direct function calls. Use the `client` fixture and `_series_values(body["indicators"], key)` to extract per-series values for comparison.
- 2026-05-04 (FEAT-102): Determinism tests (`TC-07`, `TC-08`) must repeat the full computation path (both `interpret_strategy+run_backtest` or both HTTP calls) and compare output metrics, not just raw indicator arrays. Comparing `BacktestResult.num_trades`, `total_return_pct`, `final_balance`, `win_rate_pct`, and `Trade.pnl/entry_price/exit_price` gives meaningful determinism coverage.
- 2026-05-03 (FEAT-102): pandas-ta-classic (`pandas_ta_classic`) is the maintained Python 3.12 + numpy 2.x compatible fork of pandas-ta. Column name convention: `SMA_{p}`, `EMA_{p}`, `RSI_{p}`, `ATRr_{p}`, `OBV`, `MACD_{f}_{s}_{sig}`/`MACDs_…`/`MACDh_…`, `BBU_{p}_{sd}`/`BBM_…`/`BBL_…`, `STOCHk_{k}_{d}_{sk}`/`STOCHd_…`, `ADX_{p}`/`DMP_{p}`/`DMN_{p}`, `ITS_{t}`/`IKS_{k}`/`ISA_{t}`/`ISB_{k}`.
- 2026-05-03 (FEAT-102): pandas-ta RSI and ATR warmup is `period-1` candles (first valid value at index `period-1`, not `period`). Tests asserting warmup should use `range(period-1)`, not `range(period)`.
- 2026-05-03 (FEAT-102): pandas-ta `stoch()` returns a trimmed DataFrame starting at index `k_period-1`. Use `s.reindex(range(n))` (as in `_ta_adapter.from_series`) to pad missing leading indices with NaN → None.
- 2026-05-03 (FEAT-102): pandas-ta OBV seeds at `volume[0]` with no warmup period (index 0 is valid). Old hand-rolled OBV returned None at index 0 — update tests that assert OBV[0] is None.
- 2026-05-03 (FEAT-102): Ichimoku ISB (Span B) is displaced by `kijun` periods. First valid ISB index is `senkou + kijun - 1` (e.g., 52+26-1=77 for default params), not just `senkou`.
- 2026-05-07 (FEAT-103): When a component uses a single `error` state for both action-scoped errors (e.g. clone failure) and page-scoped errors (e.g. strategies load failure), the render guard must check a secondary flag (`strategiesLoadFailed`) to decide whether to show a retry button — creating an always-paired condition that is easy to forget. Routing action errors to `toast.error()` and keeping `error` solely for blocking load failures eliminates the secondary flag from the render: every remaining `{error && (...)}` case now unconditionally shows the retry button, and no inline banner is reserved for action failures.
- 2026-05-03 (FEAT-101): Transient feedback (backtest start confirmation, shortcut guidance) was previously stored in React state as an inline banner. Replacing it with Sonner toast removes the state variable entirely — no clearing on re-submit needed, no layout disruption, and no risk of stale banners persisting across navigation events.
- 2026-05-03 (FEAT-101): The frontend has no `npm test` script. All FEAT-101 TCs are verified manually in the dev server; the test plan documents this gap. Do not attempt to run `npm test` as part of the build step.
- 2026-05-02: Before committing local agent setup, check `git diff --cached --name-status` for tool state such as `.codex/`, auth files, sessions, and logs. Keep those paths ignored and out of repository history.
- 2026-05-03 (FEAT-100): Multi-output indicators (MACD, Bollinger, Stochastic, ADX, Ichimoku, Fibonacci) each return multiple named series from a single backend indicator key. Any future indicator feature touching these must account for series key namespacing (e.g. `macd`, `macd_signal`, `macd_hist`) rather than assuming one key = one series.
- 2026-05-03 (FEAT-100): The frontend has no test runner. Backend TCs pass automatically via pytest; frontend TCs are manual-only until a Playwright suite is introduced. Document this gap in the test plan coverage table rather than leaving it implicit.
- 2026-05-03 (FEAT-100): The indicator catalog in the chart panel is derived from `BLOCK_REGISTRY` (canvas blocks). When adding new strategy indicator blocks, the chart panel automatically gains the new indicator — no separate sync needed.
- 2026-05-03 (FEAT-100): Async chart fetch hooks must guard state commits with a latest-request token, not just an is-mounted check. Dependency changes can keep the component mounted while older `/market/chart-data` responses are still in flight.
- 2026-05-03: UI readout state derived from chart interactions must reset on chart context changes such as asset or timeframe changes. Keeping hover/crosshair state above the chart canvas can otherwise display stale candle data after the data source changes.
- 2026-05-03 (FEAT-101): Global keyboard shortcut handlers that trigger toasts must guard `KeyboardEvent.repeat` or use stable toast ids. A held shortcut can otherwise enqueue duplicate transient notifications faster than users can dismiss them.
- 2026-05-03: Daily chart candles should use date-only `YYYY-MM-DD` / BusinessDay values in lightweight-charts. Offset-less ISO datetimes must be parsed as UTC for intraday candles, otherwise browsers interpret them as local time and can shift UTC chart labels by one day.
- 2026-05-15 (spec-from-issue): Test-plan commands must reflect commands that actually exist in this repo. Do not invent `npm test -- --testPathPattern=...` entries when no frontend test script/spec files exist; prefer validated repo-level commands (`npm run lint`, `npm run build`) plus explicit manual verification steps where automation is unavailable.
- 2026-05-28 (Issue #475): `useMutation.mutate()` returns `void` — the mutation runs in a Promise chain the caller cannot await. In tests, use `waitFor` to check that API mocks were called; direct synchronous assertions after `act(() => mutate(...))` will fail for network-level checks. Optimistic state updates that use direct `setState` calls (before `mutate`) remain synchronous and can still be checked synchronously.
- 2026-05-28 (Issue #475): For hooks with manual pagination/accumulation (load-more pattern), use `queryClient.fetchQuery` (imperative) rather than `useQuery` (declarative). `useQuery` re-fetches whenever its key changes, which breaks debounced search — changing the search input state immediately changes the key and fires a fetch before the debounce timer fires.
- 2026-05-28 (Issue #475): Place `setQueryData` before the first `await` in a `useMutation` `onMutate` handler so the optimistic update is synchronous in the JS call stack. If `setQueryData` comes after an `await`, React may not see the update until a later microtask tick, requiring `waitFor` in tests rather than a synchronous assertion.
- 2026-06-08 (Issue #556): When two frontend routes share one backend create endpoint (blank-canvas modal and wizard both call `POST /strategies/`) but need to be distinguished server-side, the simplest fix is a client-supplied enum field on the request schema (`entry_path`), validated server-side against a deny-list (`CLIENT_STAMPABLE_ENTRY_PATHS`) that excludes values only a dedicated route may stamp (`template_clone` stays 422 from the shared endpoint, stamped unconditionally only inside `clone_template`). No new endpoint or routing logic needed.
- 2026-06-08 (Issue #556): Adding a required field to a shared TypeScript domain type (`Strategy.entry_path`) immediately surfaces every test fixture that builds a `Strategy` object literal as a `TS2741` "missing property" error — `npx tsc --noEmit` finds them all in one pass. Fix by adding the field to each mock (`entry_path: null` mirrors the nullable-no-backfill semantics) rather than loosening the type.
- 2026-06-08 (Issue #556): To prove a large `tsc --noEmit` error count is pre-existing noise (e.g. missing `node_modules` in a remote env) rather than a regression, `git stash` the change, re-run, and diff the count — identical totals before/after confirm the baseline is untouched. `frontend/node_modules` can be entirely absent in this remote environment, which also makes `npx vitest run` fail at config-load time (`Cannot find module 'vitest/config'`); `tsc --noEmit` is then the only available frontend correctness check.
- 2026-06-08 (Issue #556): Forward-written documentation (ADR-0009, runbook §7, ANALYTICS_SETUP) can describe a multi-slice initiative's *target end state* in present tense before every slice ships — e.g. the runbook claims the manual backtest page "no longer hard-codes `manual`" even though that depends on a later frontend slice this issue doesn't touch. Don't "fix" such text mid-initiative; verify it doesn't contradict what *this* slice shipped, and leave end-state descriptions for the slice that actually delivers them.
